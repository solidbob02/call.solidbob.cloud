<!-- 원본: 실시간-상담원-어시스트-RAG-기획서-rev4.1.md (2026-08-26 수령, 3인 팀 버전) -->
<!-- = _project/plan.md(rev.4) + _project/rev4-보완지시서.md 6항목 병합본. 6항목은 2026-08-25 저장소에 반영 완료(트리거 1,500ms·trigger.py·decisions/002·003). -->
<!-- 이 파일은 수정하지 않는다. 이후 변경은 _project/decisions/ 를 따른다. 구조 규칙은 _docs/architecture.md, 도메인은 _docs/domain.md. -->
<!-- 공개 저장소다 — 부록 C "개별 사건 자료의 내부 보관" 지침에 따라 개별 사건 링크·정보는 여기에 두지 않는다. -->

# 실시간 상담원 어시스트 RAG 시스템

> **주제**: LLM — Call Center 업무 자동화 서비스
> **한 줄 정의**: 통화를 실시간으로 들으면서, 고객이 방금 물은 내용에 필요한 사내 문서를 상담원 화면에 자동으로 띄워주는 시스템
> **작성일**: 2026-08-24 (rev.4.1 — 보완지시서 반영)
> **팀**: 정성윤(AWS·인프라) / 류준(백엔드·AI) / 장민석(앱·프론트엔드)

---

## 목차

- [rev.4 변경 요약](#rev4-변경-요약)
- [1. 주제 선택 근거](#1-주제-선택-근거)
- [2. 기능 명세](#2-기능-명세)
- [3. 시스템 아키텍처](#3-시스템-아키텍처)
- [4. 핵심 기술 난제](#4-핵심-기술-난제)
- [5. 데이터 확보 계획](#5-데이터-확보-계획)
- [6. 평가 설계](#6-평가-설계)
- [7. 팀 분업](#7-팀-분업)
- [8. 8주 마일스톤](#8-8주-마일스톤)
- [9. 레퍼런스 분석](#9-레퍼런스-분석)
- [10. 차별화 포인트](#10-차별화-포인트)
- [11. 리스크 및 대응](#11-리스크-및-대응)
- [부록 A — 민감 기능 공통 설계 원칙](#부록-a--민감-기능-공통-설계-원칙)
- [부록 B — 추가 검토 모듈 (H·I)](#부록-b--추가-검토-모듈-hi)
- [부록 C — 즉시 착수할 항목](#부록-c--즉시-착수할-항목)

---

## rev.4.1 보완 요약

rev.4에 대한 보완지시서를 반영했다. **아키텍처 구성요소와 투입자원 스택은 여전히 변경 없다.**

| # | 항목 | 반영 |
|---|---|---|
| 1 | **트리거 허용 창이 3주차 침묵 기반 구현과 충돌** | 0~800ms → **0~1,500ms**. 지연시간 분포(p50/p95) 기록 추가. 1주차에 침묵 임계값 결정 |
| 2 | **GPU 미확보 시 생성 경로 폴백 부재** | **폴백 모드(검색 스니펫 직접 표시)** 명시. 리스크 대응이자 실험 조건으로 활용 |
| 3 | **F-2 순환 검증 문제 누락** | 5.5절 한계 표에 추가. 규정 작성 / 스키마 구현 / 골든셋 라벨링 **담당 분리** 원칙 신설 |
| 4 | C-5 배정이 정규식/NER 구분 없음 | P1~P5(인프라) / P6~P7(AI, 여유 시)로 분리. 절대 규칙 범위를 **P1~P5**로 한정 |
| 5 | 아키텍처 "변경 없음" 문구가 부정확 | "구성요소 추가 없음, 내부 모듈 명시"로 정정 |
| 6 | 개별 사건 자료 추적성 소실 | 팀 내부 공간 보관 지침을 부록 C에 추가 |

### 3번이 왜 중요한가

문서는 C-5, F-1·G-1, H-1에 대해 순환 검증 문제를 정직하게 지적했으나 **F-2에만 적용하지 않았다.** 팀이 만든 규정을, 팀이 만든 게이트가, 팀이 만든 골든셋으로 검증하면 100%는 당연하다. 자기 기준을 일관되게 적용하지 않은 유일한 지점이었다.

---

## rev.4 변경 요약

교차검증에서 지적된 항목을 반영했다. **아키텍처 구조와 투입자원 스택은 변경하지 않았다.** 새 DB, 새 검색엔진, 새 통신 프로토콜, 목록 밖 도구 — 아무것도 추가되지 않는다.

| # | 변경 | 성격 |
|---|---|---|
| 1 | **F 모듈 도메인을 통신사 업무로 통일** | 구조 — 아래 상세 |
| 2 | 생성 모델을 HuggingFace Transformers 용도에 명시 | 기존 스택 내 역할 명확화 |
| 3 | 트리거 "적절 시점"의 허용 창 정의 *(rev.4.1에서 1,500ms로 조정)* | 1주차 블로커 해소 |
| 4 | C-5 목표를 측정 가능한 형태로 재정의 + 지표 우선순위 명시 | 평가 설계 |
| 5 | 골든셋 일정 재배치 (1주차 10개 → 2주차 50개 → 3주차 150개) | 순환 의존 해소 |
| 6 | 레이턴시를 내부 처리 / E2E 두 지표로 분리 | 측정 정직성 |
| 7 | 화자 분리 전제 확인을 1주차 태스크로, 리스크 표에 추가 | 누락 보완 |
| 8 | 팀 분업표를 실제 3인 구성으로 재작성 | 누락 보완 |
| 9 | 8주차 성공 조건 재설정 + F-2 폴백 명시 | 범위 관리 |
| 10 | "불변 감사 로그" → "추가 전용 감사 로그" 등 표현 정정 | 주장의 한계 준수 |

### 1번 상세 — F 모듈 도메인 통일

이전 리비전은 F 모듈(종결 요건 검증)을 **실종신고 접수·종결**이라는 공공기관 업무로 설계했다. 그러나 코어(A~E)는 통신사 약관 기반 기업 콜센터다. 두 시스템 사이에 재사용되는 것이 거의 없어, **두 번째 지식베이스 + 두 번째 데이터 모델 + 두 번째 화면 + 두 번째 골든셋**이 필요했다.

**rev.4는 F-2를 통신사 업무 안의 동일 패턴으로 옮긴다.**

| | rev.3 (폐기) | rev.4 |
|---|---|---|
| 대상 업무 | 실종신고 종결 | 해지·명의변경·보상 처리 종결 |
| 지식베이스 | 별도 (업무처리 규칙) | **코어와 동일 ES 인덱스** (약관 + 응대 매뉴얼 + 내부 처리 규정) |
| 데이터 모델 | 별도 (신고 사건) | **코어 `call` 확장** (종결 사유 + 근거 필드) |
| 화면 | 별도 종결 폼 | **기존 3분할 대시보드 + 모달** |
| 골든셋 | 별도 구축 | **코어 셋에 케이스 추가** |

**차별화 서사는 유지된다.** 10.6의 주장은 "RAG를 검증 도구로 쓴다"이지 특정 도메인이 아니다. 오히려 작동하는 시스템 안에서 시연되는 편이 강하다.

**사회적 문제의식도 유지된다.** "근거 없는 종결이라는 실패 구조"로 일반화하여 문제 정의에만 사용하고, 개별 사건은 구현 대상에서 분리한다.

### 모듈 우선순위

| 모듈 | 내용 | 우선순위 |
|---|---|---|
| A~E | 실시간 어시스트 코어 | **필수 — 절대 사수** |
| C-5 | 개인정보 마스킹 | **필수 — 코어 포함, 3주차** |
| F-2 | 종결 요건 검증 게이트 | 조건부 (7주차 체크포인트) |
| F-3·F-4 | 반복 처리 연결 / 감사 로그 | 여유 시 |
| G-2 | 지역 자원 연계 검색 | 여유 시 |
| H·I | 부록 B | 코어 완료 시 택1 |

---

## 1. 주제 선택 근거

### 1.1 투입자원 목록이 이미 답을 가리킨다

제시된 투입자원 중 **Google Speech-to-Text API**와 **WebSocket**은 리뷰 감정 분석 주제에서는 쓸 데가 없다. 이 두 도구가 목록에 있다는 것은 콜센터 주제를 염두에 둔 구성으로 보인다.

| 도구 | 콜센터 주제 | 리뷰 감정 분석 |
|---|---|---|
| Google STT | 필수 | 불필요 |
| WebSocket | 필수 | 불필요 |
| Elasticsearch | 핵심 | 선택 |
| HuggingFace | 활용 | 활용 |

### 1.2 "통화 후" 대신 "통화 중"

흔한 콜센터 프로젝트는 녹음 파일을 사후 처리한다(전사 → 요약 → 티켓 생성). 이는 사실상 **배치 파일 변환 파이프라인**이며 WebSocket이 필요 없다.

본 프로젝트는 **통화가 진행되는 중에 개입**한다.

| | 사후 처리형 | 실시간 개입형 (본 프로젝트) |
|---|---|---|
| 입력 | 완성된 녹음 파일 | 0.3초 단위 스트리밍 조각 |
| 질의 | 완결된 문장 | 미완성 발화 |
| 레이턴시 요구 | 없음 | 1초 이내 |
| RAG 난이도 | 낮음 | 높음 (트리거 판정 + 오류 내성) |
| 제품 범주 | 통화 분석 | Agent Assist |

---

## 2. 기능 명세

### 2.1 화면 구성

```
┌──────────────────────┬────────────────────────┬──────────────────┐
│  실시간 자막          │  추천 카드              │  경고            │
│                      │                        │                  │
│ 고객                 │ ┌────────────────────┐ │ ⚠ 확정적 보장    │
│  아 저번에 가입할     │ │ 프로모션 할인       │ │   표현 감지      │
│  때 6개월 할인       │ │ 적용 시점 안내      │ │                  │
│  된다고 했는데       │ │                    │ │  "무조건        │
│  지금 요금 보니까    │ │ 신규 가입 할인은   │ │   환불됩니다"    │
│  안 빠진 것 같아서요 │ │ 가입 다음 달       │ │   (14:32)       │
│                      │ │ 청구서부터 반영.   │ │                  │
│ 상담원               │ │ 첫 달 미반영이     │ │ → 권장:          │
│  네 확인해           │ │ 정상입니다.        │ │  "확인 후        │
│  드리겠습니다        │ │                    │ │   안내드리겠     │
│                      │ │ 📄 약관 3.2조      │ │   습니다"        │
│  카드번호는 ****     │ │    유사도 0.87     │ │                  │
│                      │ └────────────────────┘ │                  │
└──────────────────────┴────────────────────────┴──────────────────┘
                                          F-2 게이트는 이 화면 위 모달로 표시
```

**핵심**: 상담원은 검색창에 타이핑하지 않는다. 시스템이 대화를 듣고 알아서 찾는다.

### 2.2 기능 목록

#### A. 실시간 음성 → 텍스트

| 기능 | 설명 | 도구 |
|---|---|---|
| A-1 | 스트리밍 STT (부분 결과 포함) | Google STT |
| A-2 | 화자 분리 (고객 / 상담원) | Google STT diarization 또는 채널 분리 |
| A-3 | 브라우저 실시간 전달 | WebSocket (Node.js) |
| A-4 | 발화 구간 검출 (침묵 판정) | 직접 구현 |

> **A-2 전제 확인 필요**: 실무 콜센터는 대개 2채널 스테레오(고객/상담원 물리 분리)를 사용해 diarization이 불필요하다. AI Hub 데이터의 채널 구성에 따라 난이도가 갈리므로 **1주차에 반드시 확인**한다(5.6절).

#### B. 자동 문서 추천 — **RAG 핵심**

| 기능 | 설명 |
|---|---|
| B-1 | 검색 트리거 판정 (언제 검색할지 결정) |
| B-2 | 하이브리드 검색 (형태소 + 임베딩) |
| B-3 | 리랭킹 (상위 후보 재정렬) |
| B-4 | 근거 기반 요약 카드 생성 |
| B-5 | 출처 표시 (문서명 + 조항 + 유사도) |
| B-6 | 근거 부족 시 "관련 문서 없음" 반환 |

#### C. 실시간 컴플라이언스 감지 + 마스킹

| 기능 | 설명 |
|---|---|
| C-1 | 확정적 보장 표현 탐지 ("무조건", "100% 환불") |
| C-2 | 불필요한 개인정보 요구 탐지 |
| C-3 | 필수 안내 누락 감지 (해지 시 위약금 고지 등) |
| C-4 | 권장 대체 표현 제시 |
| **C-5** | **개인정보 실시간 마스킹** |

> C-1~C-4는 KcELECTRA 계열 분류기 파인튜닝으로 구현. **재현율 우선 설계** — 놓치는 것이 과잉 경고보다 위험하다.

##### C-5 상세 — 코어에 포함

이 시스템은 **실시간 자막을 화면에 띄우고 전사 결과를 DB에 저장**한다. 마스킹이 없으면 그 자체가 설계 결함이다.

| 항목 | 처리 |
|---|---|
| 적용 지점 | ① 화면 자막 표시 전 ② PostgreSQL 저장 전 — **양쪽 모두** |
| 원본 보관 | 하지 않음. 마스킹된 형태만 저장 |
| 착수 시점 | **3주차 — 자막 구현과 동시에** |

**탐지 파이프라인** — STT 출력은 정형 문자열이 아니므로 정규화가 선행된다.

```
STT 전사 결과
   ↓
① 구분자·공백 제거 후 연속 숫자열 판정   ← 주 실패 모드 대응
   "123456 1234567" / "1234561234567" 모두 포착
   ↓
② 한글 수사 → 숫자 변환 (보조)
   "구일공" → "910"
   ↓
③ 패턴 매칭 (정규식) + NER (이름·주소)
   ↓
④ 마스킹
```

> **주의**: Google STT 한국어는 긴 숫자열을 대체로 아라비아 숫자로 정규화하므로, 한글 수사보다 **구분자 부재와 띄어쓰기 붕괴**가 지배적 실패 모드다. ①을 ②보다 앞에 둔다.

**대상 패턴 목록** (문서에 고정하고, 이 목록에 대해 누락 0건을 목표로 한다)

| # | 패턴 | 방식 |
|---|---|---|
| P1 | 주민등록번호 (13자리) | 정규식 |
| P2 | 카드번호 (14~16자리) | 정규식 |
| P3 | 계좌번호 (10~14자리) | 정규식 |
| P4 | 휴대전화번호 | 정규식 |
| P5 | 인증번호 (4~6자리, 문맥 조건) | 정규식 + 문맥 |
| P6 | 인명 | NER |
| P7 | 상세주소 | NER |

**지표 우선순위** — 두 목표는 동시에 최적화할 수 없으므로 순서를 고정한다.

> **누락 0건 > 과잉 마스킹 억제.** 애매하면 가린다. 과잉 마스킹률은 목표가 아니라 참고 기록이다.

#### D. 통화 후 처리

| 기능 | 설명 |
|---|---|
| D-1 | 상담 요약 생성 |
| D-2 | 문의 유형 자동 분류 |
| D-3 | 후속조치 항목 추출 |
| D-4 | **지식베이스 공백 리포트** — 검색 실패 질문 집계 |

**D-4 확장 — 탐지 실패도 같은 루프에 넣는다**

| 대상 | 누적하는 것 | 쓰임 |
|---|---|---|
| B (검색) | 답을 못 찾은 질문 | 지식베이스 보강 우선순위 |
| C (컴플라이언스) | 놓친 위반 표현 | 분류기 재학습 우선순위 |
| F (게이트) | 통과했으나 사후 문제가 된 건 | 요건 스키마 보강 |

#### E. 평가 하네스

| 기능 | 설명 |
|---|---|
| E-1 | 골든셋 기반 자동 채점 (규칙 기반, LLM 채점 배제) |
| E-2 | STT 오류율별 성능 곡선 자동 생성 |
| E-3 | 레이턴시 분포 측정 (p50 / p95 / p99) |
| E-4 | 기준선 미달 시 CI 실패 |

---

#### F. 종결 요건 검증 *(조건부 모듈 — 통신 도메인)*

##### 배경 — 문제 구조

신고·요청 처리 시스템에서 반복되는 실패 유형이 있다. **종결 사유는 기재되었으나 그것을 뒷받침하는 근거가 검증되지 않는 것**이다. 처리자가 "확인했다"고 적으면 그대로 닫힌다.

통신사 콜센터에도 같은 구조가 존재한다.

| 처리 유형 | 규정상 필수 확인 | 누락 시 |
|---|---|---|
| 해지 처리 | 위약금·잔여할부 고지, 고지 확인 응답 | 불완전판매 |
| 명의변경 | 본인확인 수단, 요청 경위 | 명의도용 위험 |
| 보상·환불 | 사유 근거, 승인 권한 확인 | 부적정 처리 |

##### 주장 범위 — 반드시 지킬 것

> **"근거 없는 종결의 비용을 올린다."** — "이런 일을 막습니다"가 아니다.

고의적 허위 기재는 어떤 시스템도 막을 수 없다. 이 구분을 흐리면 프로젝트의 신뢰도가 떨어진다(부록 A-3).

##### 기능 목록

| 기능 | 설명 | 착수 |
|---|---|---|
| F-1 | 고위험 처리 유형 자동 감지 → 확인 항목 사전 표시 | 여유 시 |
| **F-2** | **종결 요건 검증 게이트** — 필수 근거 미기재 시 종결 차단 | 조건부 (7주차) |
| F-3 | 동일 고객 반복 문의 자동 연결 → 이전 종결 사유 재검증 요구 | 여유 시 |
| F-4 | **추가 전용(append-only) 처리 이력** | 여유 시 |

> **F-4 표현 정정**: "불변 감사 로그"는 과장이다. PostgreSQL에서 완전한 불변성은 애플리케이션 레벨로 보장할 수 없다(DB 권한자는 삭제 가능). **추가 전용 테이블 + 정정 이력 보존** 방식으로 기술하고, 한계를 함께 적는다.

##### F-2 상세

```
상담원이 "고지 완료" 사유로 해지 처리 종결 시도
        ↓
[RAG] 코어와 동일한 ES 인덱스에서 해당 처리 유형의 필수 확인 항목 검색
        ↓
[규칙] 근거 필드 충족 여부 판정
        ↓
┌─────────────────────────────────────┐
│ ⛔ 종결 요건 미충족                   │
│                                     │
│ 처리 유형: 해지                       │
│ 필요 근거:                           │
│   ☑ 위약금 안내                      │
│   ☐ 잔여할부금 안내                  │
│   ☐ 고객 확인 응답 기록              │
│                                     │
│ 근거 3건 중 1건 → 종결 차단           │
│ 📄 근거: 응대 매뉴얼 7장 / 약관 5.1조 │
└─────────────────────────────────────┘
```

**RAG의 역할이 여기서 달라진다.** A~E의 RAG는 "답을 찾아 사용자에게 보여준다". F-2의 RAG는 **"행위를 검증할 근거를 가져온다"**. 검색 결과가 화면에 표시되는 정보가 아니라 게이트 판정의 입력이 된다.

판정 자체는 규칙으로 하고 LLM은 설명만 생성한다(부록 A-2).

##### 아키텍처 영향 — 없음

| 필요한 것 | 기존 구성요소로 해결 |
|---|---|
| 규정 검색 | 코어와 동일 ES 인덱스에 내부 처리 규정 추가 적재 |
| 근거 필드 저장 | PostgreSQL `call` 테이블 확장 |
| 차단 UI | 기존 React 대시보드 위 모달 |
| 판정 로직 | FastAPI 코어 내 모듈 |

새 DB, 새 검색엔진, 새 화면, 새 프로토콜 — **아무것도 추가되지 않는다.**

##### 상용 제품 대비 위치

조사 결과 국내 AICC는 STT/TA 기반 **불완전판매 방지**(하면 안 되는 말 감지)를 제공한다. 그러나 **"근거 없이 처리를 닫는 것을 차단하는" 기능은 확인되지 않았다.**

---

#### G. 위기 상황 자원 연계 *(여유 시 — 범위 엄격 제한)*

##### 절대 선

> **LLM이 위기 상황의 당사자와 직접 대화하게 만들지 않는다. 어떤 형태로도.**

시스템은 **상담원을 돕는 도구로만** 존재한다.

##### 단방향 경고 원칙

> **시스템은 "위험 신호가 감지됐다"만 말한다. "안전하다"는 절대 말하지 않는다.**

- 위험도 점수를 화면에 표시하지 않는다. 낮은 점수는 상담원의 경계를 늦춘다
- 신호가 있으면 안내를 띄우고, 없으면 아무것도 하지 않는다
- UI에서 **침묵이 "이상 없음"으로 읽히지 않도록** 명시한다

##### 기능

| 기능 | 설명 | 채택 |
|---|---|---|
| **G-2** | **지역 자원 연계 검색** — 주소지 기준 정신건강복지센터·자살예방센터 등 매칭 | 여유 시 |
| G-1 | 위기 대응 안내 표시 | 제외 (검증 불가) |

##### G-2만 남기는 이유

확장 기능 중 **유일하게 공개 데이터로 정답을 확인할 수 있다.** 관할이 맞는지, 운영시간이 맞는지, 폐지된 기관을 반환하지 않는지가 전부 검증 가능하다.

| 기능 | 탐지·판정 성능 검증 |
|---|---|
| F-1 / G-1 / H-1 / I-1 | 불가 또는 부분 (실제 데이터 없음) |
| F-2 / **G-2** | **가능** |

기술적 화려함이 아니라 **측정 가능성** 때문에 존재한다.

##### 만들지 않는 것

위험도 점수화 및 표시 / 당사자에 대한 자동 응답 / 위치 추적·신고 자동화 / 실제 위기 상담 통화 수집

---

## 3. 시스템 아키텍처

**구성요소 추가 없음.** 새 DB·검색엔진·프로토콜·목록 밖 도구를 도입하지 않았다.

단, rev.3 다이어그램에서 생략되어 있던 **C-5 마스킹 모듈, F-2 게이트 모듈, PostgreSQL 테이블 구성**을 명시적으로 표기했다. 이는 기존 FastAPI 코어와 PostgreSQL 내부를 세분화한 것이며 신규 인프라가 아니다.

```
[상담원 브라우저]
       │ React 대시보드 (자막 / 카드 / 경고 / 종결 모달)
       │
   WebSocket (양방향)
       │
[Node.js 게이트웨이]
       │  - 오디오 청크 중계
       │  - 자막/카드/경고 푸시
       │
       ├──→ [Google STT] ──→ 부분 전사 결과
       │
       ▼
[FastAPI 코어]
       │
       ├─ C-5 마스킹 모듈 ──→ 자막·저장 양쪽 앞단
       │
       ├─ 트리거 판정 모듈
       │     └─ 발화 완결성 / 침묵 길이 / 의도 신호
       │
       ├─ 검색 모듈 ──→ [Elasticsearch]
       │                  ├─ nori 형태소 (BM25)
       │                  ├─ dense_vector (임베딩)
       │                  └─ RRF 병합
       │
       ├─ 생성 모듈 ──→ [HF Transformers] 근거 기반 요약
       │
       ├─ 컴플라이언스 모듈 ──→ [분류기]
       │
       └─ F-2 게이트 모듈 ──→ 같은 ES 인덱스 조회 + 규칙 판정
       │
       ▼
   [PostgreSQL]
     ├─ call            통화 메타
     ├─ transcript      마스킹된 전사
     ├─ recommendation  추천 이력
     ├─ closure         종결 사유 + 근거 필드   ← F-2
     └─ eval_result     평가 결과
```

### 3.1 도구 매핑 (투입자원 기준 — 목록 밖 도구 없음)

| 투입자원 | 활용 방식 | 필수도 |
|---|---|---|
| Python | 전체 백엔드 | 필수 |
| FastAPI | 검색·생성·게이트 API | 필수 |
| Node.js | WebSocket 게이트웨이 | 필수 |
| WebSocket | 실시간 양방향 통신 | 필수 |
| Google STT | 스트리밍 음성인식 + 화자분리 | 필수 |
| Elasticsearch | nori + dense_vector 하이브리드 검색 | 필수 |
| **HuggingFace Transformers** | **① 임베딩 모델 ② 컴플라이언스 분류기 ③ 카드 요약 생성** | 필수 |
| React | 상담원 대시보드 | 필수 |
| PostgreSQL | 통화 메타·전사·추천·종결·평가 결과 | 필수 |
| Git | 형상관리 | 필수 |
| Docker | 서비스 컨테이너화 | 필수 |
| matplotlib | 오류율별 성능 곡선, 레이턴시 분포 | 필수 |
| VSCode / Ubuntu | 개발 환경 / 서버 OS | 필수 |
| Kubernetes / AWS Console | 배포 | 선택 |
| VirtualBox | 로컬 테스트 환경 | 선택 |
| OpenCV | 본 주제에서 활용처 없음 | 제외 |

> **생성 모델 확정**: 별도 상용 API를 도입하지 않는다. **HuggingFace Transformers로 소형 한국어 오픈 모델을 로드해 카드 요약을 생성**한다. 카드 요약은 검색된 근거를 2~3문장으로 다듬는 제한된 작업이라 대형 모델이 필요 없고, 레이턴시에도 유리하다. GPU 가용 여부는 1주차에 확인한다(V2).

> **생성 단계 폴백 (V2 결과에 따라 발동)**
>
> GPU를 확보하지 못하거나 첫 토큰 500ms를 맞추지 못하는 경우, **생성 없이 검색 결과 스니펫을 그대로 카드에 표시하는 모드**를 기본값으로 전환한다.
>
> | 모드 | 카드 내용 | 레이턴시 |
> |---|---|---|
> | 생성 모드 | 근거를 2~3문장으로 요약 | 검색 + 리랭킹 + 생성 |
> | **폴백 모드** | **검색된 원문 스니펫 + 출처** | 검색 + 리랭킹 (생성 단계 제거) |
>
> 폴백 모드에서도 B-5(출처 표시)와 B-6("관련 문서 없음")은 그대로 동작한다. 상담원이 얻는 핵심 가치인 **"찾아주는 것"**은 유지된다.
>
> **폴백은 리스크 대응이자 실험 조건이다.** 미리 만들어두면 "검색 스니펫만 표시할 때 vs 생성 요약 카드를 표시할 때"를 비교해 **생성 단계의 실제 기여도를 수치로** 말할 수 있고, 토큰 비용 대비 효용을 논할 근거가 생긴다.

> **캐시 저장소**: 별도 저장소를 두지 않는다. **프로세스 인메모리 LRU + PostgreSQL 영속화**로 처리한다.

---

## 4. 핵심 기술 난제

### 4.1 트리거 판정 — 언제 검색할 것인가

일반 RAG는 완성된 질문을 받는다. 스트리밍은 다르다.

```
0.4초  "아"
0.9초  "아 저번에"
1.6초  "아 저번에 가입할"
2.4초  "아 저번에 가입할 때 6개월"
3.1초  "아 저번에 가입할 때 6개월 할인 된다고 했는데"
```

- **매 조각마다 검색** → 서버 과부하, 카드 깜빡임
- **너무 늦게 검색** → 대화가 이미 다음으로 넘어감

**검토할 전략**

| 전략 | 방법 | 예상 문제 |
|---|---|---|
| 침묵 기반 | N초 무음 시 검색 | 말이 느린 고객에서 지연 |
| 종결어미 기반 | "~요", "~까요", "~는데" 패턴 | 중간에 끊어 말하면 오발동 |
| 길이 기반 | 토큰 N개 누적 시 | 의미 무관하게 발동 |
| 하이브리드 | 위 조합 + 디바운싱 | 튜닝 복잡 |

**"적절한 시점"의 정의** — 규칙 기반 채점을 위해 허용 창을 고정한다.

> 골든셋의 각 질문에 **발화 종료 시각**을 라벨링한다.
> **발화 종료 후 0 ~ 1,500ms 이내 발동을 "적절"로 판정한다.**
> 1,500ms 초과는 지연 발동, 발동 없음은 미발동으로 각각 별도 집계한다.

**1,500ms인 이유** — 3주차 v1이 침묵 기반이기 때문이다.

```
발화 종료 (t_gold)
   │
   ├─ 침묵 대기 N ms       ← 침묵 기반 트리거의 고유 지연 (통상 700~1,000ms)
   ├─ 트리거 판정 10ms
   └─ 발동 (t_fire)

t_fire − t_gold ≈ N + 10ms
```

허용 창 상한이 침묵 임계값보다 작으면 **거의 모든 발동이 지연으로 집계**되어, 지표가 구현을 평가하는 것이 아니라 구현 방식을 사전에 배제하게 된다. 상한은 `침묵 임계값 + 여유(500ms)` 이상이어야 한다.

> **1주차에 침묵 임계값 N을 결정한다.** AI Hub 음성의 발화 간 간격 분포를 확인해 정하고, 허용 창을 `N + 500ms`로 역산한다. 확인이 어려우면 N=1,000ms를 가정하고 창을 1,500ms로 고정한다.

**허용 창만으로는 부족하다 — 분포를 함께 기록한다**

> **트리거 지연시간 분포(p50/p95)를 별도로 기록한다.**
> 허용 창은 합격·불합격 판정선일 뿐이고, 개선 과정을 보여주는 것은 분포다.
> "v1 침묵 기반은 p50 920ms였고, 종결어미 하이브리드로 p50 610ms까지 줄였습니다" — 이 문장이 지표 하나보다 강하다.

이 정의가 없으면 6.2절의 "모든 지표를 규칙으로 계산한다" 원칙을 지킬 수 없다. **1주차 골든셋 스펙에 포함한다.**

### 4.2 STT 오류 내성 — 가장 중요한 실험

음성인식은 틀린다. 사람은 알아듣지만 검색은 실패한다.

| 실제 발화 | STT 결과 | 문제 |
|---|---|---|
| 해지 | 해제 | 완전히 다른 조항 검색 |
| 요금제 | 요금 제 | 띄어쓰기로 형태소 분해 실패 |
| 위약금 | 위약 금 | 동일 |

**Elasticsearch가 필요해지는 이유**

```
1. nori 형태소 분석기 → 띄어쓰기 오류 흡수
2. 자모 분해 필터      → 유사 발음 오인식 대응
3. dense_vector 필드   → 의미 기반 검색
4. RRF 병합            → 위 3개 결과 통합
```

**대표 실험 (발표의 핵심 숫자)**

```
STT 오류율을 통제하며 주입 → 구간별 성능 측정

오류율      0%    5%    10%   15%   20%
──────────────────────────────────────
BM25만      -     -     -     -     -
벡터만      -     -     -     -     -
하이브리드   -     -     -     -     -
C-5 마스킹  -     -     -     -     -   ← 재현율 곡선 추가
```

> 수치는 실측값으로 채운다. **"오류율 10%에서 Recall@5가 0.48 → 0.66으로 복구"** 형태의 문장 하나가 프로젝트 전체를 설명한다.

**이 실험의 성격을 정확히 서술할 것** — 실제 데이터 부족을 대체하는 합성이 아니라, **구간별 비교를 위해 오류율을 통제한 설계**다. 실제 통화로는 오류율 5%/10%/15% 곡선을 그릴 수 없다.

### 4.3 레이턴시 예산

**두 지표를 분리해 둘 다 측정한다.**

| 지표 | 구간 | 목표 |
|---|---|---|
| **내부 처리 지연** | 트리거 발동 → 카드 표시 | p95 ≤ 1,000ms |
| **E2E 체감 지연** | 발화 종료 → 카드 표시 | **측정·기록 (목표 미설정)** |

E2E에는 STT 부분 결과 반환 지연, 게이트웨이 홉, 브라우저 렌더링이 포함된다. 이 구간은 통제 밖이므로 목표를 걸지 않되, **통제 가능한 구간과 아닌 구간을 분리했다는 사실 자체를 성과로 기록한다.**

**내부 처리 예산**

| 단계 | 목표 | 최적화 수단 |
|---|---|---|
| 트리거 판정 | 10ms | 규칙 우선 |
| 검색 | 150ms | 인덱스 워밍업, 필드 최소화 |
| 리랭킹 | 200ms | 상위 20개만, 배치 추론 |
| 생성 | **첫 토큰 500ms** | **스트리밍 출력** — 카드는 점진 표시 |
| 전송 | 50ms | WebSocket 유지 연결 |

> 카드 완성까지 1초를 기다리게 하지 않는다. 첫 토큰부터 흘려보내면 상담원 체감이 크게 달라진다.

---

## 5. 데이터 확보 계획

### 5.1 지식베이스 — 하나로 유지

**통신사 요금제 약관 + 응대 매뉴얼 + 내부 처리 규정**을 한 ES 인덱스에 적재한다. F-2가 참조하는 종결 요건도 이 인덱스 안에 있다.

- 응대 매뉴얼과 내부 처리 규정은 팀이 직접 작성한다(가상 사업자 기준)
- 약관 원문 전재는 저작권 문제 소지가 있다. 검색 인덱스 내부 저장은 문제없으나 화면 표시는 요약·해석 중심으로 하고 출처를 명시한다

> **규정 문서 작성 시 지킬 것 (F-2 순환 검증 완화)**
>
> 규정도, 게이트도, 골든셋도 팀이 만들면 판정 정확도 100%는 자기충족적 수치가 된다. 이를 줄이기 위해 다음을 지킨다.
>
> 1. **실제 통신사 약관·이용약관의 조항 구조와 표현을 따른다.** 조 번호 체계, 문장 형식, 용어를 실물에 맞춘다
> 2. **규정 작성자와 게이트 구현자를 분리한다.** 규정을 쓴 사람이 요건 스키마를 짜면 해석 오류가 드러나지 않는다
> 3. **골든셋의 F-2 케이스는 규정 작성자가 아닌 사람이 만든다.** 같은 사람이 만들면 규정의 빈틈이 그대로 통과된다
> 4. 위 세 가지를 지켰다는 사실을 문서에 적는다
>
> **3인 배분 예시** — 규정 작성: 장민석 / 스키마·게이트 구현: 류준 / F-2 골든셋 라벨링: 정성윤

이 완화 조치를 지켜도 한계는 남는다. 5.5절에 명시한다.

### 5.2 평가용 통화 음성 — AI Hub 실제 데이터

| 데이터셋 | 규모 | 특징 |
|---|---|---|
| AI Hub 「상담 음성」 | 3,000시간 | 교육·금융·통신판매, 가상 시나리오, 저작권 해결 완료 |
| AI Hub 「고객 응대 음성」 | 3,300시간 | 5개 클래스, 감정·의도 태깅 및 요약문 포함 |
| AI Hub 「저음질 전화망 음성」 | — | 실제 상담 환경 잡음 포함 |
| 서울 열린데이터광장 「행정 민원상담 음성」 | 56개 시나리오 유형 | 8kHz, 연령·지역·성별 조합 |

**주의**: 휴대폰 본인인증이 필요하고 승인에 시간이 걸린다. **1주차 즉시 신청.**

**실제 데이터가 쓰이는 곳과 아닌 곳을 구분한다.**

| 용도 | 데이터 |
|---|---|
| STT 파이프라인 동작 검증, 데모 | AI Hub 실제 음성 |
| **4.2절 오류 내성 실험** | **텍스트 레벨 통제 오류 주입** |

### 5.3 골든셋 구축 — 일정 재배치

문서 ID가 존재해야 정답 라벨을 붙일 수 있으므로, 문서 수집과 골든셋 대량 작성은 같은 주에 병렬 수행할 수 없다.

| 주차 | 작업 |
|---|---|
| 1주차 | 문서 수집 + 청킹 + **ID 부여**, 골든셋 **10개** |
| 2주차 | 골든셋 50개 → **잠정 베이스라인** 측정 |
| 3주차 | 골든셋 150개 → **공식 기준선** 확정 |

> 2주차 측정값은 방향 확인용이며 회귀 검사에 고정하지 않는다. CI에 박히는 기준선은 150개 셋 완성 후 재측정값이다.

**골든셋 스펙에 포함할 항목**

```
- 고객 발화 스크립트
- 발화 종료 시각 (트리거 채점용, 4.1절)
- 이 시점에 표시되어야 할 문서 ID (정답)
- 표시되면 안 되는 문서 ID (오답 후보)
- 컴플라이언스 위반 삽입 여부
- 개인정보 패턴 삽입 여부 (P1~P5 필수, P6~P7 선택 — C-5 채점용)
- (F-2 케이스) 처리 유형 + 근거 필드 충족 상태 + 기대 판정
```

### 5.4 F·G 데이터

| 모듈 | 필요 데이터 | 확보 |
|---|---|---|
| F-2 | 처리 유형별 필수 확인 항목 | **가능** — 5.1절 인덱스에 포함, 팀이 규정 작성 |
| F-3 | 반복 문의 판정용 가상 이력 | **가능** — 합성 |
| G-2 | 전국 정신건강복지센터·자살예방센터 목록 | **가능** — 공개 데이터 |
| F-1 / G-1 | 실제 고위험 통화 | **불가능** |

### 5.5 데이터 한계 — 반드시 명시할 것

**검증할 수 없는 것을 검증한 것처럼 제시하지 않는다.**

| 항목 | 한계 |
|---|---|
| F-1 / G-1 탐지 성능 | 실제 데이터 확보 불가. **측정 불가** |
| **F-2 판정 정확도** | **규정·골든셋·게이트를 모두 팀이 작성했다. 100%는 "규칙 엔진이 명세대로 동작한다"는 의미이며, "실무 규정을 올바르게 해석한다"는 뜻이 아니다.** 5.1절 담당 분리로 일부 완화하되, 실무 규정 해석의 타당성은 검증되지 않는다 |
| **C-5 마스킹** | 실제 통화의 개인정보 발화 패턴을 확보할 수 없다. **"P1~P5 패턴 목록에 대한 누락 0건"**으로 범위를 한정해 측정한다. P6~P7(NER)은 여유 시 착수이며 절대 규칙 범위 밖 |
| H-1 (부록 B) | AI Hub 감정 태깅은 폭언 라벨과 다르다. 분포 괴리 존재 |
| 4.2 오류 내성 | 통제된 합성 오류 기반. 실제 STT 오류 분포와 완전히 같지 않다 |

> **측정 불가능한 절대 규칙은 범위가 한정된 절대 규칙보다 나쁘다.** C-5의 목표를 패턴 목록으로 한정하는 것은 완화가 아니라 검증 가능하게 만드는 조치다.

### 5.6 1주차 전제 확인 항목

착수 전에 확인하지 않으면 후속 설계가 흔들리는 항목들이다.

| # | 확인 대상 | 미확인 시 영향 |
|---|---|---|
| V1 | **AI Hub 데이터의 채널 구성** (2채널 스테레오 / 모노) | A-2 화자 분리 난이도 → C-1~C-3, C-5 전체 |
| V2 | GPU 가용 여부 | 생성 모델 크기 및 레이턴시 예산. **미확인 시 폴백 모드 전환 판단 불가** — 6주차에 드러나면 대응 시간이 없다 |
| V3 | Google STT 한국어 숫자 출력 형태 | C-5 정규화 단계 설계 및 순서 |
| V4 | Google STT 스트리밍 부분 결과 지연 | E2E 지표 기준값 |
| **V5** | **AI Hub 음성의 발화 간 간격 분포** | 침묵 임계값 N 결정 → 트리거 허용 창 역산(4.1절) |

---

## 6. 평가 설계

### 6.1 지표

| 영역 | 지표 | 목표 기준선 |
|---|---|---|
| 검색 | Recall@5 | ≥ 0.70 (오류 없음) / ≥ 0.60 (오류 10%) |
| 검색 | MRR | ≥ 0.55 |
| 트리거 | 적절 발동률 (종료 후 0~1,500ms) | ≥ 0.85 |
| 트리거 | 지연 발동률 / 미발동률 | 각각 별도 집계 |
| **트리거** | **지연시간 분포 p50 / p95** | **기록 (개선 추이 제시용)** |
| 생성 | 환각 수치 발생 | 150문항 중 5건 이하 |
| 생성 | 출처 표시율 | 100% |
| 컴플라이언스 | 재현율 | ≥ 0.90 |
| 컴플라이언스 | 정밀도 | ≥ 0.60 |
| **C-5** | **P1~P5 패턴 마스킹 누락** | **0건 — 절대 규칙** |
| C-5 | P6~P7(NER) 마스킹 재현율 | *(여유 시 착수 — 미구현 시 N/A)* |
| C-5 | 과잉 마스킹률 | 참고 기록 (목표 아님) |
| C-5 | STT 오류율별 마스킹 재현율 | 곡선 기록 |
| 성능 | 내부 처리 p95 | ≤ 1,000ms |
| 성능 | E2E 체감 지연 | 측정·기록 |
| 성능 | 통화당 토큰 비용 | 측정·기록 |
| **F-2** | 종결 요건 판정 정확도 | **100% — 절대 규칙** *(조건부 착수)* |
| F-2 | 근거 규정 검색 정확도 | ≥ 0.90 *(조건부 착수)* |
| F-3 | 반복 문의 연결 재현율 | ≥ 0.95 *(여유 시 — 미구현 시 N/A)* |
| G-2 | 자원 매칭 정확도 | ≥ 0.95 *(여유 시 — 미구현 시 N/A)* |
| G-2 | 폐지·이전 기관 반환 | 0건 *(여유 시 — 미구현 시 N/A)* |
| F-1 / G-1 | 탐지 성능 | **측정 불가 — 5.5절** |

> 부록 B의 H·I 지표는 **착수 확정 시 이 표에 편입**한다.

### 6.2 채점 원칙

1. **LLM을 채점자로 쓰지 않는다.** 답을 만든 모델이 자기 답을 심판하면 순환이 된다. 모든 지표를 규칙으로 계산해 재현 가능하게 한다.
2. **여러 번 실행한 값 중 최저치를 기준선으로 고정한다.** 생성 모델은 같은 입력에도 답이 달라진다.
3. **기준선 미달 시 CI 실패**로 처리한다. 개선이 아니라 회귀 방지가 목적이다.
4. **C-5·F-2의 절대 규칙은 1건이라도 뚫리면 실패로 처리한다.** 평균값으로 평가하지 않는다.
5. **모든 지표는 정답 정의가 코드로 표현 가능해야 한다.** 표현할 수 없는 지표는 지표가 아니다(4.1절 트리거 허용 창이 그 예).

---

## 7. 팀 분업

### 7.1 담당

| 담당 | 인원 | 범위 |
|---|---|---|
| **AWS·인프라** | 정성윤 | Node.js WebSocket 게이트웨이, Google STT 연동, 화자 분리, **C-5 마스킹**, PostgreSQL 스키마, Docker·배포, 레이턴시 측정 인프라, **CI 운영** |
| **백엔드·AI** | 류준 | ES 인덱스 설계·하이브리드 검색·리랭킹, 트리거 판정, 생성 모듈, 컴플라이언스 분류기, **F-2 게이트**, 평가 하네스 설계 |
| **앱·프론트엔드** | 장민석 | React 대시보드(자막·카드·경고·종결 모달), WebSocket 클라이언트, 데모 시나리오, 결과 시각화 |
| 공동 | 3인 | 골든셋 작성·라벨링, 발표 자료 |

### 7.2 부하 경고 — 반드시 읽을 것

**류준이 백엔드와 AI를 동시에 맡고 있어 구조적으로 과부하다.** 4·5주차 핵심 실험과 F-2가 모두 이 한 사람에게 걸려 있다.

완화 조치를 문서에 고정한다.

| 조치 | 내용 |
|---|---|
| **C-5를 정규식/NER로 분리 배정** | **P1~P5(정규화 + 정규식) + 파이프라인 배선 → 정성윤.** 전사 직후 처리 지점이라 STT 파이프라인 담당이 자연스럽고, 작업량의 다수를 차지한다. **P6~P7(NER 모델) → 류준, 여유 시.** NER은 HF 모델 로드·튜닝이 필요한 AI 작업이므로 인프라 담당에게 통째로 넘길 수 없다 |
| **CI 운영을 정성윤에게 이관** | 류준은 1주차에 하네스 **설계**만 하고, 이후 운영·유지는 정성윤이 맡는다 |
| 골든셋은 3인 공동 | 라벨링은 도메인 이해만 있으면 되므로 분산 가능하다 |
| **F-2 관련 역할 분리** | 규정 작성(장민석) / 스키마·게이트 구현(류준) / F-2 골든셋 라벨링(정성윤) — 5.1절 순환 검증 완화와 부하 분산을 동시에 달성 |
| 장민석이 시각화 담당 | matplotlib 성능 곡선·레이턴시 분포를 프론트 담당이 가져간다 |
| **F-2는 조건부** | 류준의 코어 작업이 지연되면 F-2를 설계 문서로 전환한다(8절 체크포인트) |

> 5주차 **마스킹 재현율 곡선**은 오류 주입 스크립트·평가 하네스와 맞물리므로 류준이 담당한다.

### 7.3 인터페이스 계약 우선

1주차에 모듈 간 스키마를 확정한다. 이것이 병렬 작업의 전제 조건이다.

```json
// 전사 이벤트 (마스킹 적용 후)
{
  "call_id": "c_001",
  "speaker": "customer",
  "text": "카드번호는 **** 입니다",
  "masked": [{"type": "P2", "span": [7, 11]}],
  "is_final": true,
  "utterance_end_ms": 3100
}

// 추천 카드
{
  "call_id": "c_001",
  "trigger_at_ms": 3150,
  "cards": [
    {
      "title": "프로모션 할인 적용 시점 안내",
      "summary": "신규 가입 할인은 가입 다음 달 청구서부터 반영됩니다.",
      "source": {"doc": "요금제약관", "clause": "3.2"},
      "score": 0.87
    }
  ],
  "internal_latency_ms": 780,
  "e2e_latency_ms": 1240
}

// 종결 판정 (F-2)
{
  "call_id": "c_001",
  "closure_type": "해지",
  "reason": "고지 완료",
  "evidence": {"위약금_안내": true, "잔여할부_안내": false, "고객확인_기록": false},
  "verdict": "blocked",
  "missing": ["잔여할부_안내", "고객확인_기록"],
  "source": {"doc": "응대매뉴얼", "clause": "7장"}
}
```

---

## 8. 8주 마일스톤

### 1주차 — 기반 + 전제 확인
- [ ] AI Hub 데이터 신청 (**최우선**)
- [ ] **5.6절 V1~V5 확인** (채널 구성 / GPU / STT 숫자 출력 / 부분 결과 지연 / 발화 간 간격)
- [ ] **침묵 임계값 N 결정 및 트리거 허용 창 역산** (4.1절)
- [ ] 인터페이스 스키마 확정 (전사·카드·종결 3종)
- [ ] 지식베이스 문서 수집 + 청킹 + **ID 부여**
- [ ] 내부 처리 규정 작성 착수 — **작성자·구현자·라벨러 분리**(5.1절)
- [ ] 골든셋 **10개** + 스펙 확정(발화 종료 시각, 개인정보 패턴 포함)
- [ ] 평가 하네스 골격(류준 설계) + CI 연결(정성윤)

### 2주차 — 베이스라인
- [ ] 가장 단순한 RAG 구현 (고정 청킹, BM25만)
- [ ] 골든셋 50개 → **잠정 베이스라인** 측정
- [ ] STT 연동 (녹음 파일 배치 모드)

### 3주차 — 실시간화 + 마스킹
- [ ] 스트리밍 STT + WebSocket 파이프라인
- [ ] 화자 분리 (V1 결과에 따라 채널 분리 또는 diarization)
- [ ] 트리거 판정 v1 (침묵 기반)
- [ ] React 자막 화면
- [ ] **C-5 개인정보 마스킹 P1~P5** — 정규화 + 정규식, 자막 표시 전 + DB 저장 전 양쪽 (정성윤)
- [ ] (여유 시) C-5 P6~P7 NER (류준)
- [ ] 골든셋 150개 → **공식 기준선 확정**

### 4주차 — 검색 품질
- [ ] nori 형태소 인덱스
- [ ] dense_vector 필드 + 임베딩 인덱싱
- [ ] RRF 하이브리드 병합
- [ ] 청킹 전략 3종 비교 → 지표 재측정

### 5주차 — 오류 내성 실험 (핵심 주차)
- [ ] STT 오류 주입 스크립트
- [ ] 오류율 0~20% 구간별 검색 성능 곡선
- [ ] **구간별 C-5 마스킹 재현율 곡선**
- [ ] 자모 분해 필터 / 리랭커 적용 후 각각 재측정
- [ ] matplotlib 시각화

### 6주차 — 생성 + 컴플라이언스
- [ ] 근거 기반 카드 생성 (HF Transformers), 스트리밍 출력
- [ ] **V2가 "GPU 없음"이면 폴백 모드를 기본값으로 전환** (3.1절)
- [ ] (여유 시) **생성 모드 vs 폴백 모드 카드 유용성 비교**
- [ ] "관련 문서 없음" 처리
- [ ] 컴플라이언스 분류기 파인튜닝
- [ ] 환각 수치 발생률 측정
- [ ] **코어(A~E, C-5) 기준선 통과 확인**

### 7주차 — 운영 관점 + F-2 체크포인트
- [ ] 내부 처리 / E2E 레이턴시 측정
- [ ] 캐싱(인메모리 LRU) 적용 및 효과 측정
- [ ] 토큰 비용 계산
- [ ] 통화 후 요약 + 지식베이스 공백 리포트
- [ ] Docker 구성

> **7주차 중반 체크포인트**
> 코어 기준선 통과 + 위 태스크 궤도 정상 → **F-2 구현 진행**
> 둘 중 하나라도 미달 → **F-2를 설계 문서로 전환** (시퀀스 다이어그램 + 요건 스키마 산출)

### 8주차 — 마감
- [ ] (조건부) F-2 마무리 또는 설계 문서 작성
- [ ] (여유 시) G-2 또는 부록 B의 H·I 중 **택1**
- [ ] **8주차 후반 3일은 발표·문서 전용으로 고정** — 개발 태스크 금지
- [ ] 아키텍처 다이어그램
- [ ] **실패 사례 분석 섹션**
- [ ] **데이터 한계 섹션** (5.5절 반영)
- [ ] 성능 리포트, 발표 자료, 데모 시나리오

### 성공 조건 — 재설정

> **STT 오류 내성 실험(4.2) + 검색 품질 개선 수치.**
> 이 두 가지가 나오면 프로젝트는 성공이다.
> **F-2는 달성 시 추가 성과로 본다.**

이전 리비전은 성공 조건을 F-2에 걸었으나, F-2는 조건부 항목이므로 부적절하다. 폴백으로 가더라도 프로젝트가 실패로 읽히지 않도록 기준을 재설정한다.

**범위 관리 규칙**: 6주차 종료 시점에 코어 기준선을 통과하지 못했다면 F·G·H·I를 착수하지 않는다. **C-5만 예외로 코어에 포함되므로 3주차에 반드시 들어간다.**

---

## 9. 레퍼런스 분석

### 9.1 결론 요약

이 프로젝트가 다루는 것은 **Agent Assist**라는 확립된 제품 범주다. 글로벌·국내 모두 상용 제품이 있으며, 이는 위험이 아니라 **기회**다. 엔터프라이즈 B2B 시장이라 개인이 서비스로 경쟁할 대상이 아니고, "실무에서 실제로 팔리는 문제를 풀었다"는 근거가 된다.

### 9.2 글로벌 상용 제품

| 제품 | URL | 참고 포인트 |
|---|---|---|
| **Amazon Q in Connect** | https://docs.aws.amazon.com/connect/latest/adminguide/amazon-q-connect.html | 통화·채팅 중 의도 자동 감지 → 실시간 생성 응답·추천 액션·문서 링크. 기능 명세 최고 레퍼런스 |
| **AWS Live Call Analytics with Agent Assist** | https://aws.amazon.com/solutions/guidance/live-call-analytics-with-agent-assist-on-aws/ | **아키텍처 다이어그램·샘플 코드 공개.** 설계 참고 |
| **Google Agent Assist** | https://cloud.google.com/agent-assist | Proactive generative knowledge assist — 본 프로젝트의 트리거 판정과 동일 문제 |
| Google CCAI 문서 | https://docs.cloud.google.com/contact-center/ccai-platform/docs/agent-assist | 실시간 전사·감정 분석·지식 어시스트 모듈 구성 |
| Connect Wisdom 콘텐츠 적재 가이드 | https://aws.amazon.com/blogs/contact-center/ingesting-content-to-power-real-time-recommendations-and-search-with-amazon-connect-wisdom | 지식베이스 적재 전략 |

### 9.3 국내 상용 솔루션

| 솔루션 | URL | 참고 포인트 |
|---|---|---|
| **LG CNS FCC RT-Advisor** | https://www.lgcns.com/kr/service/ai/ai-contact-center | "숙련되지 않은 상담사도" 실시간 지식 제공 — 가치 제안 참고 |
| **KT A'Cen On-Premise** | https://enterprise.kt.com/pd/P_PD_AI_CC_003.do | **불완전판매 방지 솔루션** = F 모듈과 인접. Barge-in, SAD, 단문발화 인식 등 실시간 음성 난점 목록 |
| 솔트룩스 AICC | https://www.saltlux.com/kor/contents/view.do?mId=35 | 상담원 지원 + KMS + 실시간 모니터링 |
| 삼성SDS AICC | https://www.samsungsds.com/kr/aicc/aicc.html | 규제 복잡도와 컴플라이언스 니즈 |
| LG U+ AICC | https://www.lguplus.com/biz/all/telecom/phones-others/aicc/B000000150 | **개인정보 마스킹·암호화** — C-5 실무 근거 |
| 메타엠 | https://meta-m.co.kr/ | TA 대시보드 — 실시간 키워드 표시 UI |
| AICC 개념 정리 | https://www.tryvox.co/glossary/aicc | 한국어 통화의 STT 정확도 가정 위험, 민감정보 마스킹 기준 필요성 |

### 9.4 데이터셋

| 출처 | URL |
|---|---|
| AI Hub 상담 음성 (3,000시간) | https://aihub.or.kr/aidata/30711 |
| AI Hub 고객 응대 음성 (3,300시간) | https://aihub.or.kr/aihubdata/data/view.do?dataSetSn=71616 |
| AI Hub 민원 콜센터 질의-응답 | https://aihub.or.kr/aidata/30716 |
| AI Hub 메인 | https://www.aihub.or.kr/ |
| 서울 행정 민원상담 음성 | https://data.seoul.go.kr/dataList/OA-22804/L/1/datasetView.do |

### 9.5 G-2 참고 (여유 시)

| 항목 | URL |
|---|---|
| 한국생명존중희망재단 | https://www.kfsp.or.kr/ |
| 보건복지상담센터 109 안내 | https://www.129.go.kr/109 |

> **정책 근거 (발표 인용 가능)**: 보건복지부는 109 콜센터 증설과 함께 "AI를 활용한 상담일지 작성 및 위기 신호 조기 발굴을 통한 업무 부담 완화"를 추진 과제로 명시했다.
> https://www.mohw.go.kr/board.es?mid=a10503010100&bid=0027&act=view&list_no=1487674

### 9.6 기술 레퍼런스

| 항목 | 내용 |
|---|---|
| **한국어 STT 벤치마크** | https://github.com/rtzr/Awesome-Korean-Speech-Recognition — 상담 음성·저음질 전화망 도메인별 CER 비교. **V3·V4 확인에 활용** |
| Elasticsearch nori | 사용자 사전으로 업계 용어 등록 필수 |
| ES dense_vector + kNN | 하이브리드 검색 구성 |
| RRF | BM25 + 벡터 결과 병합 표준 기법 |
| KLUE 벤치마크 | 한국어 NLU — 분류기 파인튜닝 참고 |

---

## 10. 차별화 포인트

### 10.1 "통화 중"에 개입한다
대부분의 콜센터 프로젝트는 통화 후 녹음을 처리한다. 실시간 개입은 트리거 판정, 오류 내성, 레이턴시 예산이라는 별개의 문제군을 만든다.

### 10.2 STT 오류율별 성능 곡선
> "오류율 10%에서 Recall@5가 0.48까지 떨어졌고, 하이브리드 검색으로 0.66까지 복구했습니다."

검색 품질을 **측정하고 개선한** 증거다. 마스킹 재현율 곡선까지 같은 축에 올리면 실험 하나로 두 가지를 설명할 수 있다.

### 10.3 채점에 LLM을 쓰지 않는다
답을 만든 모델이 자기 답을 심판하면 순환이 된다. 모든 지표를 규칙으로 계산했고, **정답 정의를 코드로 표현할 수 없는 지표는 지표로 인정하지 않았다**(트리거 허용 창을 정의한 이유).

### 10.4 재현율 우선 설계의 근거
컴플라이언스와 마스킹에서 누락이 오탐보다 위험하다는 비대칭을 지표 우선순위로 명시했다.

### 10.5 실패 사례 분석
"이런 질문은 여전히 틀리고, 원인은 이것이며, 해결하려면 이런 접근이 필요하다."

### 10.6 RAG를 검증 도구로 쓴다 (F-2)
일반적인 RAG는 답을 찾아 보여준다. F-2의 RAG는 **행위를 차단할 근거를 가져온다.** 검색 결과가 사용자에게 보여지는 정보가 아니라 게이트 판정의 입력이 되는 구조는 흔하지 않다.

### 10.7 통제 가능한 구간과 아닌 구간을 분리했다
레이턴시를 내부 처리와 E2E 체감으로 나눠 측정했다. 통제 밖 구간에 목표를 걸지 않되 기록은 남긴다.

### 10.8 검증 불가를 명시한다
F-1·G-1의 탐지 성능은 측정할 수 없다고 문서에 적었다. C-5의 절대 규칙도 "P1~P5 패턴에 대해"로 범위를 한정했다. **F-2의 판정 정확도 100%도 "팀이 만든 규정 안에서의 100%"임을 명시했다 — 절대 규칙이라는 표현이 검증 범위까지 절대적이라는 뜻은 아니다.** 자기 프로젝트의 미검증 영역을 스스로 표시하는 것이 과장된 주장보다 강한 인상을 남긴다.

### 10.9 LLM을 뺐을 때와 비교했다
대부분의 프로젝트는 "LLM을 썼다"에서 멈춘다. 이 프로젝트는 생성 폴백 모드(검색 스니펫 직접 표시)를 만들어 **생성 단계가 실제로 얼마나 기여하는지**를 비교했다. 토큰 비용 대비 효용을 논할 근거가 생긴다.

---

## 11. 리스크 및 대응

| 리스크 | 영향 | 대응 |
|---|---|---|
| **AI Hub 승인 지연** | 데모·파이프라인 검증 지연 | 1주차 즉시 신청. **4.2절 핵심 실험은 텍스트 레벨 오류 주입이므로 승인과 무관하게 진행 가능** — 영향 범위가 좁다 |
| **화자 분리 정확도 미달** | C-1~C-3, C-5 전체의 성능 상한선이 됨 | V1으로 1주차 확인. **모노일 경우 시연은 물리 2채널(브라우저 2대)로 우회**하고, 모노 결과는 오프라인 평가 한계로 명시 |
| **류준 부하 집중** | 코어와 F-2가 한 사람에게 걸림 | 7.2절 완화 조치 적용. F-2 조건부 착수 |
| Google STT 한국어 성능 미달 | 파이프라인 품질 저하 | 벤치마크 사전 확인(9.6). 오류 내성 설계 자체가 주제이므로 오류가 많아도 실험은 성립 |
| STT API 비용 초과 | 예산 소진 | 반복 실험은 배치 모드. 실시간 데모만 스트리밍 |
| GPU 미확보 | 생성 모델 크기 제약 | V2로 1주차 확인. **폴백 모드(검색 스니펫 표시)로 전환 — 코어 가치인 "찾아주는 것"은 유지됨**(3.1절) |
| **F-2 규정을 팀이 작성** | 판정 정확도 100%가 자기충족적 수치가 됨 | 실제 약관 체계에 맞춰 작성. **규정 작성자 / 스키마 구현자 / 골든셋 라벨러 분리**(5.1절). 5.5절에 한계 명시 |
| **트리거 허용 창과 구현 방식 충돌** | 지표가 구현 방식을 사전 배제 | 허용 창을 침묵 임계값 + 500ms로 역산. 분포(p50/p95)를 함께 기록해 개선 추이 제시 |
| 레이턴시 목표 미달 | 데모 설득력 저하 | 목표 미달해도 **측정하고 원인을 분해한 기록**이 있으면 성과 |
| 범위 과다 | 미완성 | F·G·H·I는 조건부. B(검색)와 E(평가), C-5를 절대 사수 |
| **개인정보 노출** | 자막·DB에 평문 저장 | **C-5를 3주차 코어로 포함.** P1~P5 누락 0건, P6~P7은 여유 시 |
| 약관 저작권 | 공개 시 문제 소지 | 원문 전재 대신 요약·해석 + 출처 표시 |
| 민감 도메인 표현 | 과장·부적절 서술 | 부록 A 준수. 개별 사건 실명·세부 인용 금지 |

---

## 부록 A — 민감 기능 공통 설계 원칙

C-5, F, G, 부록 B의 H·I에 **공통 적용**한다. 코드보다 먼저 합의한다.

### A-1. 시스템의 발언 범위

| 할 수 있는 말 | 하면 안 되는 말 |
|---|---|
| "위험 신호가 감지되었습니다" | "안전합니다" |
| "이 종결 사유는 근거 3건이 필요합니다" | "이 건은 종결해도 됩니다" |
| "관할 기관은 다음과 같습니다" | "이 사람은 위험도 78%입니다" |
| "이 요청 유형은 확인 항목 3건이 필요합니다" | "이 사람은 사기범입니다" |

### A-2. 판정과 설명의 분리

- **판정은 규칙이 한다.** 종결 가능 여부, 요건 충족, 마스킹 대상 여부, 기관 매칭 — 전부 결정론적 로직
- **LLM은 설명만 한다.** 판정 결과를 사람이 읽을 문장으로 옮기는 역할
- 숫자와 판정을 생성 모델에 맡기지 않는다

### A-3. 주장의 한계

| 쓰지 않을 표현 | 대신 쓸 표현 |
|---|---|
| "이 시스템이 있었으면 막을 수 있었다" | "근거 없는 종결을 구조적으로 어렵게 만드는 설계를 검토했다" |
| "불변 감사 로그" | "추가 전용 감사 로그 — 완전한 불변성은 애플리케이션 레벨에서 보장 불가" |
| "개인정보를 완벽히 차단합니다" | "정의된 P1~P5 패턴에 대해 누락 0건을 목표로 한다" |
| "위기를 정확히 탐지합니다" | "탐지 성능은 검증되지 않았으며, 상담원의 판단을 대체하지 않습니다" |

### A-4. 발표·시연 시 주의

- 개별 사건의 실명·지역·세부 정황을 쓰지 않는다. 문제 정의는 **일반적 실패 유형**으로 서술한다 — "처리 시스템에서 종결 사유의 근거가 검증되지 않는 문제"
- 위기 통화를 재현하는 데모는 하지 않는다
- G-2를 다룰 경우 자살예방 보도준칙을 참고해 표현을 다듬고, 구체적 방법·장소 묘사는 넣지 않는다
- G-2 포함 시 발표 자료 말미에 상담 안내: **자살예방 상담전화 109 (24시간, 비밀보장)**

### A-5. 데이터 취급

- 실제 위기 상담 음성·기록을 수집하지 않는다
- 시연·평가용 시나리오는 팀이 작성한 가상 사례만 사용한다
- 가상 사례에 실제 인물을 연상시키는 정보를 넣지 않는다

---

## 부록 B — 추가 검토 모듈 (H·I)

**코어 완료 후 여유가 있을 때 둘 중 하나만** 착수한다. 착수 확정 시 지표를 6.1절에 편입한다.

### B-1. 모듈 I — 본인확인 요건 게이트 *(H·I 중 권장)*

**주장 범위**: "본인확인 없는 민감 요청 처리의 비용을 올린다." — "보이스피싱을 막습니다"가 아니다.

F-2와 구조가 1:1로 대응된다. 규정 문서만 바뀌고 흐름은 동일하다.

| 기능 | 설명 | 채택 |
|---|---|---|
| **I-2** | 본인확인 요건 검증 게이트 (F-2와 동일 패턴) | 권장 |
| I-3 | 공개 사기 화법 사례와 유사도 매칭 → **경고 카드만, 자동 차단 없음** | 여유 시 |
| I-1 | 고위험 요청 패턴 탐지 | 여유 시 (검증 불가) |
| I-4 | 통화 후 의심 플래그 집계 | 여유 시 |

**I를 권장하는 이유**: F-2와 스토리가 이어진다("근거 없는 종결" → "확인 없는 민감 요청"). H가 안고 있는 거버넌스 문제가 없다.

**한계**: 실제 사기 통화 데이터는 확보하지 않는다. I-1 탐지 성능은 측정 불가. 금융감독원·경찰청 공개 사기 유형 자료는 **규정 문서처럼 인덱싱하여 I-2·I-3의 RAG 근거로만** 쓴다.

### B-2. 모듈 H — 악성민원 대응 *(범위 축소 전제)*

**주장 범위**: "폭언 노출을 기록하고 대응을 돕는다." — "감정노동을 없앤다"가 아니다.

| 기능 | 설명 | 채택 |
|---|---|---|
| H-1 | 고객 채널 대상 폭언·욕설 실시간 탐지 | 가능 |
| H-2 | 상담원용 대응 문구 카드 (B-4 재사용) | 가능 |
| H-3 | 통화 종료 시 고위험 민원 태깅 (D-2·D-3 확장) | 가능 |
| H-4 | 상담원별 누적 노출 지표 | **제외 권장** |
| H-5 | 임계치 초과 알림 | **직전 1건 기반으로 축소** |

#### H-1의 데이터 — 정정

"C모듈과 동일한 분류기, 대상 채널만 전환"이라는 설명은 부정확하다. **아키텍처는 재사용되지만 모델은 새로 학습해야 한다.** C모듈은 확정적 보장 표현·개인정보 요구를 잡는 라벨셋이고, H-1은 폭언·욕설·성희롱이다. 라벨·학습 데이터·평가셋이 모두 다르다.

AI Hub 감정 태깅을 근거로 "검증 가능"이라고 보는 것도 낙관적이다. **감정 라벨과 폭언 라벨은 다르다.** 화난 고객이 모두 폭언을 하지 않고, 차분한 어조의 성희롱도 있다.

> **정정**: H-1의 출발점은 **혐오표현 멀티라벨 데이터셋(Unsmile 계열) 기반 분류기**다. 클래스 불균형이 크므로 정확도가 아닌 macro-F1으로 평가한다.

#### H-4 / H-5를 축소하는 이유

**상담원별 누적 지표는 직원 감시 도구가 될 수 있다.** 보호 목적으로 설계해도 조직에 들어가면 성과 평가로 전용되는 것이 이 종류 데이터의 전형적 경로이며, 팀 프로젝트에는 이를 통제할 거버넌스가 없다.

권장 형태 (아래로 갈수록 안전)

1. 집계는 팀·시간대 단위로만 — "야간 시간대 고위험 통화 비율"
2. 개인별 지표는 본인만 열람, 관리자 미노출
3. **H-5는 직전 통화 1건만 보고 판정** — 누적 기록 없이 목적 달성. 저장할 데이터가 없어 가장 깔끔하다

3번을 권장한다.

### B-3. H·I 리스크

| 리스크 | 대응 |
|---|---|
| H-4·H-5가 감시 도구로 전용 | 개인 단위 누적 저장을 하지 않는 설계 채택 |
| H-1을 C모듈 재사용으로 오해 | 별도 학습·평가셋 필요를 명시 |
| I-3이 자동 차단으로 오해 | "경고 카드만, 판단은 상담원" 반복 명시 |
| H·I 동시 착수 | **둘 다 하지 않는다. 택1, 그것도 여유 시** |

---

## 부록 C — 즉시 착수할 항목

### 오늘 처리

1. **AI Hub 회원가입 + 휴대폰 인증 + 데이터 신청** — 승인에 시간이 걸린다
2. **V1 확인** — AI Hub 데이터셋 상세 페이지에서 채널 구성(스테레오/모노) 확인. 이것이 3주차 설계를 좌우한다
3. **V2 확인** — GPU 가용 여부. 생성 모델 크기와 레이턴시 예산이 여기 걸린다

### 1주차 초반

4. **인터페이스 스키마 3종 확정** — 전사·카드·종결. 병렬 작업의 전제
5. **골든셋 10개 시범 작성** — 라벨링이 실제로 가능한지 검증. 여기서 막히면 지식베이스 구성을 바꿔야 한다
6. **골든셋 스펙에 발화 종료 시각 필드 포함** — 없으면 트리거 지표를 채점할 수 없다

### 팀 확인 사항

7. 투입자원 목록 밖 도구 사용 가능 여부 — 본 기획서는 목록 안에서만 구성했으므로 현재는 해당 없음
8. 7.2절 부하 완화 조치에 대한 3인 합의
9. 5.1절 F-2 역할 분리(규정 작성 / 스키마 구현 / 골든셋 라벨링) 배분 확정

### 개별 사건 자료의 내부 보관

F 모듈 문제의식의 출처는 본 기획서 본문에 두지 않는다. 다만 팀이 몇 주 뒤 문제의식을 설명해야 할 때 근거를 다시 찾아야 하므로, **팀 위키·비공개 노션·저장소 `docs/internal/` 등 별도 공간**에 다음 형태로 남긴다.

```
F 모듈 문제의식의 출처 (내부 전용 — 발표·제출본 반영 금지)
  - 링크
  - 참고한 것: 종결 사유의 근거가 검증되지 않는 구조
  - 참고하지 않은 것: 개별 사건의 시점·지역·경위·당사자
  - 취급: 진행 중 사안이며 유가족이 존재한다. 발표에서 언급하지 않는다
```

### 문서 변경 후 재점검 (반영 완료 시 확인)

- [ ] 6.1절 지표 중 **정답 정의를 코드로 표현할 수 없는 항목이 남아 있는지** 재점검 (6.2절 원칙 5)
- [ ] 5.5절 한계 표에 **누락된 기능이 더 없는지** 재점검 — F-2가 빠져 있었던 것과 같은 유형의 누락
