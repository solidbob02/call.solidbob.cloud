# CLAUDE.md — 실시간 상담원 어시스트 RAG

이 저장소는 **제품 코드 저장소가 아니라 프로젝트의 기록 저장소**다.
지킬 사이트(`call.solidbob.cloud`)가 곧 프로젝트의 상태이자 산출물이다.
Claude 는 매 세션 이 파일의 규칙을 따른다. 규칙과 실제가 어긋나면 규칙을 고치고 커밋한다.

---

## 0. 세션 시작 루틴 (모든 세션의 첫 3분, 예외 없음)
첫페이지는 사업명이고 두 번째
  페이지는 목차일 것이다. 첫페이지 텍스트를 제공해 달라. 그러면 그걸로 클로드 코드에게
  요청할 것이다. 
```
1. CLAUDE.md            ← 지금 이 파일 (규칙)
2. _project/STATE.md    ← 현재 어디까지 왔는가 (단 하나의 현재 상태)
3. _data/milestones.yml ← 이번 주차에 남은 체크박스
4. 최근 _posts/ 로그 2~3건 ← 직전 세션이 무엇을 왜 했는가
```

이 4개를 읽기 전에 코드를 건드리지 않는다.
`STATE.md` 의 **`다음 작업`** 첫 항목이 이번 세션의 기본 시작점이다.
사용자가 다른 지시를 하면 그것이 우선하고, 대신 `STATE.md` 를 그에 맞게 고친다.

---

## 1. 프로젝트 정의

**한 줄**: 통화를 실시간으로 들으면서, 고객이 방금 물은 내용에 필요한 사내 문서를 상담원 화면에 자동으로 띄워주는 시스템.

**핵심 차별점**: 통화 *후* 녹음 처리가 아니라 통화 *중* 개입. 이 차이가 트리거 판정 / STT 오류 내성 / 레이턴시 예산이라는 세 개의 기술 난제를 만든다.

**기간**: 8주 (2026-08-24 ~ 2026-10-18)
**팀**: 3인 — 정성윤(AWS·인프라) / 류준(백엔드·AI) / 장민석(앱·프론트엔드). 골든셋·발표 자료는 공동.
**원본 기획서**: `_project/plan.md` = **기획서 rev.4 (3인 팀)** 사본. 판단이 갈릴 때의 최종 근거.
`_project/rev4-보완지시서.md` 가 그 위에 얹히는 패치이며, **충돌 시 보완지시서가 우선한다**(트리거 허용 창 800ms → 0~1,500ms).
채택 근거는 `_project/decisions/001-기획서-rev4-채택.md`. 이전 판은 `plan-rev1-initial.md`, 대안은 `plan-rev4-alt-5인안.md` 에 보존.
기획서와 다르게 갈 때는 반드시 `_project/decisions/` 에 이유를 남긴다.

### 기능 블록

| 블록 | 내용 | 우선순위 |
|---|---|---|
| A | 실시간 STT (스트리밍·화자분리·발화구간 검출) | 필수 |
| **B** | **자동 문서 추천 = RAG 핵심** | **사수** |
| C | 실시간 컴플라이언스 감지 | 필수 |
| **C-5** | **개인정보 실시간 마스킹 (자막·저장 양쪽 앞단, 3주차)** | **사수 — 코어** |
| D | 통화 후 처리 (요약·유형분류·지식베이스 공백 리포트) | 필수 |
| **E** | **평가 하네스 (골든셋·자동채점·CI)** | **사수** |
| F-2 | 종결 요건 검증 게이트 | 조건부 (7주차 체크포인트) |
| F-1·F-3·F-4 / G-2 / H·I | 확장 모듈 | 여유 시 |

**6주차 종료 시점에 코어 기준선을 통과하지 못했다면 F·G·H·I 를 착수하지 않는다.** C-5 만 예외로 코어에 포함된다.
성공 조건은 F-2 가 아니라 **STT 오류 내성 실험 + 검색 품질 개선 수치**다.

### 기술 스택 (기획서 3.1 고정)

Python / FastAPI · Node.js WebSocket 게이트웨이 · Google STT · Elasticsearch(nori + dense_vector + RRF) · HuggingFace Transformers · React · MySQL · Docker · matplotlib
→ **OpenCV 는 이 주제에 활용처가 없다. 끼워넣지 않는다.**

---

## 2. 절대 원칙 (사용자의 명시적 지시 없이는 위반 금지)

1. **LLM 을 채점자로 쓰지 않는다.** 모든 평가 지표는 규칙 기반으로 계산해 재현 가능해야 한다. 답을 만든 모델이 자기 답을 심판하면 순환이다.
2. **측정하지 않은 수치를 기록하지 않는다.** 기획서 4.2 의 성능 표는 *형식 예시*다. `jekyll/_data/metrics.yml` 에는 실제로 실행해서 나온 값만 넣고, 미측정은 `null` 로 둔다. 예시 숫자를 실측처럼 옮겨 적는 것은 이 프로젝트에서 가장 심각한 오류다.
3. **컴플라이언스 감지와 마스킹은 재현율 우선.** 놓치는 것(FN)이 과잉 경고(FP)보다 위험하다. C-5 는 **누락 0건 > 과잉 마스킹 억제** 순서를 고정한다 — 애매하면 가린다.
4. **기준선은 여러 번 실행한 값 중 최저치로 고정한다.** 생성 모델은 같은 입력에도 답이 달라진다.
5. **기준선 미달은 CI 실패.** 개선이 아니라 회귀 방지가 목적이다.
   **절대 규칙(C-5 누락 0건, F-2 판정 정확도)은 평균이 아니라 1건이라도 뚫리면 실패**로 처리한다.
6. **약관 원문을 사이트에 전재하지 않는다.** 검색 인덱스 내부 저장은 무방하나, 공개 페이지에는 요약·해석 + 출처(문서명·조항)만 싣는다.
7. **자체 통화 녹음은 하지 않는다.** 음성 데이터는 AI Hub 등 저작권·개인정보가 해결된 출처만 사용한다.
8. **실패를 지운 기록은 기록이 아니다.** 안 된 실험, 미달한 지표, 틀린 가설은 그대로 남긴다. 8주차 "실패 사례 분석" 섹션의 재료가 된다.
9. **판정은 규칙이, 설명만 LLM 이 한다.** 종결 가능 여부·요건 충족·마스킹 대상 판정을 생성 모델에 맡기지 않는다.
10. **측정할 수 없는 것을 측정한 것처럼 쓰지 않는다.** F-1·G-1 탐지 성능은 측정 불가이고, F-2 의 100% 는 "팀이 만든 규정 안에서의 100%"다. 범위를 한정해 적는다.

---

## 3. 저장소 구조 — 무엇을 어디에 쓰는가

사이트는 **개발 제안서 형식**이다. 표지(`/`) → 목차(`/toc/`) → 5개 절(`/docs/1~5-*`) 구조를 유지한다.

```
CLAUDE.md                    이 파일. 규칙과 프로젝트 헌법 (저장소 전체 기준)
README.md                    저장소 소개
docs/                        기술 문서 (지킬 밖 — 사이트에 게시되지 않음)
  architecture.md            구현자용 아키텍처 + 데이터 모델 + ERD 재생성 절차
  erd/schema.mmd             ERD Mermaid 소스 — 정본
  erd/schema.png             ERD 이미지 — 소스에서 생성한 산출물
  erd/mmd-config.json        렌더 설정 (한글 글꼴)
_project/                    ⚠ 비공개. 지킬 루트 밖이라 사이트에 올라가지 않음
  STATE.md                   현재 상태 단 하나. 매 세션 갱신 (가장 중요)
  plan.md                    원본 기획서 사본 (수정하지 않음)
  decisions/                 결정 기록 (ADR). NNN-제목.md
  templates/                 로그·결정 기록 템플릿
jekyll/                      ← 지킬 사이트 루트. 지킬 명령은 전부 이 안에서 실행한다
  _config.yml                사이트 설정 (collections, header_pages)
  Gemfile / Gemfile.lock     루비 의존성
  index.html                 표지 — 사업명, 기간, 팀, 저장소·데모 주소
  toc.md                     목차 — 제안서 전체의 진입점 (/toc/)
  log.md                     개발 로그 목록 (/log/)
  open-items.md              미결 항목 (/open-items/)
  progress.md                진행 상황 — 체크리스트 + 실측 지표 (/progress/)
  404.html
  _docs/
    1-business.md            1. 사업 개요
    2-requirements.md        2. 개발 요구 사항
    3-guidelines.md          3. 주요 개발 수행 지침
    4-schedule.md            4. 개발 일정 및 추진 체계
    5-appendix.md            5. 부록 (용어 정의 · 서식 · 참고 자료)
    architecture.md          세부 문서 — 시스템 아키텍처       (nav_order 91)
    interface-contract.md    세부 문서 — 인터페이스 계약        (92)
    golden-set.md            세부 문서 — 골든셋 설계            (93)
    evaluation.md            세부 문서 — 평가 설계              (94)
    how-to-read.md           세부 문서 — 이 기록을 읽는 법      (95)
  _posts/                    개발 로그. 시간 순서. 한 번 쓴 건 고치지 않음
  _data/
    milestones.yml           8주 체크리스트 + 상태. 기계가 읽는 진행률
    metrics.yml              지표 목표치 / 실측치. 숫자의 단일 출처
    open_items.yml           미결 항목. /open-items/ 로 렌더됨
  assets/main.scss           제안서 표지·목차 스타일 (minima 위에 덧씌움)
```

**지킬 사이트는 `jekyll/` 안에만 둔다.** 루트에는 저장소 전체에 걸리는 것(CLAUDE.md, README.md, `docs/`, `_project/`)과 앞으로 추가될 다른 코드(Flutter 앱 등)가 온다.

`docs/` 와 `jekyll/_docs/` 를 혼동하지 않는다.

| | `jekyll/_docs/` | `docs/` |
|---|---|---|
| 성격 | 제안서 본문·세부 문서 (공개 페이지) | 구현자용 기술 문서·다이어그램 소스 |
| 독자 | 읽는 사람 전부 | 코드를 만지는 사람 |
| 게시 | 사이트에 렌더됨 | **게시되지 않음** (git 에만) |
| 예 | `2-requirements.md`, `evaluation.md` | `architecture.md`, `erd/schema.mmd` |

같은 내용을 두 곳에 적지 않는다. 사이트 쪽은 요약과 링크만 두고 상세는 `docs/` 에 둔다.
`_project/` 는 이제 지킬 루트 밖이므로 언더스코어와 무관하게 사이트에 게시되지 않는다.


**헤더 내비게이션은 목차 / 개발 로그 / 미결 항목 3개로 고정한다** (`jekyll/_config.yml` 의 `header_pages`). 페이지를 늘릴 때는 내비가 아니라 목차에 추가한다.

새 절을 만들지 말고 기존 5개 절 안에 넣는다. 절 구조를 바꿔야 한다면 결정 기록을 먼저 쓴다.

### 세 가지 기록의 역할 구분 — 헷갈리면 여기를 본다

| | 무엇 | 시간 축 | 수정 | 비유 |
|---|---|---|---|---|
| `jekyll/_posts/` | 그날 한 일과 왜 | 과거 (append) | 안 함 | 항해 일지 |
| `jekyll/_docs/` | 지금 맞는 사실 | 현재 (overwrite) | 계속 | 해도(海圖) |
| `_project/STATE.md` | 지금 어디 있고 다음은 뭔가 | 현재 (overwrite) | 매 세션 | 나침반 |
| `jekyll/_data/open_items.yml` | 아직 정하지 못한 것 | 현재 | 해소 시 닫음 | 항해 중 미해결 표시 |

같은 내용을 세 곳에 복사하지 않는다. 로그는 "무엇을 했다 + `jekyll/_docs/x` 갱신함"으로 쓰고 상세는 `_docs` 에 둔다.

---

## 4. 기록 규칙

### 4.1 진행 로그 — `jekyll/_posts/YYYY-MM-DD-슬러그.md`

작업이 있었던 **모든 세션은 로그 1건**을 남긴다. 여러 세션이 같은 날이면 슬러그로 구분한다.
템플릿: `_project/templates/log-post.md`

```yaml
---
layout: post
title: "W1 — 골든셋 시나리오 10건 시범 작성"
date: 2026-08-25 14:00:00 +0900
categories: log
week: 1                      # 1~8
track: [eval, data]          # voice | search | core | classify | frontend | eval | data | infra
status: done                 # done | partial | blocked
metrics_touched: false       # _data/metrics.yml 을 고쳤으면 true
---
```

본문은 이 4개 절을 **순서대로, 비워두지 말고** 쓴다.

```markdown
## 한 일
## 판단과 근거      ← 왜 그 선택을 했는가. 버린 선택지도 한 줄
## 막힌 것          ← 없으면 "없음". 있으면 재현 방법까지
## 다음 세션 첫 작업 ← STATE.md 의 '다음 작업' 첫 항목과 반드시 일치
```

제목 규칙: `W{주차} — {한 일}`. 주차 표기가 있어야 8주 타임라인이 복원된다.

⚠ `date` 는 **실제 작성 시각**으로 쓴다. 미래 시각을 적으면 지킬이 그 글을 건너뛰어 사이트에 나오지 않는다
(`date '+%Y-%m-%d %H:%M:%S %z'` 로 확인). 글을 쓴 뒤 `/log/` 에 실제로 떴는지 확인한다.

### 4.2 사실 문서 — `jekyll/_docs/*.md` (제안서 각 절 + 세부 문서)

설계·스키마·측정 결과처럼 **"지금 무엇이 맞는가"** 는 로그가 아니라 여기에 쓴다.
읽는 사람이 최신 파일 하나만 보면 되도록, 과거 내용은 지우고 덮어쓴다 (이력은 git 과 `_posts` 에 있다).

```yaml
---
layout: page
title: 인터페이스 계약
nav_order: 20
updated: 2026-08-24        # 고칠 때마다 갱신
owner: core                # 담당 트랙
status: draft              # draft | agreed | frozen
---
```

`nav_order` 1~5 는 제안서 본문 절, 91 이상은 세부 문서다. 절 문서의 소제목은 목차가 앵커로 걸고 있으므로
(`1) 사업 목적` → `#1-사업-목적`) **소제목 문구를 바꾸면 `toc.md` 의 링크도 같이 고친다.**

`status: agreed` 인 문서(특히 인터페이스 계약)를 바꿀 때는 결정 기록을 함께 남긴다.

### 4.3 결정 기록 — `_project/decisions/NNN-제목.md`

되돌리기 어렵거나 나중에 "왜 이렇게 했지?"가 나올 선택은 여기에 남긴다.
예: 지식베이스 도메인 선택, 트리거 전략, 청킹 방식, 임베딩 모델, 기획서와 다르게 간 모든 지점.
템플릿: `_project/templates/decision.md` (맥락 / 선택지 / 결정 / 결과 / 되돌리는 법)

### 4.4 숫자 — `jekyll/_data/metrics.yml`

**모든 성능 수치는 이 파일이 유일한 출처다.** 포스트나 문서에 숫자를 직접 타이핑하지 않고 `site.data.metrics` 를 참조한다.
측정값 한 건에는 반드시 이 4가지가 붙는다.

```yaml
    value: 0.66
    measured_at: 2026-09-20     # 언제
    commit: a1b2c3d             # 어느 코드로
    command: "python eval/run.py --error-rate 0.10"   # 어떻게 재현하나
    n: 150                      # 골든셋 몇 건 기준
```

이 4개 중 하나라도 못 채우면 그 숫자는 아직 기록할 준비가 안 된 것이다.

### 4.5 진행률 — `jekyll/_data/milestones.yml`

체크박스 상태는 여기서만 바꾼다. 상태값: `todo` | `doing` | `done` | `blocked` | `dropped`.
`dropped` 는 반드시 `note` 에 이유를 적는다. 기획서에 있던 항목을 조용히 지우지 않는다.

### 4.6 미결 항목 — `jekyll/_data/open_items.yml` (공개)

아직 정하지 못한 것, 사용자 답을 기다리는 것은 여기에 쓴다. `STATE.md` 의 "미결 질문"과 짝을 이루되,
**남에게 보여도 되는 것은 이 파일에, 내부 사정은 STATE.md 에** 둔다.

```yaml
- id: OI-06
  title: 한 줄 제목
  why: 왜 이걸 정해야 하는가, 안 정하면 무엇이 막히는가
  blocks: [w4.dense]        # milestones.yml 의 task id
  owner: user | team | search | ...
  status: open              # open | resolved
  opened: 2026-08-24
```

해소되면 지우지 않고 `status: resolved` + `resolution` + `resolved` 날짜를 채운다. 되돌리기 어려운 결정이었다면 `_project/decisions/` 에도 남긴다.

### 4.7 현재 상태 — `_project/STATE.md`

**세션을 끝낼 때 이 파일을 갱신하지 않았다면 그 세션은 끝나지 않은 것이다.**
다음 세션의 Claude 는 이 파일만 믿는다. 사람이 기억하고 있을 거라고 가정하지 않는다.

---

## 5. 세션 종료 루틴

작업이 있었다면 아래를 모두 수행하고 마친다. (조사·질의응답만 한 세션은 3만 수행)

1. `_project/STATE.md` 갱신 — 현재 주차, 진행 중, **다음 작업(우선순위 순)**, 블로커, 미결 질문
2. `jekyll/_posts/` 에 로그 1건 — 4.1 형식
3. 바뀐 사실은 `jekyll/_docs/` 해당 절에 반영, 새 숫자는 `jekyll/_data/metrics.yml`, 체크박스는 `jekyll/_data/milestones.yml`
4. 새로 생긴 미결 사항은 `jekyll/_data/open_items.yml` 에 추가, 해소된 것은 `resolved` 처리
5. 되돌리기 어려운 선택을 했으면 `_project/decisions/` 에 1건
6. `bundle exec jekyll build` 로 빌드 통과 확인 (front matter·리퀴드 오류가 여기서 잡힌다)
7. 커밋 — 6절 규칙

사용자가 "커밋해"라고 하지 않으면 커밋은 하지 않되, **1~6은 지시 없이 수행한다.** 기록은 작업의 일부다.

---

## 6. 커밋 규칙

```
log(w3): 스트리밍 STT 파이프라인 연결
docs(search): 인터페이스 계약 v2 확정
data(metrics): 오류율 10% 구간 Recall@5 실측 반영
rule: 세션 기록 규칙에 결정 기록 추가
```

타입: `log` | `docs` | `data` | `code` | `rule` | `chore`
푸시는 사용자가 지시할 때만. `main` 에 직접 커밋해도 되지만, 실험성 작업은 브랜치를 판다.

---

## 7. 공개 / 비공개 경계 ⚠

이 사이트는 GitHub Pages 로 **공개 게시**된다. 커밋 = 발행이다.

| | 위치 |
|---|---|
| 공개해도 되는 것 | 아키텍처, 스키마, 실험 방법과 결과, 실패 분석, 진행 로그 |
| 공개하면 안 되는 것 | 약관 원문, API 키·자격증명, 개인정보 포함 데이터 샘플, 면접·평가 전략 등 내부 서사 |

내부 성격의 글은 `_project/` 아래에 둔다 (빌드 제외됨). 판단이 애매하면 `_project/` 에 두고 사용자에게 묻는다.
**어떤 경우에도 자격증명을 커밋하지 않는다.** 키는 `.env` (gitignore) 로 관리한다.

---

## 8. 지표 목표 기준선 (rev.4 6.1절 + 보완지시서)

정본은 `jekyll/_data/metrics.yml`. 아래는 요약이다.

| 영역 | 지표 | 기준선 |
|---|---|---|
| 검색 | Recall@5 | ≥ 0.70 (오류 0%) / ≥ 0.60 (오류 10%) |
| 검색 | MRR | ≥ 0.55 |
| 트리거 | 적절 발동률 (종료 후 **0~1,500ms**) | ≥ 0.85 |
| 트리거 | 조기/지연 발동률, 지연 분포(p50/p95) | 별도 집계·기록 |
| 생성 | 환각 수치 발생 / 출처 표시율 | 150문항 중 5건 이하 / 100% |
| 컴플라이언스 | 재현율 / 정밀도 | ≥ 0.90 / ≥ 0.60 |
| **C-5** | **P1~P7 마스킹 누락** | **0건 — 절대 규칙** |
| C-5 | 과잉 마스킹률 / 오류율별 재현율 | 참고 기록 / 곡선 기록 |
| 성능 | 내부 처리 p95 / E2E 체감 | ≤ 1,000ms / 측정·기록 |
| **F-2** | 종결 요건 판정 정확도 | **100% — 절대 규칙** (조건부) |
| F-1 / G-1 | 탐지 성능 | **측정 불가** |

**대표 실험**: STT 오류율 0~20% 구간별 곡선 — BM25 / 벡터 / 하이브리드 + **C-5 마스킹 재현율**을 같은 축에.

---

## 9. 개발 환경

```bash
# Ruby 3.3.12 (rbenv), Jekyll 4.4.1 — 이미 설치됨
cd /home/hi/call.solidbob.cloud/jekyll                # ⚠ 저장소 루트가 아니라 jekyll/ 안에서 실행
bundle exec jekyll serve --host 0.0.0.0 --port 4001   # ⚠ 4000 은 다른 사이트가 점유 중
bundle exec jekyll build                              # 기록 커밋 전 검증용
```

- `jekyll/_config.yml` 을 고치면 서버를 재시작해야 반영된다 (자동 리로드 대상이 아님).
- 외부(Tailscale 등) 접근은 이 머신에 Tailscale 이 없고 WSL2 가 NAT 이라 추가 설정이 필요하다. 로컬 미리보기는 `http://localhost:4001`.

---

## 10. 8주 마일스톤 요약

상세 체크리스트와 현재 상태는 `jekyll/_data/milestones.yml` 이 정본이다.

| 주차 | 주제 | 이 주차의 산출물 |
|---|---|---|
| 1 | 기반 + **전제 확인** | AI Hub 신청, **V1~V4 확인**, 스키마 3종, 문서 수집·청킹·ID 부여, 골든셋 10건, 하네스 + CI |
| 2 | 베이스라인 | 단순 RAG, 골든셋 50건 → **잠정** 기준선 |
| 3 | 실시간화 + **마스킹** | 스트리밍 STT + WebSocket + 트리거 v1 + 자막, **C-5**, 골든셋 150건 → **공식 기준선** |
| 4 | 검색 품질 | nori + dense_vector + RRF, 청킹 3종 비교 |
| 5 | **오류 내성 (핵심)** | 오류율별 성능 곡선, 자모 필터·리랭커 재측정 |
| 6 | 생성 + 컴플라이언스 | 근거 기반 카드, **폴백 모드 비교**, 분류기 파인튜닝, **코어 기준선 통과 확인** |
| 7 | 운영 + **F-2 체크포인트** | 내부/E2E 레이턴시, 캐싱, 토큰 비용, 공백 리포트, Docker |
| 8 | 마감 | 아키텍처 다이어그램, **실패 사례 분석**, 성능 리포트, 발표 |

**1주차 최우선**: AI Hub 데이터 신청(휴대폰 인증 + 승인 대기)과 **V1~V4 전제 확인**(채널 구성 / GPU / STT 숫자 출력 / 부분 결과 지연).
AI Hub 승인이 늦어도 5주차 핵심 실험은 텍스트 레벨 오류 주입이라 진행 가능하다 — 밀리는 것은 데모와 파이프라인 검증이다.

---

## 11. Claude 작업 방식

- 기획서에 이미 답이 있는 것을 다시 묻지 않는다. `_project/plan.md` 를 먼저 본다.
- 판단이 갈리면 선택지를 나열하지 말고 **권고안 하나 + 이유**를 제시하고 진행한다. 되돌리기 어려운 것만 사전에 확인한다.
- 측정으로 결론 낼 수 있는 논쟁은 측정한다. "이 방식이 더 나은 것 같다"는 근거가 아니다.
- 안 된 것을 됐다고 보고하지 않는다. 미달 지표는 숫자 그대로 쓰고 원인 가설을 붙인다.
