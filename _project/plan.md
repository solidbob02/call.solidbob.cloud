<!-- 원본: 실시간-상담원-어시스트-RAG-기획서-rev4.md (2026-08-25 수령, 3인 팀 버전) -->
<!-- 2026-08-27 부터 이 파일을 직접 수정한다(사용자 지시). 수정 이력은 아래에 남긴다. -->
<!-- 변경의 공식 경로는 _project/decisions/ 다 — CLAUDE.md §1 문서 우선순위. -->
<!-- 이전 판: plan-rev1-initial.md (초판) -->


> **⚠ 2026-08-28 수정 (rev.5)**: **다산콜센터 단일 도메인 + 외국인 지원**으로 방향을 바꿨다.
> 아래 「rev.5 변경 요약」에 무엇이 바뀌고 무엇을 남겼는지 적었다. 근거: `_project/decisions/201`.
>
> **⚠ 2026-08-27 수정**: 관계형 DB 를 **MySQL → PostgreSQL** 로 바꿨다(7곳). 이 파일은 원래 rev.4 사본이라 수정하지 않는 문서였으나 사용자 지시로 직접 고쳤다 — `CLAUDE.md` §3 규칙도 함께 갱신했다. 근거·되돌리는 법: `_project/decisions/018-DB-PostgreSQL-전환.md`, `rev4-보완지시서.md` 11번.

# 실시간 상담원 어시스트 RAG 시스템

> **주제**: LLM — Call Center 업무 자동화 서비스
> **한 줄 정의**: 서울시 120 다산콜센터 상담원이 통화 중에, **그 절차에 필요한 구비서류를 빠짐없이 보고**,
> **한국어가 서툰 외국인 고객의 말을 정확히 알아듣게** 하는 시스템
> **작성일**: 2026-08-24 (rev.4) → **2026-08-28 (rev.5 — 다산 단일 도메인 + 외국인 지원)**
> **팀**: 정성윤(AWS·인프라) / 류준(AI) / 장민석(서버) / 조서희(프론트엔드)

---

## 목차

- [rev.5 변경 요약](#rev5-변경-요약-2026-08-28)
- [rev.4 변경 요약](#rev4-변경-요약)
- [1. 주제 선택 근거](#1-주제-선택-근거)
- [2. 기능 명세](#2-기능-명세)
- [3. 시스템 아키텍처](#3-시스템-아키텍처)
- [4. 핵심 기술 난제](#4-핵심-기술-난제)
- [5. 데이터 확보 계획](#5-데이터-확보-계획)
- [6. 평가 설계](#6-평가-설계)
- [7. 팀 분업](#7-팀-분업)
- [8. 8주 마일스톤](#8-8주-마일스톤)
- [9. 레퍼런스 분석](#9-레퍼런스-분석)
- [10. 차별화 포인트](#10-차별화-포인트)
- [11. 리스크 및 대응](#11-리스크-및-대응)
- [부록 A — 민감 기능 공통 설계 원칙](#부록-a--민감-기능-공통-설계-원칙)
- [부록 B — 추가 검토 모듈 (H·I)](#부록-b--추가-검토-모듈-hi)
- [부록 C — 즉시 착수할 항목](#부록-c--즉시-착수할-항목)

---

## rev.5 변경 요약 (2026-08-28)

**도메인을 다산콜센터 하나로 좁히고, 외국인 고객 지원을 차별점으로 세웠다.**
근거·되돌리는 법: `_project/decisions/201`. 아래 표가 이 개정의 전부다.

| # | 바뀐 것 | 이유 |
|---|---|---|
| 1 | **도메인 4종 → 다산콜센터 하나** | AI Hub 4개 도메인 중 **음성 데이터를 가진 유일한 도메인**이다. 통번역 프로젝트에서 음성이 없으면 시작할 수 없다. 지식베이스 조항 102 → **20개**, 골든셋 50 → **13건** |
| 2 | **B: 문서 추천 → 필요서류 자동 제시** | 실측 — 다산 고객 질문의 **2.60%(174건)** 가 서류를 묻고, 그 문의가 **69종 서비스**에 흩어져 있다. 상담원이 외울 수 있는 규모가 아니다 |
| 3 | **A-5 동시 통번역 신설** | 새 차별점. ⚠ **A-3 이 아니다** — 아래 「ID 충돌」 참고 |
| 4 | **C-6 콜 가드 신설** | C-1~C-4 는 **상담원** 위반을, C-6 은 **고객** 폭언을 잡는다. **방향이 반대라 대체가 아니라 추가**다 |
| 5 | **D 에 감정분석 기반 상담품질 평가 추가** | 통화 후 처리의 확장 |
| 6 | **F-2 를 필요서류 체크리스트로 전용** | 다산에는 종결 처리 유형이 없다. 그러나 "필수 항목을 규칙으로 정의하고 빠진 것을 `missing` 으로 돌려주는" 구조가 같고, **서류를 빠뜨리면 고객이 헛걸음하므로 재현율 우선인 것도 같다** |
| 7 | **B-0 도메인 라우팅 폐기** | 라우팅할 도메인이 없다. 학습 코드까지 삭제했다 |
| 8 | **에스컬레이션 보류** | 제안된 그림이 AI 콜봇 선응대를 전제하는데 우리에게 콜봇이 없다 |

### 누구를 돕는가

국내 체류외국인 **287만 명**(2026-06-30, 정부는 연내 300만 전망). 외국인 노동자·재외국인·교포·
결혼이민자가 **한국어가 서툰 채로** 민원 전화를 건다. 120 다산콜재단이 **이미 사람 통역으로
외국어 상담을 운영 중**이라 수요가 검증돼 있다. AICC 시장에 대기업이 가득한 상황(9절)에서
**차별점을 여기서 만든다.**

### ⚠ ID 충돌 — 동시 통번역은 `A-5` 다

`decisions/201`(2026-08-28 최초 작성)은 동시 통번역을 **A-3** 으로 적었으나, 이 기획서의
**A-3 은 이미 「브라우저 실시간 전달(WebSocket)」** 이고 A-4 는 발화 구간 검출이다.
같은 ID 가 두 뜻을 가지면 추적성 규칙(`rfp-harness §1.3` — 코드 상단 `# Requirement: <ID>`)이
깨진다. **동시 통번역에 새 번호 `A-5` 를 준다.**

기존 A-3·A-4 를 밀지 않은 이유: 둘은 공개 사본 `docs/plan-rev4.1.md` 와 사이트 2절에 이미
실려 있어 재번호가 더 많은 곳을 건드린다. **아직 코드에 `# Requirement: A-3` 이 하나도
없을 때 잡았다** — 코드가 붙은 뒤였으면 훨씬 비쌌다.

> 부록 A 의 절 번호(`부록 A-3` 등)는 **다른 이름공간**이다. 기능 ID 와 헷갈리지 않도록
> 항상 `부록` 을 붙여 쓴다. 이 관례는 rev.4 부터 있었다.

### A-5 의 1차 범위는 ⓑ 다 (2026-08-28 확정)

「고객 외국어 → 한글」(ⓐ)과 「어눌한 한국어를 정확히 알아듣기」(ⓑ)는 **다른 기능이고 필요한
데이터가 다르다.** `decisions/201` 초안에는 둘이 섞여 있었다.

| | ⓐ 고객이 모국어로 말함 | **ⓑ 고객이 어눌한 한국어로 말함** |
|---|---|---|
| 기술 | STT(vi) → 번역(vi→ko) | STT(ko) 억양 내성 |
| 상용 API | 대체로 됨 | **가장 못하는 지점** |
| 평가 데이터 | ❌ 확보 못 함 | ✅ AI Hub 505 · 71479 (5.2절) |

**ⓑ 를 1차 범위로 한다** — ① 데이터가 있어 **측정이 가능하다** ② API 가 가장 약한 지점이라
우리 기여가 실제로 생긴다(ⓐ 는 구글이 이미 잘해서 API 를 끼우는 것 이상이 없다).
**ⓐ 와 상담원→고객 TTS 는 폐기가 아니라 여유 시 확장**이다.

### 뒤늦게 반영한 것 — 트리거 허용 창 1,500ms (2026-08-28)

**이 문서만 800ms 로 남아 있었다.** 팀은 **2026-08-25 에 이미 1,500ms 로 확정**했고
(`rev4-보완지시서` 1번 안 A), 코드(`ai/apps/evaluation/metrics/trigger.py`
`ON_TIME_WINDOW_MS = (0, 1500)`)·사이트 4.1절·6.1절은 그때 전부 반영됐다.
**기획서 사본에만 패치가 안 들어가 3일간 두 값이 공존했다.**

2026-08-28 사용자 확인으로 정리했다. **새 결정이 아니라 누락된 반영**이다 —
근거·되돌리는 법: `_project/decisions/202`.

> 이것이 `CLAUDE.md` §1 이 말한 **"파생 문서가 기획서와 다르면 기획서가 맞다"의 예외**다.
> 여기서는 **기획서 쪽이 낡았다** — 규칙이 상정한 방향과 반대다. 그래서 규칙을
> 기계적으로 적용했다면 잘못된 값을 살렸을 것이다. **어느 쪽이 최신인지는 결정 기록으로
> 판단한다**(문서 우선순위 1위가 결정 기록인 이유).

### 이 개정에서 **고치지 않은** 것

- **부록 B(H·I)·G-2** — 다산 전환의 영향을 아직 검토하지 않았다
- **§3 아키텍처의 도구 매핑** — 스택은 그대로다(전환은 도메인·기능 변경이지 스택 변경이 아니다)

---

## rev.4 변경 요약

> ⚠ **아래는 rev.4(2026-08-24)의 기록이다.** 그때는 코어가 **통신사 약관 기반**이었다.
> 이후 도메인 4종(`decisions/004`)을 거쳐 **다산콜센터 하나**가 됐다(`decisions/201`).
> **고치지 않고 남긴다** — 무엇을 왜 그렇게 결정했었는지가 기록이다(절대 원칙 8).
> 지금 무엇이 유효한지는 위 「rev.5 변경 요약」을 본다.

교차검증에서 지적된 항목을 반영했다. **아키텍처 구조와 투입자원 스택은 변경하지 않았다.** 새 DB, 새 검색엔진, 새 통신 프로토콜, 목록 밖 도구 — 아무것도 추가되지 않는다.

| # | 변경 | 성격 |
|---|---|---|
| 1 | ~~**F 모듈 도메인을 통신사 업무로 통일**~~ → **rev.5 에서 다산 필요서류로 다시 옮겼다** | 구조 — 아래 상세 |
| 2 | 생성 모델을 HuggingFace Transformers 용도에 명시 | 기존 스택 내 역할 명확화 |
| 3 | 트리거 "적절 시점"의 허용 창 정의 (~~0~800ms~~ → **0~1,500ms**, 2026-08-25 확정) | 1주차 블로커 해소 |
| 4 | C-5 목표를 측정 가능한 형태로 재정의 + 지표 우선순위 명시 | 평가 설계 |
| 5 | 골든셋 일정 재배치 (1주차 10개 → 2주차 50개 → 3주차 150개) | 순환 의존 해소 |
| 6 | 레이턴시를 내부 처리 / E2E 두 지표로 분리 | 측정 정직성 |
| 7 | 화자 분리 전제 확인을 1주차 태스크로, 리스크 표에 추가 | 누락 보완 |
| 8 | 팀 분업표를 실제 3인 구성으로 재작성 | 누락 보완 |
| 9 | 8주차 성공 조건 재설정 + F-2 폴백 명시 | 범위 관리 |
| 10 | "불변 감사 로그" → "추가 전용 감사 로그" 등 표현 정정 | 주장의 한계 준수 |

### 1번 상세 — F 모듈 도메인 통일

이전 리비전은 F 모듈(종결 요건 검증)을 **실종신고 접수·종결**이라는 공공기관 업무로 설계했다. 그러나 코어(A~E)는 통신사 약관 기반 기업 콜센터다. 두 시스템 사이에 재사용되는 것이 거의 없어, **두 번째 지식베이스 + 두 번째 데이터 모델 + 두 번째 화면 + 두 번째 골든셋**이 필요했다.

**rev.4는 F-2를 통신사 업무 안의 동일 패턴으로 옮긴다.**

| | rev.3 (폐기) | rev.4 |
|---|---|---|
| 대상 업무 | 실종신고 종결 | 해지·명의변경·보상 처리 종결 |
| 지식베이스 | 별도 (업무처리 규칙) | **코어와 동일 ES 인덱스** (약관 + 응대 매뉴얼 + 내부 처리 규정) |
| 데이터 모델 | 별도 (신고 사건) | **코어 `call` 확장** (종결 사유 + 근거 필드) |
| 화면 | 별도 종결 폼 | **기존 3분할 대시보드 + 모달** |
| 골든셋 | 별도 구축 | **코어 셋에 케이스 추가** |

**차별화 서사는 유지된다.** 10.6의 주장은 "RAG를 검증 도구로 쓴다"이지 특정 도메인이 아니다. 오히려 작동하는 시스템 안에서 시연되는 편이 강하다.

**사회적 문제의식도 유지된다.** "근거 없는 종결이라는 실패 구조"로 일반화하여 문제 정의에만 사용하고, 개별 사건은 구현 대상에서 분리한다.

### 모듈 우선순위

| 모듈 | 내용 | 우선순위 |
|---|---|---|
| A~E | 실시간 어시스트 코어 | **필수 — 절대 사수** |
| C-5 | 개인정보 마스킹 | **필수 — 코어 포함, 3주차** |
| F-2 | 종결 요건 검증 게이트 | 조건부 (7주차 체크포인트) |
| F-3·F-4 | 반복 처리 연결 / 감사 로그 | 여유 시 |
| G-2 | 지역 자원 연계 검색 | 여유 시 |
| H·I | 부록 B | 코어 완료 시 택1 |

---

## 1. 주제 선택 근거

### 1.1 투입자원 목록이 이미 답을 가리킨다

제시된 투입자원 중 **Google Speech-to-Text API**와 **WebSocket**은 리뷰 감정 분석 주제에서는 쓸 데가 없다. 이 두 도구가 목록에 있다는 것은 콜센터 주제를 염두에 둔 구성으로 보인다.

| 도구 | 콜센터 주제 | 리뷰 감정 분석 |
|---|---|---|
| Google STT | 필수 | 불필요 |
| WebSocket | 필수 | 불필요 |
| Elasticsearch | 핵심 | 선택 |
| HuggingFace | 활용 | 활용 |

### 1.2 "통화 후" 대신 "통화 중"

흔한 콜센터 프로젝트는 녹음 파일을 사후 처리한다(전사 → 요약 → 티켓 생성). 이는 사실상 **배치 파일 변환 파이프라인**이며 WebSocket이 필요 없다.

본 프로젝트는 **통화가 진행되는 중에 개입**한다.

| | 사후 처리형 | 실시간 개입형 (본 프로젝트) |
|---|---|---|
| 입력 | 완성된 녹음 파일 | 0.3초 단위 스트리밍 조각 |
| 질의 | 완결된 문장 | 미완성 발화 |
| 레이턴시 요구 | 없음 | 1초 이내 |
| RAG 난이도 | 낮음 | 높음 (트리거 판정 + 오류 내성) |
| 제품 범주 | 통화 분석 | Agent Assist |

### 1.3 왜 다산콜센터인가 (2026-08-28 추가)

**실측으로 골랐다.** AI Hub 에서 확보 가능한 4개 도메인을 실제로 세어 비교했다.

| | 다산콜센터 | 금융보험 | 쇼핑 | 질병관리본부 |
|---|---|---|---|---|
| **통화 음성** | **6,614건 / 12.7시간** | ❌ 없음 | ❌ 없음 | ❌ 없음 |
| 서류 문의 | 174건 (2.60%) · **69종 서비스** | 435건 (4.11%) · 145종 | **0건** | — |
| 외국인 맥락 | 서류 문의 상위 단어에 **`여권` 4위** | 없음 | 없음 | 없음 |
| 외국어 상담 수요 | **120이 사람 통역으로 이미 운영 중** | — | — | — |

**음성이 있는 곳이 여기뿐이다.** 서류 문의 자체는 금융이 더 많지만(435건 vs 174건),
음성이 없으면 A-5 통번역을 얹을 수 없고 A-1·A-2 도 실제 통화로 검증할 수 없다.
`여권` 이 상위 단어에 있다는 것은 **외국인 맥락이 데이터에 이미 들어 있다**는 뜻이다.

**공공 도메인이라 약관 저작권 문제도 가볍다** — 서울시 민원 절차는 공개 정보다.

---

## 2. 기능 명세

### 2.1 화면 구성

```
┌──────────────────────┬────────────────────────┬──────────────────┐
│  실시간 자막          │  필요서류 카드          │  경고            │
│  (+ 통번역)          │                        │                  │
│ 고객 🇻🇳 베트남        │ ┌────────────────────┐ │ ⚠ 고객 폭언      │
│  대리 신청하려는데    │ │ 전입신고 — 대리신청 │ │   감지 (C-6)     │
│  뭐 가져가야 되나요   │ │                    │ │                  │
│  ┌ 원문 ───────────┐ │ │ ☐ 위임장           │ │  (해당 없음)     │
│  │ 대리 신청하려는데 │ │ │ ☐ 위임자 신분증    │ │                  │
│  │ 뭐 가져가야       │ │ │ ☐ 대리인 신분증    │ │ ⚠ 확정적 보장    │
│  │ 되나요            │ │ │ ☐ 가족관계증명서   │ │   표현 (C-1)     │
│  └ 숙련도: 중급 ────┘ │ │   (동거인 아닌 경우)│ │                  │
│                      │ │                    │ │ → 권장:          │
│ 상담원               │ │ 📄 응대매뉴얼 3.1   │ │  "확인 후        │
│  네, 확인해          │ │ 📄 이용약관 4.1     │ │   안내드리겠     │
│  드리겠습니다        │ │                    │ │   습니다"        │
│                      │ │ ⚠ 4건 중 0건 확인  │ │                  │
│  전화번호는 ****     │ │    (F-2 체크리스트) │ │                  │
└──────────────────────┴────────────────────────┴──────────────────┘
```

**rev.4 에서 바뀐 것 셋** —

1. **왼쪽에 원문 병기.** 고객이 서툰 한국어로 말하면 STT 결과와 함께 **무엇을 어떻게
   말했는지**를 상담원이 볼 수 있어야 한다. STT 가 틀렸을 때 상담원이 알아채는 유일한 경로다.
   **숙련도 표시는 시스템의 확신도가 아니라 STT 신뢰도 맥락**이다 — 점수를 쓰지 않는다(부록 A-1).
2. **가운데가 "추천 카드" 에서 "필요서류 체크리스트"** 로 바뀌었다. 유사도 점수를 화면에
   쓰지 않는다 — rev.4 의 `유사도 0.87` 표기는 **부록 A-1 금지 표현에 걸린다**(2026-08-28 발견).
3. **오른쪽 경고가 두 방향**이다. C-1~C-4 는 상담원 발언을, **C-6 은 고객 폭언**을 잡는다.

> **화면에 쓰지 않는 것**: 위험도 점수 · 유사도 수치 · "안전합니다" 류 단정 · 고객 국적 추정.
> 부록 A-1 그대로다. **숙련도는 데이터셋 라벨(5.2절)이지 우리가 고객을 판정한 값이 아니다** —
> 실서비스에서 이 칸을 어떻게 채울지는 미정이며, 채울 수 없으면 **빈칸으로 둔다.**

### 2.2 기능 목록

#### A. 실시간 음성 → 텍스트

| 기능 | 설명 | 도구 |
|---|---|---|
| A-1 | 스트리밍 STT (부분 결과 포함) | Google STT |
| A-2 | 화자 분리 (고객 / 상담원) | Google STT diarization 또는 채널 분리 |
| A-3 | 브라우저 실시간 전달 | WebSocket (Node.js) |
| A-4 | 발화 구간 검출 (침묵 판정) | 직접 구현 |
| **A-5** | **동시 통번역** — 서툰 한국어를 정확히 전사(1차 범위 ⓑ) · 외국어 → 한글, 상담원 → 고객 TTS(확장 ⓐ) | Google STT (Chirp 3) · TTS |

> **A-5 는 rev.5 신설이다.** ⚠ `decisions/201` 초안이 이것을 A-3 으로 적었으나 A-3 은
> 이미 WebSocket 전달이다 — 「rev.5 변경 요약 › ID 충돌」 참고. **코드 주석에는 `A-5` 를 쓴다.**
>
> **Chirp 3 로 학습은 불필요하나 측정은 필요하다.** 85개 이상 언어·스트리밍·자동 언어감지를
> 지원하므로 모델을 만들 이유가 없다. 그러나 **정답 전사가 붙은 외국인 화자 음성이 없으면
> A-5 가 되는지 안 되는지 말할 수 없다**(절대 원칙 10). 확보 경로는 5.2절.

> **A-2 전제 확인 필요**: 실무 콜센터는 대개 2채널 스테레오(고객/상담원 물리 분리)를 사용해 diarization이 불필요하다. AI Hub 데이터의 채널 구성에 따라 난이도가 갈리므로 **1주차에 반드시 확인**한다(5.6절).

#### B. 필요서류 자동 제시 — **RAG 핵심 · 메인 기능**

> **rev.5 에서 좁혀졌다.** "관련 문서를 추천한다"에서 **"이 절차에 필요한 구비서류를
> 빠짐없이 띄운다"** 로 목적을 명확히 했다. 근거는 실측이다 — 다산 고객 질문의 **2.60%(174건)**
> 가 서류를 묻고, 그 문의가 **69종 서비스**에 흩어져 있다. 상담원이 외울 수 있는 규모가 아니고,
> **서류를 하나 빠뜨리면 고객이 헛걸음한다.** 그래서 **재현율 우선**이다.

| 기능 | 설명 |
|---|---|
| B-1 | 검색 트리거 판정 (언제 검색할지 결정) |
| B-2 | 하이브리드 검색 (형태소 + 임베딩) |
| B-3 | 리랭킹 (상위 후보 재정렬) |
| B-4 | 근거 기반 **서류 목록** 생성 |
| B-5 | 출처 표시 (문서명 + 조항) — **유사도 수치는 화면에 쓰지 않는다**(부록 A-1) |
| B-6 | 근거 부족 시 "관련 문서 없음" 반환 |

> **B-0 도메인 라우팅은 rev.5 에서 폐기됐다.** 라우팅할 도메인이 없다. 학습 코드까지 지웠다.
> 남은 사실 하나 — AI Hub 전사로 학습한 분류기(정확도 0.879)가 **골든셋 문체로 전혀 전이되지
> 않았다.** 혼동 행렬이 검색 기반 규칙과 동일했다. **A-5·C-6·D 에서 모델을 학습할 때 밟지 않을
> 함정으로 남긴다**(절대 원칙 8).

#### C. 실시간 컴플라이언스 감지 + 마스킹

| 기능 | 설명 |
|---|---|
| C-1 | 확정적 보장 표현 탐지 ("무조건", "100% 환불") |
| C-2 | 불필요한 개인정보 요구 탐지 |
| C-3 | 필수 안내 누락 감지 (**대리 신청 시 위임장·신분증 안내 등** — `DASAN-MANUAL-3.1`) |
| C-4 | 권장 대체 표현 제시 |
| **C-5** | **개인정보 실시간 마스킹** |
| **C-6** | **콜 가드 — 고객 욕설·폭언 탐지 (rev.5 신설)** |

> C-1~C-4는 KcELECTRA 계열 분류기 파인튜닝으로 구현. **재현율 우선 설계** — 놓치는 것이 과잉 경고보다 위험하다.

> **C-6 은 C-1~C-4 의 대체가 아니라 추가다.** 방향이 반대다 — C-1~C-4 는 **상담원**의 금지
> 표현을 잡아 회사를 보호하고, **C-6 은 고객의 폭언을 잡아 상담원을 보호한다.** 화자가 다르고
> 사전이 다르므로 별도 블록이다. 판정은 규칙, 설명만 LLM(절대 원칙 9)이고 **위험도 점수를
> 화면에 내지 않는다**(부록 A-1) — 감지 여부만 표시한다.

##### C-5 상세 — 코어에 포함

이 시스템은 **실시간 자막을 화면에 띄우고 전사 결과를 DB에 저장**한다. 마스킹이 없으면 그 자체가 설계 결함이다.

| 항목 | 처리 |
|---|---|
| 적용 지점 | ① 화면 자막 표시 전 ② PostgreSQL 저장 전 — **양쪽 모두** |
| 원본 보관 | 하지 않음. 마스킹된 형태만 저장 |
| 착수 시점 | **3주차 — 자막 구현과 동시에** |

**탐지 파이프라인** — STT 출력은 정형 문자열이 아니므로 정규화가 선행된다.

```
STT 전사 결과
   ↓
① 구분자·공백 제거 후 연속 숫자열 판정   ← 주 실패 모드 대응
   "123456 1234567" / "1234561234567" 모두 포착
   ↓
② 한글 수사 → 숫자 변환 (보조)
   "구일공" → "910"
   ↓
③ 패턴 매칭 (정규식) + NER (이름·주소)
   ↓
④ 마스킹
```

> **주의**: Google STT 한국어는 긴 숫자열을 대체로 아라비아 숫자로 정규화하므로, 한글 수사보다 **구분자 부재와 띄어쓰기 붕괴**가 지배적 실패 모드다. ①을 ②보다 앞에 둔다.

**대상 패턴 목록** (문서에 고정하고, 이 목록에 대해 누락 0건을 목표로 한다)

| # | 패턴 | 방식 |
|---|---|---|
| P1 | 주민등록번호 (13자리) | 정규식 |
| P2 | 카드번호 (14~16자리) | 정규식 |
| P3 | 계좌번호 (10~14자리) | 정규식 |
| P4 | 휴대전화번호 | 정규식 |
| P5 | 인증번호 (4~6자리, 문맥 조건) | 정규식 + 문맥 |
| P6 | 인명 | NER |
| P7 | 상세주소 | NER |

**지표 우선순위** — 두 목표는 동시에 최적화할 수 없으므로 순서를 고정한다.

> **누락 0건 > 과잉 마스킹 억제.** 애매하면 가린다. 과잉 마스킹률은 목표가 아니라 참고 기록이다.

#### D. 통화 후 처리

| 기능 | 설명 |
|---|---|
| D-1 | 상담 요약 생성 |
| D-2 | 문의 유형 자동 분류 |
| D-3 | 후속조치 항목 추출 |
| D-4 | **지식베이스 공백 리포트** — 검색 실패 질문 집계 |
| **D-5** | **감정분석 기반 상담품질 평가 (rev.5 신설)** |
| **D-6** | **통화 종료 즉시 핵심 내용 제시 (rev.5 신설)** — D-1 요약을 통화 직후로 당긴다 |

> **D-5 의 한계를 먼저 적는다.** 감정 라벨이 붙은 데이터는 AI Hub 「고객 응대 음성」에 있으나
> **다산 데이터에는 없다.** 다산으로 옮겨 재려면 라벨을 우리가 붙여야 하고, 그러면 그 라벨이
> 곧 정답이 된다 — **"팀이 만든 기준 안에서의 정확도"** 로 범위를 한정해 적는다(절대 원칙 10,
> F-2 의 100% 를 다루는 방식과 같다). **상담원 평가에 쓰이는 지표이므로 특히 조심한다** —
> 개인에 대한 단정("이 상담원은 불친절하다")을 시스템이 말하지 않는다(부록 A-1).

**D-4 확장 — 탐지 실패도 같은 루프에 넣는다**

| 대상 | 누적하는 것 | 쓰임 |
|---|---|---|
| B (검색) | 답을 못 찾은 질문 | 지식베이스 보강 우선순위 |
| C (컴플라이언스) | 놓친 위반 표현 | 분류기 재학습 우선순위 |
| F (게이트) | 통과했으나 사후 문제가 된 건 | 요건 스키마 보강 |

#### E. 평가 하네스

| 기능 | 설명 |
|---|---|
| E-1 | 골든셋 기반 자동 채점 (규칙 기반, LLM 채점 배제) |
| E-2 | STT 오류율별 성능 곡선 자동 생성 |
| E-3 | 레이턴시 분포 측정 (p50 / p95 / p99) |
| E-4 | 기준선 미달 시 CI 실패 |

---

#### F. 종결 요건 검증 → **필요서류 체크리스트로 전용** *(rev.5)*

> **다산에는 종결 처리 유형이 없다**(`POLICY-1`). 그런데 F-2 의 구조 — "필수 항목을 규칙으로
> 정의하고, 빠진 것을 `missing` 으로 돌려준다" — 는 **필요서류 체크리스트와 같다.**
> 서류를 빠뜨리면 고객이 헛걸음하므로 **재현율 우선인 것도 같다.** 모듈을 버리지 않고 전용한다.
>
> ⚠ **그래서 골든셋의 F-2 채점 케이스가 현재 0건이다.** 종결 케이스가 도메인과 함께 빠졌고,
> 서류 체크리스트 케이스는 아직 안 만들었다. 하네스는 **"측정 불가"** 로 보고하며,
> 이것을 통과로 위장하지 않는다.

##### 배경 — 문제 구조

**정보를 안내하는 업무에도 "빠뜨리면 안 되는 필수 항목"이 있다.**

rev.4 는 이 모듈을 통신사의 *종결 요건 검증*(해지 시 위약금 고지 등)으로 설계했다.
다산에는 그런 처리 유형이 없다 — 실측 카테고리가 **전부 정보 안내형**이고, 상담원이
"종결"을 선언하고 근거 필드를 채우는 절차가 존재하지 않는다(`DASAN-POLICY-1`).

**그러나 같은 실패 구조가 있다. 서류를 빠뜨리는 것이다.**

| 절차 | 규정상 필수 안내 | 누락 시 |
|---|---|---|
| 증명서 **대리 신청** | 위임장 · 위임자 신분증 · 대리인 신분증 | **고객이 헛걸음한다** |
| 전입신고 대리 | 위 3종 + 가족관계증명서(동거인 아닌 경우) | 재방문 · 민원 재발생 |
| 법정 기한 있는 민원 | 처리기한 안내 | 기대 불일치 · 항의 |

근거 조항: `DASAN-TERM-4.1`(증명서 발급 요건) · `DASAN-MANUAL-3.1`(대리 신청 확인).
**둘 다 이미 지식베이스에 있고**, `DASAN-MANUAL-3.1` 은 원래부터 **C-3(필수 안내 누락)
탐지 대상**으로 표시돼 있었다 — 이 전용은 없던 것을 만드는 게 아니라 **이미 있던 요구를
게이트로 세우는 것**이다.

**구조가 rev.4 의 F-2 와 같다** — ① 필수 항목을 규칙으로 정의한다 ② 충족 여부를 판정한다
③ 빠진 것을 `missing` 으로 돌려준다. **재현율 우선인 것도 같다.**
그래서 모듈을 버리지 않고 전용한다.

##### 무엇이 달라지는가 — 차단하지 않는다

| | rev.4 (통신 종결 요건) | **rev.5 (다산 필요서류)** |
|---|---|---|
| 판정 대상 | 상담원이 채운 근거 필드 | **대화에서 안내된 서류 항목** |
| 미충족 시 | **종결 차단** | **경고 표시** — 차단하지 않는다 |
| 절대 규칙 | 종결 100% 차단 | **필수 서류 누락 0건 탐지** |

**차단을 뺀 이유**: 다산에는 차단할 "종결" 행위가 없다. 통화는 상담원이 끊으면 끝난다.
게이트가 막을 수 있는 지점이 없으므로 **화면 경고가 유일한 개입 수단**이다.
rev.4 의 "100% 차단" 절대 규칙은 **"필수 서류를 하나도 놓치지 않는다"** 로 옮겨간다 —
차단률이 아니라 **탐지 재현율**이 절대 규칙이 된다.

##### 주장 범위 — 반드시 지킬 것

> **"안내 누락의 비용을 올린다."** — "빠뜨리는 일을 막습니다"가 아니다.

상담원이 화면을 보지 않으면 어떤 시스템도 막을 수 없다. 이 구분을 흐리면 프로젝트의
신뢰도가 떨어진다(부록 A-3). rev.4 의 표현("근거 없는 종결의 비용을 올린다")과 같은 태도다.

##### 기능 목록

| 기능 | 설명 | 착수 |
|---|---|---|
| ~~F-1~~ | ~~고위험 처리 유형 자동 감지~~ | **폐기** — 다산에 "고위험 처리 유형"이 없다 |
| **F-2** | **필요서류 체크리스트 게이트** — 필수 서류 미안내 시 경고 | 조건부 (7주차) |
| ~~F-3~~ | ~~동일 고객 반복 문의 연결~~ | **보류** — 통화 간 고객 식별이 필요한데 다산 데이터에 고객 ID 가 없다 |
| ~~F-4~~ | ~~추가 전용 처리 이력~~ | **보류** — 차단이 없어 이력을 남길 판정 행위가 없다 |

> **F-1·F-3·F-4 를 지운 것은 축소가 아니라 정직이다.** 셋 다 통신사 업무 구조를 전제로
> 설계됐고, 다산에서는 **적용할 대상 자체가 없다.** 억지로 옮겨 적으면 "측정할 수 없는
> 것을 측정한 것처럼" 쓰게 된다(절대 원칙 10). 되살릴 근거가 생기면 결정 기록으로 되돌린다.

##### F-2 상세 — 다산 기준

```
고객: "대리 신청하려는데 뭐 가져가야 되나요"
        ↓
[B] 코어와 동일한 ES 인덱스에서 해당 절차의 필요서류 검색
    → DASAN-TERM-4.1 · DASAN-MANUAL-3.1
        ↓
[규칙] 대화에서 각 서류가 안내됐는지 판정 (LLM 아님)
        ↓
┌─────────────────────────────────────┐
│ ⚠ 필수 서류 안내 누락                 │
│                                     │
│ 절차: 증명서 대리 신청                │
│ 필요 서류:                           │
│   ☑ 위임장                          │
│   ☐ 위임자 신분증                    │
│   ☐ 대리인 신분증                    │
│                                     │
│ 3건 중 1건 안내 → 2건 누락            │
│ 📄 근거: 이용약관 4.1 / 응대매뉴얼 3.1 │
└─────────────────────────────────────┘
```

**RAG 의 역할이 여기서 달라진다.** A~E 의 RAG 는 "답을 찾아 사용자에게 보여준다".
F-2 의 RAG 는 **"안내가 빠졌는지 검증할 근거를 가져온다"**. 검색 결과가 화면에 표시되는
정보가 아니라 **게이트 판정의 입력**이 된다. rev.4 에서 이 문단만은 그대로 성립한다.

판정 자체는 규칙으로 하고 LLM 은 설명만 생성한다(부록 A-2, 절대 원칙 9).

##### ⚠ 현재 측정 불가 상태

**골든셋의 F-2 채점 케이스가 0건이다.** 종결 케이스가 도메인과 함께 빠졌고, 서류
체크리스트 케이스는 아직 만들지 않았다. 하네스는 **"측정 불가"** 로 보고하며 **이것을
통과로 위장하지 않는다.** 3주차 골든셋 재확장(150건)에서 **구비서류 케이스 30건 이상**을
넣는 것이 선행 조건이다.

##### 외국인 고객과 겹치는 지점

**서류를 빠뜨렸을 때 가장 크게 손해 보는 사람이 외국인 고객이다.** 재방문 비용이 크고,
무엇이 빠졌는지 되묻기도 어렵다. A-5(통번역)와 F-2 가 **같은 사용자를 향한다** —
전환의 두 축이 여기서 만난다.

##### 아키텍처 영향 — 없음

| 필요한 것 | 기존 구성요소로 해결 |
|---|---|
| 규정 검색 | 코어와 동일 ES 인덱스에 내부 처리 규정 추가 적재 |
| 안내 항목 저장 | PostgreSQL `call` 테이블 확장 |
| 경고 UI | 기존 React 대시보드 위 카드 — **차단 모달이 아니다**(rev.5) |
| 판정 로직 | FastAPI 코어 내 모듈 |

새 DB, 새 검색엔진, 새 화면, 새 프로토콜 — **아무것도 추가되지 않는다.**

##### 상용 제품 대비 위치

조사 결과 국내 AICC 는 STT/TA 기반 **불완전판매 방지**(하면 안 되는 말 감지)를 제공한다.
**하면 안 되는 말을 잡는 기능은 있는데, 해야 하는데 안 한 말을 잡는 기능은 확인되지 않았다.**

> **rev.5**: 이 대비는 다산으로 옮겨도 그대로 성립한다. C-1~C-4 가 전자(금지 표현),
> F-2 가 후자(필수 안내 누락)다. **방향이 반대라 두 모듈이 서로를 대체하지 않는다** —
> C-6(고객 폭언)을 C-1~C-4 의 추가로 둔 것과 같은 논리다.

---

#### G. 위기 상황 자원 연계 *(여유 시 — 범위 엄격 제한)*

##### 절대 선

> **LLM이 위기 상황의 당사자와 직접 대화하게 만들지 않는다. 어떤 형태로도.**

시스템은 **상담원을 돕는 도구로만** 존재한다.

##### 단방향 경고 원칙

> **시스템은 "위험 신호가 감지됐다"만 말한다. "안전하다"는 절대 말하지 않는다.**

- 위험도 점수를 화면에 표시하지 않는다. 낮은 점수는 상담원의 경계를 늦춘다
- 신호가 있으면 안내를 띄우고, 없으면 아무것도 하지 않는다
- UI에서 **침묵이 "이상 없음"으로 읽히지 않도록** 명시한다

##### 기능

| 기능 | 설명 | 채택 |
|---|---|---|
| **G-2** | **지역 자원 연계 검색** — 주소지 기준 정신건강복지센터·자살예방센터 등 매칭 | 여유 시 |
| G-1 | 위기 대응 안내 표시 | 제외 (검증 불가) |

##### G-2만 남기는 이유

확장 기능 중 **유일하게 공개 데이터로 정답을 확인할 수 있다.** 관할이 맞는지, 운영시간이 맞는지, 폐지된 기관을 반환하지 않는지가 전부 검증 가능하다.

| 기능 | 탐지·판정 성능 검증 |
|---|---|
| F-1 / G-1 / H-1 / I-1 | 불가 또는 부분 (실제 데이터 없음) |
| F-2 / **G-2** | **가능** |

기술적 화려함이 아니라 **측정 가능성** 때문에 존재한다.

##### 만들지 않는 것

위험도 점수화 및 표시 / 당사자에 대한 자동 응답 / 위치 추적·신고 자동화 / 실제 위기 상담 통화 수집

---

## 3. 시스템 아키텍처

**rev.3에서 변경 없음.** F-2는 기존 FastAPI 코어 내 모듈로 들어간다.

```
[상담원 브라우저]
       │ React 대시보드 (자막 / 카드 / 경고 / 종결 모달)
       │
   WebSocket (양방향)
       │
[Node.js 게이트웨이]
       │  - 오디오 청크 중계
       │  - 자막/카드/경고 푸시
       │
       ├──→ [Google STT] ──→ 부분 전사 결과
       │
       ▼
[FastAPI 코어]
       │
       ├─ C-5 마스킹 모듈 ──→ 자막·저장 양쪽 앞단
       │
       ├─ 트리거 판정 모듈
       │     └─ 발화 완결성 / 침묵 길이 / 의도 신호
       │
       ├─ 검색 모듈 ──→ [Elasticsearch]
       │                  ├─ nori 형태소 (BM25)
       │                  ├─ dense_vector (임베딩)
       │                  └─ RRF 병합
       │
       ├─ 생성 모듈 ──→ [HF Transformers] 근거 기반 요약
       │
       ├─ 컴플라이언스 모듈 ──→ [분류기]
       │
       └─ F-2 게이트 모듈 ──→ 같은 ES 인덱스 조회 + 규칙 판정
       │
       ▼
   [PostgreSQL]
     ├─ call            통화 메타
     ├─ transcript      마스킹된 전사
     ├─ recommendation  추천 이력
     ├─ closure         종결 사유 + 근거 필드   ← F-2
     └─ eval_result     평가 결과
```

### 3.1 도구 매핑 (투입자원 기준 — 목록 밖 도구 없음)

| 투입자원 | 활용 방식 | 필수도 |
|---|---|---|
| Python | 전체 백엔드 | 필수 |
| FastAPI | 검색·생성·게이트 API | 필수 |
| Node.js | WebSocket 게이트웨이 | 필수 |
| WebSocket | 실시간 양방향 통신 | 필수 |
| Google STT | 스트리밍 음성인식 + 화자분리 | 필수 |
| Elasticsearch | nori + dense_vector 하이브리드 검색 | 필수 |
| **HuggingFace Transformers** | **① 임베딩 모델 ② 컴플라이언스 분류기 ③ 카드 요약 생성** | 필수 |
| React | 상담원 대시보드 | 필수 |
| PostgreSQL | 통화 메타·전사·추천·종결·평가 결과 | 필수 |
| Git | 형상관리 | 필수 |
| Docker | 서비스 컨테이너화 | 필수 |
| matplotlib | 오류율별 성능 곡선, 레이턴시 분포 | 필수 |
| VSCode / Ubuntu | 개발 환경 / 서버 OS | 필수 |
| Kubernetes / AWS Console | 배포 | 선택 |
| VirtualBox | 로컬 테스트 환경 | 선택 |
| OpenCV | 본 주제에서 활용처 없음 | 제외 |

> **생성 모델 확정**: 별도 상용 API를 도입하지 않는다. **HuggingFace Transformers로 소형 한국어 오픈 모델을 로드해 카드 요약을 생성**한다. 카드 요약은 검색된 근거를 2~3문장으로 다듬는 제한된 작업이라 대형 모델이 필요 없고, 레이턴시에도 유리하다. GPU 가용 여부는 1주차에 확인한다.

> **캐시 저장소**: 별도 저장소를 두지 않는다. **프로세스 인메모리 LRU + PostgreSQL 영속화**로 처리한다.

---

## 4. 핵심 기술 난제

### 4.1 트리거 판정 — 언제 검색할 것인가

일반 RAG는 완성된 질문을 받는다. 스트리밍은 다르다.

```
0.4초  "아"
0.9초  "아 저번에"
1.6초  "아 저번에 가입할"
2.4초  "아 저번에 가입할 때 6개월"
3.1초  "아 저번에 가입할 때 6개월 할인 된다고 했는데"
```

- **매 조각마다 검색** → 서버 과부하, 카드 깜빡임
- **너무 늦게 검색** → 대화가 이미 다음으로 넘어감

**검토할 전략**

| 전략 | 방법 | 예상 문제 |
|---|---|---|
| 침묵 기반 | N초 무음 시 검색 | 말이 느린 고객에서 지연 |
| 종결어미 기반 | "~요", "~까요", "~는데" 패턴 | 중간에 끊어 말하면 오발동 |
| 길이 기반 | 토큰 N개 누적 시 | 의미 무관하게 발동 |
| 하이브리드 | 위 조합 + 디바운싱 | 튜닝 복잡 |

**"적절한 시점"의 정의** — 규칙 기반 채점을 위해 허용 창을 고정한다.

> 골든셋의 각 질문에 **발화 종료 시각**을 라벨링한다.
> **발화 종료 후 0 ~ 1,500ms 이내 발동을 "적절"로 판정한다.**
> 0ms 이전(말 끝나기 전) 발동은 조기 발동, 1,500ms 초과는 지연 발동으로 각각 별도 집계한다.

**왜 1,500ms 인가** — 800ms 로는 목표 적절 발동률 0.85 가 **구조적으로 불가능**하다.
트리거가 Google STT 의 `is_final` 을 기다리는데, 그 엔드포인팅 지연이 **실측 +346ms** 다.
출발선이 이미 +346ms 이므로 800ms 창에 남는 여유는 **450ms 뿐**이고, 여기에 검색·판정
지연이 더해지면 대부분의 발동이 창을 넘겨 "지연"으로 집계된다.
**1,500ms = STT 엔드포인팅 실측 346ms + 판정 여유**다.

> ⚠ **이 값은 「빠르다」는 주장이 아니라 「적절/지연을 가르는 판정선」이다.** 개선 추이는
> 창이 아니라 **발동 지연시간 분포(p50/p95)** 로 본다(6.1절). 창을 넓혔다고 성능이
> 좋아진 것이 아니다 — **잴 수 없던 것을 잴 수 있게 만든 것**이다.

이 정의가 없으면 6.2절의 "모든 지표를 규칙으로 계산한다" 원칙을 지킬 수 없다. **1주차 골든셋 스펙에 포함한다.**

### 4.2 STT 오류 내성 — 가장 중요한 실험

음성인식은 틀린다. 사람은 알아듣지만 검색은 실패한다.

| 실제 발화 | STT 결과 | 문제 |
|---|---|---|
| 해지 | 해제 | 완전히 다른 조항 검색 |
| 생활하수도 | 생활 하수도 | 띄어쓰기로 형태소 분해 실패 |
| 가족관계증명서 | 가족 관계 증명서 | 동일 |

**Elasticsearch가 필요해지는 이유**

```
1. nori 형태소 분석기 → 띄어쓰기 오류 흡수
2. 자모 분해 필터      → 유사 발음 오인식 대응
3. dense_vector 필드   → 의미 기반 검색
4. RRF 병합            → 위 3개 결과 통합
```

**대표 실험 (발표의 핵심 숫자)**

```
STT 오류율을 통제하며 주입 → 구간별 성능 측정

오류율      0%    5%    10%   15%   20%
──────────────────────────────────────
BM25만      -     -     -     -     -
벡터만      -     -     -     -     -
하이브리드   -     -     -     -     -
C-5 마스킹  -     -     -     -     -   ← 재현율 곡선 추가
```

> 수치는 실측값으로 채운다. **"오류율 10%에서 Recall@5가 0.48 → 0.66으로 복구"** 형태의 문장 하나가 프로젝트 전체를 설명한다.

**이 실험의 성격을 정확히 서술할 것** — 실제 데이터 부족을 대체하는 합성이 아니라, **구간별 비교를 위해 오류율을 통제한 설계**다. 실제 통화로는 오류율 5%/10%/15% 곡선을 그릴 수 없다.

### 4.3 레이턴시 예산

**두 지표를 분리해 둘 다 측정한다.**

| 지표 | 구간 | 목표 |
|---|---|---|
| **내부 처리 지연** | 트리거 발동 → 카드 표시 | p95 ≤ 1,000ms |
| **E2E 체감 지연** | 발화 종료 → 카드 표시 | **측정·기록 (목표 미설정)** |

E2E에는 STT 부분 결과 반환 지연, 게이트웨이 홉, 브라우저 렌더링이 포함된다. 이 구간은 통제 밖이므로 목표를 걸지 않되, **통제 가능한 구간과 아닌 구간을 분리했다는 사실 자체를 성과로 기록한다.**

**내부 처리 예산**

| 단계 | 목표 | 최적화 수단 |
|---|---|---|
| 트리거 판정 | 10ms | 규칙 우선 |
| 검색 | 150ms | 인덱스 워밍업, 필드 최소화 |
| 리랭킹 | 200ms | 상위 20개만, 배치 추론 |
| 생성 | **첫 토큰 500ms** | **스트리밍 출력** — 카드는 점진 표시 |
| 전송 | 50ms | WebSocket 유지 연결 |

> 카드 완성까지 1초를 기다리게 하지 않는다. 첫 토큰부터 흘려보내면 상담원 체감이 크게 달라진다.

**⚠ A-5 예산은 아직 없다 (rev.5)**

위 표는 **A-5 이전에 짠 것**이다. 통번역이 들어가면 두 경로가 더해진다.

| 경로 | 더해지는 것 | 상태 |
|---|---|---|
| 고객 → 상담원 화면 | STT → (ⓐ면 번역) → 화면 | **미측정** |
| 상담원 → 고객 (확장 ⓐ) | 번역 → TTS 합성 → 재생 | **미측정** |

**1차 범위 ⓑ 에서는 번역 경로가 없다** — 한국어를 한국어로 전사하므로 STT 지연만
얹힌다. 그래서 **ⓑ 를 먼저 하는 것이 지연 측면에서도 유리하다.**
ⓐ 로 확장할 때 이 표를 다시 짠다. **목표치를 지금 지어내지 않는다**(절대 원칙 2).

---

## 5. 데이터 확보 계획

### 5.1 지식베이스 — 하나로 유지

**다산콜센터 이용약관 + 응대 매뉴얼 + 내부 처리 규정**을 한 ES 인덱스에 적재한다.
F-2(필요서류 체크리스트)가 참조하는 필수 항목 정의도 이 인덱스 안에 있다.

> **rev.5**: 원래 "통신사 요금제 약관"이었다가 4개 도메인을 거쳐 **다산 하나**가 됐다.
> 조항은 **20개**다(4종일 때 102개). **인덱스도 하나로 유지한다** — 도메인이 하나면
> 도메인별 인덱스와 단일 인덱스가 같은 것이 되므로, `decisions/017` 의 레이아웃 비교는
> **실측 없이 닫았다**(비교 대상이 사라진 것이지 결과가 나온 것이 아니다).

- 응대 매뉴얼과 내부 처리 규정은 팀이 직접 작성한다(가상 사업자 기준)
- 약관 원문 전재는 저작권 문제 소지가 있다. 검색 인덱스 내부 저장은 문제없으나 화면 표시는 요약·해석 중심으로 하고 출처를 명시한다

### 5.2 평가용 통화 음성 — AI Hub 실제 데이터

| 데이터셋 | 규모 | 특징 |
|---|---|---|
| AI Hub 「상담 음성」 | 3,000시간 | 교육·금융·통신판매, 가상 시나리오, 저작권 해결 완료 |
| AI Hub 「고객 응대 음성」 | 3,300시간 | 5개 클래스, 감정·의도 태깅 및 요약문 포함 |
| AI Hub 「저음질 전화망 음성」 | — | 실제 상담 환경 잡음 포함 |
| 서울 열린데이터광장 「행정 민원상담 음성」 | 56개 시나리오 유형 | 8kHz, 연령·지역·성별 조합 |

**A-5 를 위한 외국인 화자 한국어 음성 (2026-08-28 조사)**

| 데이터셋 | 규모 | 특징 |
|---|---|---|
| AI Hub 「외국인 한국어 발화 음성」 (`dataSetSn=505`, 2021) | **4,302시간** | 베트남·영어·일본·중국·태국어 모어 화자. 대본읽기 + 질문답변 |
| AI Hub 「교육용 아시아어 사용자 한국어 음성」 (`71479`, 2022) | **1,500시간** | **숙련도 4등급 라벨**(Beginner/Intermediate/Advance/Fluent) + 발음오류 태깅 |
| AI Hub 「교육용 영어 모국어 사용자 한국어 음성」 (`71469`) | — | 같은 계열 |

**`71479` 의 숙련도 등급이 핵심이다.** 등급별로 나누지 않으면 WER 하나로 뭉개져
"외국인 한국어 인식률 0.7" 같은 **해석 불가능한 숫자**가 된다.

⚠ **한계 — 수치에 반드시 함께 적는다.**

- **전화 음성이 아니다.** 위 데이터는 44.1kHz 스튜디오/온라인 녹음이고, 우리 실제 통화는
  **8kHz 협대역**이다(실측: 서울 민원상담 8000Hz/2ch · 저음질 전화망 8000Hz/1ch).
  **8kHz 로 다운샘플링해서 잰다.**
- **민원 대화가 아니다** — 낭독·질문답변이라 어휘가 다르다.
- **따라서 이 데이터로 낸 수치는 상한이다.** 실제 통화에서는 더 나쁘다.
  이 문장 없이 숫자만 옮기면 절대 원칙 10 위반이다.

> **2026-08-28 정정**: `decisions/201` 초안은 외국인 화자 데이터를 **"0건 — 최대 리스크"** 로
> 적었다. **틀렸다.** 보유분 5종에 없다는 사실을 확보 불가로 넘겨짚었고, 찾아보니 있었다.
> 남겨 두는 이유는 절대 원칙 8 이다 — 틀린 가설도 기록이다.

**주의**: 휴대폰 본인인증이 필요하고 승인에 시간이 걸린다. **1주차 즉시 신청.**
위 외국인 화자 데이터도 **내국인만 신청 가능**이며 우리는 해당된다.

> **부수 발견 (2026-08-28 실측)**: 서울 민원상담 음성 6,614개는 **연속 통화가 아니라
> 중앙값 6.5초짜리 발화 단위 클립**이고, 파일명에 화자 역할이 붙어 있다(민원인 5,549 ·
> 상담사 1,044). **화자 분리(A-2) 성능을 이 데이터로 잴 수 없다** — 5.5절 한계에 포함된다.
> 대신 **ASR 평가셋으로는 오히려 적합**하다.

**실제 데이터가 쓰이는 곳과 아닌 곳을 구분한다.**

| 용도 | 데이터 |
|---|---|
| STT 파이프라인 동작 검증, 데모 | AI Hub 실제 음성 |
| **4.2절 오류 내성 실험** | **텍스트 레벨 통제 오류 주입** |

### 5.3 골든셋 구축 — 일정 재배치

문서 ID가 존재해야 정답 라벨을 붙일 수 있으므로, 문서 수집과 골든셋 대량 작성은 같은 주에 병렬 수행할 수 없다.

| 주차 | 작업 |
|---|---|
| 1주차 | 문서 수집 + 청킹 + **ID 부여**, 골든셋 **10개** |
| 2주차 | 골든셋 50개 → **잠정 베이스라인** 측정 |
| 3주차 | 골든셋 150개 → **공식 기준선** 확정 |

> 2주차 측정값은 방향 확인용이며 회귀 검사에 고정하지 않는다. CI에 박히는 기준선은 150개 셋 완성 후 재측정값이다.

**골든셋 스펙에 포함할 항목**

```
- 고객 발화 스크립트
- 발화 종료 시각 (트리거 채점용, 4.1절)
- 이 시점에 표시되어야 할 문서 ID (정답)
- 표시되면 안 되는 문서 ID (오답 후보)
- 컴플라이언스 위반 삽입 여부
- 개인정보 패턴 삽입 여부 (P1~P7 중, C-5 채점용)
- (F-2 케이스) 처리 유형 + 근거 필드 충족 상태 + 기대 판정
```

### 5.4 F·G 데이터

| 모듈 | 필요 데이터 | 확보 |
|---|---|---|
| F-2 | 처리 유형별 필수 확인 항목 | **가능** — 5.1절 인덱스에 포함, 팀이 규정 작성 |
| F-3 | 반복 문의 판정용 가상 이력 | **가능** — 합성 |
| G-2 | 전국 정신건강복지센터·자살예방센터 목록 | **가능** — 공개 데이터 |
| F-1 / G-1 | 실제 고위험 통화 | **불가능** |

### 5.5 데이터 한계 — 반드시 명시할 것

**검증할 수 없는 것을 검증한 것처럼 제시하지 않는다.**

| 항목 | 한계 |
|---|---|
| F-1 / G-1 탐지 성능 | 실제 데이터 확보 불가. **측정 불가** |
| **C-5 마스킹** | 실제 통화의 개인정보 발화 패턴을 확보할 수 없다. **"P1~P7 패턴 목록에 대한 누락 0건"**으로 범위를 한정해 측정한다 |
| H-1 (부록 B) | AI Hub 감정 태깅은 폭언 라벨과 다르다. 분포 괴리 존재 |
| 4.2 오류 내성 | 통제된 합성 오류 기반. 실제 STT 오류 분포와 완전히 같지 않다 |

> **측정 불가능한 절대 규칙은 범위가 한정된 절대 규칙보다 나쁘다.** C-5의 목표를 패턴 목록으로 한정하는 것은 완화가 아니라 검증 가능하게 만드는 조치다.

### 5.6 1주차 전제 확인 항목

착수 전에 확인하지 않으면 후속 설계가 흔들리는 항목들이다.

| # | 확인 대상 | 미확인 시 영향 |
|---|---|---|
| V1 | **AI Hub 데이터의 채널 구성** (2채널 스테레오 / 모노) | A-2 화자 분리 난이도 → C-1~C-3, C-5 전체 |
| V2 | GPU 가용 여부 | 생성 모델 크기 및 레이턴시 예산 |
| V3 | Google STT 한국어 숫자 출력 형태 | C-5 정규화 단계 설계 |
| V4 | Google STT 스트리밍 부분 결과 지연 | E2E 지표 기준값 |

---

## 6. 평가 설계

### 6.1 지표

| 영역 | 지표 | 목표 기준선 |
|---|---|---|
| 검색 | Recall@5 | ≥ 0.70 (오류 없음) / ≥ 0.60 (오류 10%) |
| **A-5** | **숙련도 등급별 WER / CER** | **측정·기록** — 목표치를 지어내지 않는다. 등급별로 나눠 적고 평균 하나로 뭉개지 않는다 |
| **C-6** | 고객 폭언 탐지 재현율 / 정밀도 | ≥ 0.90 / ≥ 0.60 (C-1~C-4 와 같은 기준) |
| **D-5** | 감정분석 기반 품질 평가 | **측정·기록** — 라벨을 우리가 붙이므로 "팀이 만든 기준 안에서" 로 범위를 한정한다 |
| 검색 | MRR | ≥ 0.55 |
| 트리거 | 적절 발동률 (종료 후 **0~1,500ms**) | ≥ 0.85 |
| 트리거 | 조기 발동률 / 지연 발동률 | 각각 별도 집계 |
| 생성 | 환각 수치 발생 | 150문항 중 5건 이하 |
| 생성 | 출처 표시율 | 100% |
| 컴플라이언스 | 재현율 | ≥ 0.90 |
| 컴플라이언스 | 정밀도 | ≥ 0.60 |
| **C-5** | **P1~P7 패턴 마스킹 누락** | **0건 — 절대 규칙** |
| C-5 | 과잉 마스킹률 | 참고 기록 (목표 아님) |
| C-5 | STT 오류율별 마스킹 재현율 | 곡선 기록 |
| 성능 | 내부 처리 p95 | ≤ 1,000ms |
| 성능 | E2E 체감 지연 | 측정·기록 |
| 성능 | 통화당 토큰 비용 | 측정·기록 |
| **F-2** | **필요서류 체크리스트 판정 정확도** | **100% — 절대 규칙** *(조건부 착수)*. ⚠ **현재 골든셋 케이스 0건 — 측정 불가** |
| F-2 | 근거 규정 검색 정확도 | ≥ 0.90 *(조건부 착수)* |
| F-3 | 반복 문의 연결 재현율 | ≥ 0.95 *(여유 시 — 미구현 시 N/A)* |
| G-2 | 자원 매칭 정확도 | ≥ 0.95 *(여유 시 — 미구현 시 N/A)* |
| G-2 | 폐지·이전 기관 반환 | 0건 *(여유 시 — 미구현 시 N/A)* |
| ~~B-0~~ | ~~도메인 분류 정확도~~ | **rev.5 폐기** — 분류할 도메인이 없다 |
| F-1 / G-1 | 탐지 성능 | **측정 불가 — 5.5절** |

> 부록 B의 H·I 지표는 **착수 확정 시 이 표에 편입**한다.

> **⚠ 2026-08-28 — 이 표에 실측치를 적지 않는다.** 2026-08-27 에 낸 **Recall@5 0.857 ·
> MRR 0.702** 는 **4개 도메인 골든셋 14건 기준이라 무효**다. 지금 골든셋은 **13건**이고,
> 1건이 7.7%p 를 움직여 신뢰할 수 없다. **다산 기준으로 재측정하기 전까지 이 자리는 비워
> 둔다**(절대 원칙 2). 지운 것이 아니라 무효 표시해 사이트 6절에 남겨 두었다.

> **트리거 허용 창은 1,500ms 다** (2026-08-28 정리, `decisions/202`). 이 문서만 800ms 로
> 남아 있었고 코드·사이트는 2026-08-25 부터 1,500ms 였다. 산식은 4.1절에 있다.
> **적절 발동률은 합/불 판정선일 뿐이고, 개선 추이는 아래 발동 지연시간 분포(p50/p95)로 본다.**

### 6.2 채점 원칙

1. **LLM을 채점자로 쓰지 않는다.** 답을 만든 모델이 자기 답을 심판하면 순환이 된다. 모든 지표를 규칙으로 계산해 재현 가능하게 한다.
2. **여러 번 실행한 값 중 최저치를 기준선으로 고정한다.** 생성 모델은 같은 입력에도 답이 달라진다.
3. **기준선 미달 시 CI 실패**로 처리한다. 개선이 아니라 회귀 방지가 목적이다.
4. **C-5·F-2의 절대 규칙은 1건이라도 뚫리면 실패로 처리한다.** 평균값으로 평가하지 않는다.
5. **모든 지표는 정답 정의가 코드로 표현 가능해야 한다.** 표현할 수 없는 지표는 지표가 아니다(4.1절 트리거 허용 창이 그 예).

---

## 7. 팀 분업

### 7.1 담당

> **⚠ 이 표는 rev.5 에서 갱신됐다.** rev.4 는 **3인 체제**였다. 2026-08-26 팀 개편으로
> 플러터 앱 개발을 접고, 장민석이 프론트엔드에서 백엔드·AI 로 옮기고, 조서희가 새로
> 합류해 프론트엔드를 전담한다(`decisions/005`). 백엔드·AI 는 `fastapi/` 가 `server/`·`ai/`
> 로 갈리면서 **디렉터리 경계를 담당 경계로** 삼았다(`decisions/012`).
> **브랜치 이름 = 디렉터리 이름 = 담당자**다(`decisions/015`).

| 담당 | 인원 | 디렉터리 · 브랜치 | 범위 |
|---|---|---|---|
| **AWS·인프라** | 정성윤 | `services/gateway/` · `infra/` · `PM` | Node.js WebSocket 게이트웨이, Google STT 연동, 화자 분리, PostgreSQL 스키마, Docker·배포, 레이턴시 측정 인프라, **CI 운영** |
| **AI** | 류준 | `ai/` · `ai` | 청킹·BM25·리랭크·임베딩, 트리거 판정, 생성 모듈, 컴플라이언스 분류기, **A-5 통번역**, **C-6 콜 가드**, **D-5 감정분석**, 평가 하네스 |
| **서버** | 장민석 | `server/` · `server` | 파이프라인 배선, 클린 아키텍처, 계약(포트·DTO), **C-5 마스킹**(2026-08-27 이관 — `decisions/019`), **F-2 게이트** |
| **프론트엔드** | 조서희 | `apps/dashboard/` · `frontend` | React 대시보드(자막·원문 병기·서류 카드·경고), WebSocket 클라이언트, 데모 시나리오, 결과 시각화 |
| 공동 | 4인 | — | 골든셋 작성·라벨링, 발표 자료 |

> **rev.5 신규 기능 셋(A-5·C-6·D-5)을 전부 `ai/` 에 배정했다.** 셋 다 모델을 쓰고,
> `server/.importlinter` 가 `server/` 안에서 모델 라이브러리 import 를 금지하기 때문이다.
> ⚠ **이는 류준 쪽 부하를 다시 늘린다** — 7.2절이 경고했던 바로 그 구조다.
> **A-5 스파이크 결과에 따라 재분배가 필요할 수 있다.**

### 7.2 부하 경고 — 반드시 읽을 것

**류준이 백엔드와 AI를 동시에 맡고 있어 구조적으로 과부하다.** 4·5주차 핵심 실험과 F-2가 모두 이 한 사람에게 걸려 있다.

완화 조치를 문서에 고정한다.

| 조치 | 내용 |
|---|---|
| C-5를 정성윤에게 배정 | 전사 직후 처리 지점이라 STT 파이프라인 담당이 자연스럽다. 정규식 중심이라 AI 전문성 의존도가 낮다 |
| **CI 운영을 정성윤에게 이관** | 류준은 1주차에 하네스 **설계**만 하고, 이후 운영·유지는 정성윤이 맡는다 |
| 골든셋은 3인 공동 | 라벨링은 도메인 이해만 있으면 되므로 분산 가능하다 |
| 장민석이 시각화 담당 | matplotlib 성능 곡선·레이턴시 분포를 프론트 담당이 가져간다 |
| **F-2는 조건부** | 류준의 코어 작업이 지연되면 F-2를 설계 문서로 전환한다(8절 체크포인트) |

### 7.3 인터페이스 계약 우선

1주차에 모듈 간 스키마를 확정한다. 이것이 병렬 작업의 전제 조건이다.

```json
// 전사 이벤트 (마스킹 적용 후)
{
  "call_id": "c_001",
  "speaker": "customer",
  "text": "카드번호는 **** 입니다",
  "masked": [{"type": "P2", "span": [7, 11]}],
  "is_final": true,
  "utterance_end_ms": 3100
}

// 필요서류 카드 (rev.5 — 이전엔 "추천 카드")
{
  "call_id": "c_001",
  "trigger_at_ms": 3150,
  "cards": [
    {
      "title": "증명서 대리 신청 — 필요 서류",
      "documents": ["위임장", "위임자 신분증", "대리인 신분증"],
      "summary": "대리 신청은 위임장과 양측 신분증이 필요합니다.",
      "source": {"doc": "이용약관", "clause": "4.1"},
      "score": 0.87
    }
  ],
  "internal_latency_ms": 780,
  "e2e_latency_ms": 1240
}

// 필요서류 체크리스트 판정 (F-2, rev.5 — 이전엔 "종결 판정")
{
  "call_id": "c_001",
  "procedure": "증명서_대리신청",
  "evidence": {"위임장_안내": true, "위임자_신분증_안내": false, "대리인_신분증_안내": false},
  "verdict": "incomplete",
  "missing": ["위임자_신분증_안내", "대리인_신분증_안내"],
  "source": {"doc": "응대매뉴얼", "clause": "3.1"}
}
```

> **`score` 는 페이로드에만 있고 화면에 쓰지 않는다.** 부록 A-1 이 수치 표기를 금지하며,
> rev.4 의 화면 구성에 `유사도 0.87` 이 찍혀 있던 것은 **위반이었다**(2026-08-28 발견, 2.1절에서 제거).
> 계약에 남겨 두는 이유는 **평가 하네스가 랭킹을 채점할 때 필요**하기 때문이다.

> **`verdict` 가 `blocked` → `incomplete` 로 바뀌었다.** 다산에는 차단할 "종결" 행위가 없다
> — 통화는 상담원이 끊으면 끝난다. 게이트는 **경고만** 한다(F 블록 「무엇이 달라지는가」).

> ⚠ **이 계약 변경은 아직 코드에 반영되지 않았다.** 현재
> `server/apps/hub/app/dtos/closure_verdict_dto.py` 와 `db/schema.sql` 의 `closure` 테이블은
> **통신사 스키마**(`closure_type`·`reason`·`위약금_안내`)를 그대로 쓴다.
> **`server/` 는 장민석 담당**이므로(`decisions/012`) 여기서 고치지 않았다 — 위 표는 **바뀌어야
> 할 목표 형태**이고, 실제 변경은 그쪽과 맞춘 뒤에 한다. 영향 범위: DTO 1개 · 스키마 1개 ·
> `closure_gate` 스포크의 규칙 정의(`closure_rule.py`) · 관련 테스트.

---

## 8. 8주 마일스톤

> **⚠ rev.5 재배치 (2026-08-28).** 아래는 현재 시점(**2주차 진행 중**)에서 남은 일을 다시
> 배치한 것이다. **이미 지난 1~2주차는 실제로 한 일로 덮어썼다** — 계획을 사실처럼 남기면
> 나중에 무엇을 했는지 알 수 없게 된다. 일자별 기록은 `jekyll/_logs/` 가 담당한다.
>
> **rev.4 대비 바뀐 것 넷** — ① 도메인 축소로 **골든셋을 다시 만들어야 해서** 3주차가
> 재확장에 걸린다 ② **A-5 스파이크가 5주차 앞에 들어간다**(구현 전에 측정 가능성부터)
> ③ **C-6·D-5 가 6·7주차에 추가**됐다 ④ F-2 가 **필요서류 체크리스트**로 바뀌었다.

### 1주차 — 기반 + 전제 확인
- [ ] AI Hub 데이터 신청 (**최우선**)
- [ ] **5.6절 V1~V4 확인** (채널 구성 / GPU / STT 숫자 출력 / 부분 결과 지연)
- [ ] 인터페이스 스키마 확정 (전사·카드·종결 3종)
- [ ] 지식베이스 문서 수집 + 청킹 + **ID 부여**
- [ ] 골든셋 **10개** + 스펙 확정(발화 종료 시각, 개인정보 패턴 포함)
- [ ] 평가 하네스 골격(류준 설계) + CI 연결(정성윤)

### 2주차 — 베이스라인 *(진행 중)*
- [x] 가장 단순한 RAG 구현 (조항 단위 청킹, BM25만) — ES + nori
- [x] 골든셋 50개 → 잠정 베이스라인 측정 (**Recall@5 0.857 · MRR 0.702**)
- [x] **도메인 전환** — 다산 단일화(`decisions/201`). ⚠ **위 수치는 이 전환으로 무효가 됐다**
- [ ] STT 연동 (녹음 파일 배치 모드) — 정성윤

### 3주차 — 골든셋 재건 + 실시간화 + 마스킹
- [ ] **골든셋 다산 기준 150개 재확장** — **이번 주차 최우선.** 지금 13건이라 어떤 지표도
      신뢰할 수 없다. **구비서류 케이스 30건 이상**을 넣는다(F-2 전용의 선행 조건)
- [ ] **다산 기준 Recall@5 · MRR 재측정** → **공식 기준선 확정**
- [ ] 스트리밍 STT + WebSocket 파이프라인
- [ ] 화자 분리 — ⚠ **다산 음성은 발화 단위 클립이라 이걸로 잴 수 없다**(5.2절). 측정 방법부터 정한다
- [ ] 트리거 판정 v1 (침묵 기반)
- [ ] React 자막 화면 — **원문 병기 포함**(2.1절)
- [ ] **C-5 개인정보 마스킹** — 자막 표시 전 + DB 저장 전 양쪽
- [ ] **A-5 스파이크 시작** — AI Hub 505/71479 신청(승인에 시간이 걸리므로 **먼저 낸다**)

### 4주차 — 검색 품질
- [ ] nori 형태소 인덱스
- [ ] dense_vector 필드 + 임베딩 인덱싱
- [ ] RRF 하이브리드 병합
- [ ] 청킹 전략 3종 비교 → 지표 재측정

### 5주차 — 오류 내성 실험 (핵심 주차) + A-5 측정
- [ ] STT 오류 주입 스크립트
- [ ] 오류율 0~20% 구간별 검색 성능 곡선
- [ ] **구간별 C-5 마스킹 재현율 곡선**
- [ ] 자모 분해 필터 / 리랭커 적용 후 각각 재측정
- [ ] **A-5 — 8kHz 다운샘플링 후 숙련도 등급별 WER/CER 측정** *(rev.5 신설)*
- [ ] **A-5 판정**: 측정이 안 되거나 결과가 나쁘면 **코어에서 뺀다.** 스파이크의 목적이 이 판단이다
- [ ] matplotlib 시각화 — **오류 축 두 개(STT 품질 · 화자 숙련도)를 같은 그림에** (10.2절)

### 6주차 — 생성 + 컴플라이언스 + 콜 가드
- [ ] 근거 기반 **필요서류 목록** 생성 (HF Transformers), 스트리밍 출력
- [ ] "관련 문서 없음" 처리
- [ ] 컴플라이언스 분류기 파인튜닝 (C-1~C-4 — **상담원** 발화)
- [ ] **C-6 콜 가드** — 고객 욕설·폭언 탐지 *(rev.5 신설)*. C-1~C-4 와 **화자가 반대**라 사전이 다르다
- [ ] 환각 수치 발생률 측정
- [ ] **코어(A~E, C-5) 기준선 통과 확인** — 여기서 F·G·H·I 착수 여부가 갈린다

### 7주차 — 운영 관점 + 통화 후 처리 + F-2 체크포인트
- [ ] 내부 처리 / E2E 레이턴시 측정 — **A-5 경로 포함**(4.3절 예산을 여기서 채운다)
- [ ] 캐싱(인메모리 LRU) 적용 및 효과 측정
- [ ] 토큰 비용 계산
- [ ] 통화 후 요약(D-1) + 지식베이스 공백 리포트(D-4)
- [ ] **D-6 통화 종료 즉시 핵심 내용 제시** *(rev.5 신설)* — D-1 을 통화 직후로 당긴다
- [ ] **D-5 감정분석 기반 상담품질 평가** *(rev.5 신설)* — ⚠ **다산에 감정 라벨이 없다.**
      라벨을 우리가 붙이면 그 라벨이 곧 정답이 되므로 **"팀이 만든 기준 안에서"** 로 범위를 한정해 적는다
- [ ] Docker 구성

> **7주차 중반 체크포인트**
> 코어 기준선 통과 + 위 태스크 궤도 정상 → **F-2(필요서류 체크리스트) 구현 진행**
> 둘 중 하나라도 미달 → **F-2를 설계 문서로 전환** (시퀀스 다이어그램 + 요건 스키마 산출)
>
> ⚠ **F-2 는 골든셋에 구비서류 케이스가 실려 있어야 착수할 수 있다**(3주차 선행 조건).
> 지금은 채점 케이스가 **0건**이라 만들어도 잴 수가 없다.

### 8주차 — 마감
- [ ] (조건부) F-2 마무리 또는 설계 문서 작성
- [ ] (여유 시) G-2 또는 부록 B의 H·I 중 **택1**
- [ ] **8주차 후반 3일은 발표·문서 전용으로 고정** — 개발 태스크 금지
- [ ] 아키텍처 다이어그램
- [ ] **실패 사례 분석 섹션**
- [ ] **데이터 한계 섹션** (5.5절 반영)
- [ ] 성능 리포트, 발표 자료, 데모 시나리오

### 성공 조건 — 재설정

> **STT 오류 내성 실험(4.2) + 검색 품질 개선 수치.**
> 이 두 가지가 나오면 프로젝트는 성공이다.
> **F-2는 달성 시 추가 성과로 본다.**
>
> **rev.5 에서 바뀌지 않았다.** A-5(통번역)를 성공 조건에 **넣지 않는다** — 차별점이지만
> **측정 가능성이 아직 확인되지 않았다**(5주차 판정). 검증되지 않은 것을 성공 조건에 걸면
> 스파이크 결과가 나쁠 때 프로젝트 전체가 실패로 읽힌다. **A-5 도 F-2 와 같이 추가 성과로 둔다.**

이전 리비전은 성공 조건을 F-2에 걸었으나, F-2는 조건부 항목이므로 부적절하다. 폴백으로 가더라도 프로젝트가 실패로 읽히지 않도록 기준을 재설정한다.

**범위 관리 규칙**: 6주차 종료 시점에 코어 기준선을 통과하지 못했다면 F·G·H·I를 착수하지 않는다. **C-5만 예외로 코어에 포함되므로 3주차에 반드시 들어간다.**

---

## 9. 레퍼런스 분석

### 9.1 결론 요약

이 프로젝트가 다루는 것은 **Agent Assist**라는 확립된 제품 범주다. 글로벌·국내 모두 상용 제품이 있으며, 이는 위험이 아니라 **기회**다. 엔터프라이즈 B2B 시장이라 개인이 서비스로 경쟁할 대상이 아니고, "실무에서 실제로 팔리는 문제를 풀었다"는 근거가 된다.

### 9.2 글로벌 상용 제품

| 제품 | URL | 참고 포인트 |
|---|---|---|
| **Amazon Q in Connect** | https://docs.aws.amazon.com/connect/latest/adminguide/amazon-q-connect.html | 통화·채팅 중 의도 자동 감지 → 실시간 생성 응답·추천 액션·문서 링크. 기능 명세 최고 레퍼런스 |
| **AWS Live Call Analytics with Agent Assist** | https://aws.amazon.com/solutions/guidance/live-call-analytics-with-agent-assist-on-aws/ | **아키텍처 다이어그램·샘플 코드 공개.** 설계 참고 |
| **Google Agent Assist** | https://cloud.google.com/agent-assist | Proactive generative knowledge assist — 본 프로젝트의 트리거 판정과 동일 문제 |
| Google CCAI 문서 | https://docs.cloud.google.com/contact-center/ccai-platform/docs/agent-assist | 실시간 전사·감정 분석·지식 어시스트 모듈 구성 |
| Connect Wisdom 콘텐츠 적재 가이드 | https://aws.amazon.com/blogs/contact-center/ingesting-content-to-power-real-time-recommendations-and-search-with-amazon-connect-wisdom | 지식베이스 적재 전략 |

### 9.3 국내 상용 솔루션

| 솔루션 | URL | 참고 포인트 |
|---|---|---|
| **LG CNS FCC RT-Advisor** | https://www.lgcns.com/kr/service/ai/ai-contact-center | "숙련되지 않은 상담사도" 실시간 지식 제공 — 가치 제안 참고 |
| **KT A'Cen On-Premise** | https://enterprise.kt.com/pd/P_PD_AI_CC_003.do | **불완전판매 방지 솔루션** = F 모듈과 인접. Barge-in, SAD, 단문발화 인식 등 실시간 음성 난점 목록 |
| 솔트룩스 AICC | https://www.saltlux.com/kor/contents/view.do?mId=35 | 상담원 지원 + KMS + 실시간 모니터링 |
| 삼성SDS AICC | https://www.samsungsds.com/kr/aicc/aicc.html | 규제 복잡도와 컴플라이언스 니즈 |
| LG U+ AICC | https://www.lguplus.com/biz/all/telecom/phones-others/aicc/B000000150 | **개인정보 마스킹·암호화** — C-5 실무 근거 |
| 메타엠 | https://meta-m.co.kr/ | TA 대시보드 — 실시간 키워드 표시 UI |
| AICC 개념 정리 | https://www.tryvox.co/glossary/aicc | 한국어 통화의 STT 정확도 가정 위험, 민감정보 마스킹 기준 필요성 |

### 9.4 데이터셋

| 출처 | URL |
|---|---|
| AI Hub 상담 음성 (3,000시간) | https://aihub.or.kr/aidata/30711 |
| AI Hub 고객 응대 음성 (3,300시간) | https://aihub.or.kr/aihubdata/data/view.do?dataSetSn=71616 |
| AI Hub 민원 콜센터 질의-응답 | https://aihub.or.kr/aidata/30716 |
| AI Hub 메인 | https://www.aihub.or.kr/ |
| 서울 행정 민원상담 음성 | https://data.seoul.go.kr/dataList/OA-22804/L/1/datasetView.do |

### 9.5 G-2 참고 (여유 시)

| 항목 | URL |
|---|---|
| 한국생명존중희망재단 | https://www.kfsp.or.kr/ |
| 보건복지상담센터 109 안내 | https://www.129.go.kr/109 |

> **정책 근거 (발표 인용 가능)**: 보건복지부는 109 콜센터 증설과 함께 "AI를 활용한 상담일지 작성 및 위기 신호 조기 발굴을 통한 업무 부담 완화"를 추진 과제로 명시했다.
> https://www.mohw.go.kr/board.es?mid=a10503010100&bid=0027&act=view&list_no=1487674

### 9.6 기술 레퍼런스

| 항목 | 내용 |
|---|---|
| **한국어 STT 벤치마크** | https://github.com/rtzr/Awesome-Korean-Speech-Recognition — 상담 음성·저음질 전화망 도메인별 CER 비교. **V3·V4 확인에 활용** |
| Elasticsearch nori | 사용자 사전으로 업계 용어 등록 필수 |
| ES dense_vector + kNN | 하이브리드 검색 구성 |
| RRF | BM25 + 벡터 결과 병합 표준 기법 |
| KLUE 벤치마크 | 한국어 NLU — 분류기 파인튜닝 참고 |

---

## 10. 차별화 포인트

> **rev.5 에서 축이 하나 늘었다.** 10.0 이 새 차별점이고, 10.1~10.8 은 rev.4 의 것을
> 그대로 유지한다 — **기술적 차별점이 약해진 것이 아니라 대상이 좁고 선명해진 것**이다.

### 10.0 **아무도 안 하는 사용자를 고른다** *(rev.5 신설 — 가장 강한 축)*

AICC 시장은 대기업으로 이미 포화다(9절). **같은 것을 더 잘 만들어 이기는 게임이 아니다.**

국내 체류외국인 **287만 명**은 한국어가 서툰 채로 민원 전화를 건다. 상용 AICC 는
**깨끗한 한국어를 전제로 만들어져 있다.** 120 이 **사람 통역으로 외국어 상담을 이미 운영
중**이라는 사실이 수요를 증명하지만, 통역사를 부르기도 애매한 **"그럭저럭 통하는 어눌한
한국어"** 가 빈 자리로 남아 있다 — 다만 이 마지막 문장은 **추론이고 확인한 사실이 아니다.**

**그래서 A-5 의 1차 범위를 ⓑ 로 잡았다.** 고객이 모국어로 말하는 ⓐ 는 구글이 이미 잘하므로
**API 를 끼우는 것 이상의 기여가 없다.** 상용 API 가 가장 못하는 지점을 골라야 기여가 생긴다.

### 10.0-b 실측으로 도메인을 골랐다 *(rev.5 신설)*

"콜센터니까 통신사" 로 시작했다가, **AI Hub 4개 도메인을 실제로 세어** 다산으로 옮겼다(1.3절).
**음성이 있는 곳이 여기뿐**이었고, 서류 문의 상위 단어에 **`여권` 이 4위**로 있었다.
도메인 선택을 감이 아니라 **데이터로 한 기록**이 남아 있다.

### 10.1 "통화 중"에 개입한다
대부분의 콜센터 프로젝트는 통화 후 녹음을 처리한다. 실시간 개입은 트리거 판정, 오류 내성, 레이턴시 예산이라는 별개의 문제군을 만든다.

### 10.2 STT 오류율별 성능 곡선
> "오류율 10%에서 Recall@5가 0.48까지 떨어졌고, 하이브리드 검색으로 0.66까지 복구했습니다."

검색 품질을 **측정하고 개선한** 증거다. 마스킹 재현율 곡선까지 같은 축에 올리면 실험 하나로 두 가지를 설명할 수 있다.

> **rev.5**: 위 두 숫자는 **아직 우리가 낸 값이 아니라 형식 예시다.** A-5 의 숙련도 등급별
> WER 이 같은 축에 올라가면 **"오류가 어디서 오는가"를 두 방향(STT 품질 · 화자 숙련도)으로
> 분해**할 수 있다 — rev.4 에는 없던 축이다.

### 10.3 채점에 LLM을 쓰지 않는다
답을 만든 모델이 자기 답을 심판하면 순환이 된다. 모든 지표를 규칙으로 계산했고, **정답 정의를 코드로 표현할 수 없는 지표는 지표로 인정하지 않았다**(트리거 허용 창을 정의한 이유).

### 10.4 재현율 우선 설계의 근거
컴플라이언스와 마스킹에서 누락이 오탐보다 위험하다는 비대칭을 지표 우선순위로 명시했다.

### 10.5 실패 사례 분석
"이런 질문은 여전히 틀리고, 원인은 이것이며, 해결하려면 이런 접근이 필요하다."

### 10.6 RAG를 검증 도구로 쓴다 (F-2)
일반적인 RAG는 답을 찾아 보여준다. F-2의 RAG는 **행위를 차단할 근거를 가져온다.** 검색 결과가 사용자에게 보여지는 정보가 아니라 게이트 판정의 입력이 되는 구조는 흔하지 않다.

### 10.7 통제 가능한 구간과 아닌 구간을 분리했다
레이턴시를 내부 처리와 E2E 체감으로 나눠 측정했다. 통제 밖 구간에 목표를 걸지 않되 기록은 남긴다.

### 10.8-b 틀린 판단을 지우지 않는다 *(rev.5 신설)*

전환 과정에서 **우리가 틀린 것 셋을 그대로 남겼다.**

| 틀린 것 | 실제 | 남긴 곳 |
|---|---|---|
| "외국인 화자 데이터 0건 — 최대 리스크" | AI Hub 에 **5,800시간 이상 있었다.** 보유분에 없다는 것을 확보 불가로 넘겨짚었다 | 5.2절 · `decisions/201` 부록 A |
| B-0 분류기가 검색 기반을 이길 것 | **못 이겼다.** AI Hub 정확도는 0.879 로 올랐는데 골든셋 전이가 **0** 이었다 | 2.2절 B 블록 |
| A-3 을 통번역에 쓸 수 있다 | **ID 충돌.** A-3 은 이미 WebSocket 전달이었다 | rev.5 요약 › ID 충돌 |

셋 다 **고쳐서 지우는 대신 무엇이 틀렸는지와 함께** 남겼다(절대 원칙 8).
세 번째는 **코드에 `# Requirement:` 주석이 붙기 전에 잡아서 비용이 거의 들지 않았다** —
추적성 규칙이 실제로 작동한 사례다.

### 10.8 검증 불가를 명시한다
F-1·G-1의 탐지 성능은 측정할 수 없다고 문서에 적었다. C-5의 절대 규칙도 "P1~P7 패턴에 대해"로 범위를 한정했다. 자기 프로젝트의 미검증 영역을 스스로 표시하는 것이 과장된 주장보다 강한 인상을 남긴다.

---

## 11. 리스크 및 대응

| 리스크 | 영향 | 대응 |
|---|---|---|
| **AI Hub 승인 지연** | 데모·파이프라인 검증 지연 | 1주차 즉시 신청. **4.2절 핵심 실험은 텍스트 레벨 오류 주입이므로 승인과 무관하게 진행 가능** — 영향 범위가 좁다 |
| **화자 분리 정확도 미달** | C-1~C-3, C-5 전체의 성능 상한선이 됨 | V1으로 1주차 확인. **모노일 경우 시연은 물리 2채널(브라우저 2대)로 우회**하고, 모노 결과는 오프라인 평가 한계로 명시 |
| ~~**류준 부하 집중**~~ *(rev.5 — 재발)* | 2026-08-26 팀 개편으로 한 번 해소됐으나, **rev.5 신규 기능 셋(A-5·C-6·D-5)이 전부 `ai/` 로 갔다** | A-5 스파이크 결과를 보고 **재분배를 검토**한다. F-2·C-5 는 이미 `server/`(장민석)로 이관됐다 |
| **A-5 를 측정할 수 없을 위험** *(rev.5 신설)* | 차별점 자체가 검증 불가가 된다 | **구현 전에 스파이크부터** — 데이터 확보(5.2절) → 8kHz 다운샘플링 → 숙련도 등급별 WER. **막히면 A-5 를 코어에서 뺀다.** 스파이크의 목적이 그 판단이다 |
| **골든셋 13건 — 지표 신뢰 불가** *(rev.5 신설)* | 모든 검색 수치가 무의미해진다 | 다산 기준 **150건 재확장**이 3주차 최우선. 그 전까지 **어떤 문서에도 검색 수치를 적지 않는다** |
| **A-5 지연 예산 미계산** *(rev.5 신설)* | 실시간성이 깨진다 | 번역·TTS 왕복이 내부 처리 p95 1,000ms 위에 얹힌다. **4.3절 예산을 다시 짜야 한다** |
| **D-5 가 사람을 평가한다** *(rev.5 신설)* | 상담원 개인에 대한 부당한 단정 | 판정은 규칙, 설명만 LLM(절대 원칙 9). **개인에 대한 단정을 시스템이 말하지 않는다**(부록 A-1). "팀이 만든 기준 안에서" 로 범위 한정 |
| Google STT 한국어 성능 미달 | 파이프라인 품질 저하 | 벤치마크 사전 확인(9.6). 오류 내성 설계 자체가 주제이므로 오류가 많아도 실험은 성립 |
| STT API 비용 초과 | 예산 소진 | 반복 실험은 배치 모드. 실시간 데모만 스트리밍 |
| GPU 미확보 | 생성 모델 크기 제약 | V2로 1주차 확인. 소형 모델로 대응, 카드 요약은 대형 모델 불필요 |
| 레이턴시 목표 미달 | 데모 설득력 저하 | 목표 미달해도 **측정하고 원인을 분해한 기록**이 있으면 성과 |
| 범위 과다 | 미완성 | F·G·H·I는 조건부. B(검색)와 E(평가), C-5를 절대 사수 |
| **개인정보 노출** | 자막·DB에 평문 저장 | **C-5를 3주차 코어로 포함.** P1~P7 누락 0건 |
| 약관 저작권 | 공개 시 문제 소지 | 원문 전재 대신 요약·해석 + 출처 표시 |
| 민감 도메인 표현 | 과장·부적절 서술 | 부록 A 준수. 개별 사건 실명·세부 인용 금지 |

---

## 부록 A — 민감 기능 공통 설계 원칙

C-5, F, G, 부록 B의 H·I에 **공통 적용**한다. 코드보다 먼저 합의한다.

### A-1. 시스템의 발언 범위

| 할 수 있는 말 | 하면 안 되는 말 |
|---|---|
| "위험 신호가 감지되었습니다" | "안전합니다" |
| "이 종결 사유는 근거 3건이 필요합니다" | "이 건은 종결해도 됩니다" |
| "관할 기관은 다음과 같습니다" | "이 사람은 위험도 78%입니다" |
| "이 요청 유형은 확인 항목 3건이 필요합니다" | "이 사람은 사기범입니다" |

### A-2. 판정과 설명의 분리

- **판정은 규칙이 한다.** 종결 가능 여부, 요건 충족, 마스킹 대상 여부, 기관 매칭 — 전부 결정론적 로직
- **LLM은 설명만 한다.** 판정 결과를 사람이 읽을 문장으로 옮기는 역할
- 숫자와 판정을 생성 모델에 맡기지 않는다

### A-3. 주장의 한계

| 쓰지 않을 표현 | 대신 쓸 표현 |
|---|---|
| "이 시스템이 있었으면 막을 수 있었다" | "근거 없는 종결을 구조적으로 어렵게 만드는 설계를 검토했다" |
| "불변 감사 로그" | "추가 전용 감사 로그 — 완전한 불변성은 애플리케이션 레벨에서 보장 불가" |
| "개인정보를 완벽히 차단합니다" | "정의된 P1~P7 패턴에 대해 누락 0건을 목표로 한다" |
| "위기를 정확히 탐지합니다" | "탐지 성능은 검증되지 않았으며, 상담원의 판단을 대체하지 않습니다" |

### A-4. 발표·시연 시 주의

- 개별 사건의 실명·지역·세부 정황을 쓰지 않는다. 문제 정의는 **일반적 실패 유형**으로 서술한다 — "처리 시스템에서 종결 사유의 근거가 검증되지 않는 문제"
- 위기 통화를 재현하는 데모는 하지 않는다
- G-2를 다룰 경우 자살예방 보도준칙을 참고해 표현을 다듬고, 구체적 방법·장소 묘사는 넣지 않는다
- G-2 포함 시 발표 자료 말미에 상담 안내: **자살예방 상담전화 109 (24시간, 비밀보장)**

### A-5. 데이터 취급

- 실제 위기 상담 음성·기록을 수집하지 않는다
- 시연·평가용 시나리오는 팀이 작성한 가상 사례만 사용한다
- 가상 사례에 실제 인물을 연상시키는 정보를 넣지 않는다

---

## 부록 B — 추가 검토 모듈 (H·I)

**코어 완료 후 여유가 있을 때 둘 중 하나만** 착수한다. 착수 확정 시 지표를 6.1절에 편입한다.

### B-1. 모듈 I — 본인확인 요건 게이트 *(H·I 중 권장)*

**주장 범위**: "본인확인 없는 민감 요청 처리의 비용을 올린다." — "보이스피싱을 막습니다"가 아니다.

F-2와 구조가 1:1로 대응된다. 규정 문서만 바뀌고 흐름은 동일하다.

| 기능 | 설명 | 채택 |
|---|---|---|
| **I-2** | 본인확인 요건 검증 게이트 (F-2와 동일 패턴) | 권장 |
| I-3 | 공개 사기 화법 사례와 유사도 매칭 → **경고 카드만, 자동 차단 없음** | 여유 시 |
| I-1 | 고위험 요청 패턴 탐지 | 여유 시 (검증 불가) |
| I-4 | 통화 후 의심 플래그 집계 | 여유 시 |

**I를 권장하는 이유**: F-2와 스토리가 이어진다("근거 없는 종결" → "확인 없는 민감 요청"). H가 안고 있는 거버넌스 문제가 없다.

**한계**: 실제 사기 통화 데이터는 확보하지 않는다. I-1 탐지 성능은 측정 불가. 금융감독원·경찰청 공개 사기 유형 자료는 **규정 문서처럼 인덱싱하여 I-2·I-3의 RAG 근거로만** 쓴다.

### B-2. 모듈 H — 악성민원 대응 *(범위 축소 전제)*

**주장 범위**: "폭언 노출을 기록하고 대응을 돕는다." — "감정노동을 없앤다"가 아니다.

| 기능 | 설명 | 채택 |
|---|---|---|
| H-1 | 고객 채널 대상 폭언·욕설 실시간 탐지 | 가능 |
| H-2 | 상담원용 대응 문구 카드 (B-4 재사용) | 가능 |
| H-3 | 통화 종료 시 고위험 민원 태깅 (D-2·D-3 확장) | 가능 |
| H-4 | 상담원별 누적 노출 지표 | **제외 권장** |
| H-5 | 임계치 초과 알림 | **직전 1건 기반으로 축소** |

#### H-1의 데이터 — 정정

"C모듈과 동일한 분류기, 대상 채널만 전환"이라는 설명은 부정확하다. **아키텍처는 재사용되지만 모델은 새로 학습해야 한다.** C모듈은 확정적 보장 표현·개인정보 요구를 잡는 라벨셋이고, H-1은 폭언·욕설·성희롱이다. 라벨·학습 데이터·평가셋이 모두 다르다.

AI Hub 감정 태깅을 근거로 "검증 가능"이라고 보는 것도 낙관적이다. **감정 라벨과 폭언 라벨은 다르다.** 화난 고객이 모두 폭언을 하지 않고, 차분한 어조의 성희롱도 있다.

> **정정**: H-1의 출발점은 **혐오표현 멀티라벨 데이터셋(Unsmile 계열) 기반 분류기**다. 클래스 불균형이 크므로 정확도가 아닌 macro-F1으로 평가한다.

#### H-4 / H-5를 축소하는 이유

**상담원별 누적 지표는 직원 감시 도구가 될 수 있다.** 보호 목적으로 설계해도 조직에 들어가면 성과 평가로 전용되는 것이 이 종류 데이터의 전형적 경로이며, 팀 프로젝트에는 이를 통제할 거버넌스가 없다.

권장 형태 (아래로 갈수록 안전)

1. 집계는 팀·시간대 단위로만 — "야간 시간대 고위험 통화 비율"
2. 개인별 지표는 본인만 열람, 관리자 미노출
3. **H-5는 직전 통화 1건만 보고 판정** — 누적 기록 없이 목적 달성. 저장할 데이터가 없어 가장 깔끔하다

3번을 권장한다.

### B-3. H·I 리스크

| 리스크 | 대응 |
|---|---|
| H-4·H-5가 감시 도구로 전용 | 개인 단위 누적 저장을 하지 않는 설계 채택 |
| H-1을 C모듈 재사용으로 오해 | 별도 학습·평가셋 필요를 명시 |
| I-3이 자동 차단으로 오해 | "경고 카드만, 판단은 상담원" 반복 명시 |
| H·I 동시 착수 | **둘 다 하지 않는다. 택1, 그것도 여유 시** |

---

## 부록 C — 즉시 착수할 항목

### 오늘 처리

1. **AI Hub 회원가입 + 휴대폰 인증 + 데이터 신청** — 승인에 시간이 걸린다
2. **V1 확인** — AI Hub 데이터셋 상세 페이지에서 채널 구성(스테레오/모노) 확인. 이것이 3주차 설계를 좌우한다
3. **V2 확인** — GPU 가용 여부. 생성 모델 크기와 레이턴시 예산이 여기 걸린다

### 1주차 초반

4. **인터페이스 스키마 3종 확정** — 전사·카드·종결. 병렬 작업의 전제
5. **골든셋 10개 시범 작성** — 라벨링이 실제로 가능한지 검증. 여기서 막히면 지식베이스 구성을 바꿔야 한다
6. **골든셋 스펙에 발화 종료 시각 필드 포함** — 없으면 트리거 지표를 채점할 수 없다

### 팀 확인 사항

7. 투입자원 목록 밖 도구 사용 가능 여부 — 본 기획서는 목록 안에서만 구성했으므로 현재는 해당 없음
8. 7.2절 부하 완화 조치에 대한 3인 합의
