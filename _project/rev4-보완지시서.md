# rev.4 보완 지시서 — B안 기반 추가 반영 사항

> **대상**: 「실시간 상담원 어시스트 RAG 시스템」 기획서 rev.4 (3인 팀 버전)
> **성격**: 이 문서는 새 기획서가 아니라 **기존 rev.4에 얹을 패치 목록**이다. 언급되지 않은 부분은 그대로 둔다.
> **원칙**: 아키텍처 구조와 투입자원 스택은 변경하지 않는다. 아래 항목 중 신규 도구를 요구하는 것은 없다.
> **작성일**: 2026-08-24

---

## 0. 요약

| # | 항목 | 성격 | 우선순위 |
|---|---|---|---|
| **1** | 트리거 허용 창 800ms가 3주차 침묵 기반 구현과 충돌 | **일정·지표 영향** | **높음 — 1주차 고정 전** |
| **2** | GPU 미확보 시 생성 경로에 폴백이 없음 | **리스크 대응 부재** | **높음 — V2 확인과 동시** |
| **3** | F-2의 순환 검증 문제가 한계·리스크 표에 누락 | **평가 정직성** | **높음 — 문서 수정** |
| 4 | C-5 배정이 정규식/NER 구분 없이 통째로 인프라 담당에 감 | 분업 현실성 | 중간 |
| 5 | 아키텍처 "rev.3에서 변경 없음" 문구가 실제와 불일치 | 문서 정확성 | 낮음 |
| 6 | 개별 사건 자료의 추적성 소실 | 팀 운영 | 낮음 |
| **9** | **핵심 시나리오 도메인을 실제 확보 데이터(금융보험·다산콜센터·쇼핑·질병관리본부) 4종으로 한정** | **범위·데이터 정합성** | **높음 — 2026-08-26 확정** |
| **10** | **팀을 3인 → 4인으로 개편** (플러터 앱 중단, 장민석이 백엔드·AI로, 조서희 신규 합류해 프론트엔드 전담) | **팀 구성·부하 재분배** | **높음 — 2026-08-26 확정** |

1~3번, 9번, 10번은 반영을 권장(9·10번은 이미 확정·반영됨). 4~6번은 판단에 따라 선택한다.

---

# 1. 트리거 허용 창 800ms — 3주차 구현과 충돌한다

## 1.1 문제

기획서의 두 서술이 서로 충돌한다.

| 위치 | 내용 |
|---|---|
| 4.1절 | **"발화 종료 후 0 ~ 800ms 이내 발동을 '적절'로 판정한다"** |
| 6.1절 | 적절 발동률 목표 **≥ 0.85** |
| 8절 3주차 | **"트리거 판정 v1 (침묵 기반)"** |

침묵 기반 트리거는 정의상 **N초의 무음을 기다린 뒤** 발동한다. 발화가 끝난 시점부터 발동까지 최소 N초가 이미 소비된다.

```
발화 종료 (t_gold)
   │
   ├─ 침묵 대기 N ms          ← 침묵 기반 트리거의 고유 지연
   │
   ├─ 트리거 판정 10ms        ← 4.3절 내부 처리 예산
   │
   └─ 발동 (t_fire)

t_fire − t_gold ≈ N + 10ms
```

일반적인 침묵 임계값은 700~1,000ms다. 여기에 판정 지연이 더해지면 **거의 모든 발동이 800ms를 초과**하여 "지연 발동"으로 집계된다.

**결과**: 3주차 v1으로는 적절 발동률 0.85가 구조적으로 달성 불가능하다. 지표가 구현을 평가하는 것이 아니라, 구현 방식을 사전에 배제하는 상태가 된다.

## 1.2 제약 조건

허용 창 상한은 다음 부등식을 만족해야 한다.

```
허용 창 상한  >  침묵 임계값(N)  +  트리거 판정 지연(10ms)
```

침묵 임계값을 700ms로 잡으면 상한은 최소 710ms를 넘겨야 하고, 실용적으로는 여유를 둬야 한다. 1,000ms로 잡으면 상한은 1,000ms 이상이어야 한다.

## 1.3 반영안

### 안 A — 상한을 늘린다 (권장)

```
발화 종료 후 0 ~ 1,500ms 이내 발동을 "적절"로 판정한다.
  1,500ms 초과 → 지연 발동
  발동 없음     → 미발동
```

근거: 침묵 대기(≤1,000ms) + 판정·큐잉 여유(500ms). 침묵 기반·종결어미 기반·하이브리드 모두 이 창 안에서 경쟁할 수 있다.

### 안 B — 침묵 임계값을 먼저 정하고 창을 역산한다

1주차에 침묵 임계값 N을 실측으로 정하고(AI Hub 음성의 발화 간 간격 분포 확인), `허용 창 = N + 500ms`로 고정한다. 더 정확하지만 1주차 작업이 하나 늘어난다.

### 어느 안이든 함께 넣을 것

> **트리거 지연시간 분포(p50/p95)를 별도로 기록한다.**
> 허용 창은 합격/불합격 판정선일 뿐이고, 개선 과정을 보여주는 것은 분포다.
> "v1 침묵 기반은 p50 920ms였고, 종결어미 하이브리드로 p50 610ms까지 줄였습니다" — 이 문장이 지표 하나보다 강하다.

## 1.4 수정 위치

| 위치 | 수정 |
|---|---|
| 4.1절 "적절한 시점의 정의" | 0~800ms → **0~1,500ms** (또는 안 B 적용) |
| 6.1절 트리거 행 | 허용 창 값 동기화 + **"트리거 지연시간 분포(p50/p95) 기록"** 행 추가 |
| 8절 1주차 | "골든셋 스펙 확정"에 **"침묵 임계값 N 결정 및 허용 창 역산"** 포함 |

---

# 2. GPU 미확보 시 생성 경로에 폴백이 없다

## 2.1 문제

기획서는 생성 모델을 다음과 같이 확정했다.

> 3.1절: **"별도 상용 API를 도입하지 않는다. HuggingFace Transformers로 소형 한국어 오픈 모델을 로드해 카드 요약을 생성한다. GPU 가용 여부는 1주차에 확인한다."**
> 5.6절: **V2 — GPU 가용 여부** (미확인 시 영향: 생성 모델 크기 및 레이턴시 예산)

**V2가 "GPU 없음"으로 나왔을 때의 경로가 문서에 없다.**

- 상용 API는 이미 배제되어 있다
- CPU 추론으로 4.3절의 **첫 토큰 500ms**는 소형 모델이라도 달성이 불확실하다
- 6주차에 가서야 이 사실이 드러나면 코어 마감선에서 대응할 시간이 없다

## 2.2 반영안 — 폴백 모드를 명시한다

3.1절 생성 모델 항목 아래에 다음을 추가한다.

> **생성 단계 폴백 (V2 결과에 따라 발동)**
>
> GPU를 확보하지 못하거나 첫 토큰 500ms를 맞추지 못하는 경우, **생성 없이 검색 결과 스니펫을 그대로 카드에 표시하는 모드**를 기본값으로 전환한다.
>
> | 모드 | 카드 내용 | 레이턴시 |
> |---|---|---|
> | 생성 모드 | 근거를 2~3문장으로 요약 | 검색 + 리랭킹 + 생성 |
> | **폴백 모드** | **검색된 원문 스니펫 + 출처** | 검색 + 리랭킹 (생성 단계 제거) |
>
> 폴백 모드에서도 B-5(출처 표시)와 B-6("관련 문서 없음")은 그대로 동작한다. 상담원이 얻는 핵심 가치인 **"찾아주는 것"**은 유지된다.

## 2.3 이것이 리스크 대응이자 실험 조건이다

폴백을 미리 만들어두면 6주차에 다음 비교가 가능해진다.

```
카드 유용성 비교
  A) 검색 스니펫만 표시
  B) 생성 요약 카드 표시

→ 생성 단계가 실제로 얼마나 기여하는지 수치로 말할 수 있다
→ 토큰 비용 대비 효용을 논할 근거가 생긴다
```

대부분의 프로젝트는 "LLM을 썼다"에서 멈춘다. **"LLM을 뺐을 때와 비교했다"**는 한 단계 위의 서술이다. 10절 차별화 포인트에 한 항목으로 넣을 만하다.

## 2.4 수정 위치

| 위치 | 수정 |
|---|---|
| 3.1절 생성 모델 주석 | 폴백 모드 표 추가 |
| 5.6절 V2 행 | "미확인 시 영향"에 **"폴백 모드 전환 판단 불가"** 추가 |
| 11절 리스크 표 | GPU 미확보 행의 대응에 **"폴백 모드로 전환. 코어 가치는 유지됨"** 명시 |
| 8절 6주차 | (여유 시) **"생성 모드 vs 폴백 모드 카드 유용성 비교"** 추가 |
| 10절 | (선택) 차별화 항목으로 추가 |

---

# 3. F-2의 순환 검증 문제가 누락되어 있다

## 3.1 문제

기획서의 세 서술을 나란히 놓으면 문제가 드러난다.

| 위치 | 내용 |
|---|---|
| 5.1절 | **"응대 매뉴얼과 내부 처리 규정은 팀이 직접 작성한다"** |
| 5.3절 | 골든셋에 **"(F-2 케이스) 처리 유형 + 근거 필드 충족 상태 + 기대 판정"** 포함 |
| 6.1절 | **F-2 종결 요건 판정 정확도 100% — 절대 규칙** |

즉 **팀이 만든 규정을, 팀이 만든 게이트가, 팀이 만든 골든셋으로 검증한다.** 100%가 나오는 것은 설계상 당연하며, 이 수치는 "게이트가 잘 작동한다"가 아니라 **"규칙 엔진에 버그가 없다"** 정도만 의미한다.

**같은 문서가 다른 기능에 대해서는 이 문제를 정확히 지적하고 있다.**

| 기능 | 순환 검증 지적 | 위치 |
|---|---|---|
| C-5 | 있음 — "실제 통화의 개인정보 발화 패턴 확보 불가" | 5.5절 |
| F-1 / G-1 | 있음 — "측정 불가" | 5.5절 |
| H-1 | 있음 — "감정 라벨과 폭언 라벨은 다르다" | 5.5절 |
| **F-2** | **없음** | — |

5.5절 한계 표에도, 11절 리스크 표에도 F-2 관련 항목이 없다. **자기 기준을 일관되게 적용하지 않은 유일한 지점이다.**

## 3.2 왜 이것이 중요한가

- 발표·면접에서 **"이 100%는 무슨 의미인가요"**라는 질문이 나오면 답이 있어야 한다
- 문서의 다른 부분이 한계를 정직하게 적고 있어서, 이 한 곳의 누락이 더 눈에 띈다
- 10.8("검증 불가를 명시한다")의 설득력이 이 누락으로 약해진다

## 3.3 반영안

### ① 5.5절 한계 표에 행 추가

| 항목 | 한계 |
|---|---|
| **F-2 판정 정확도** | **규정·골든셋·게이트를 모두 팀이 작성했다. 100%는 "규칙 엔진이 명세대로 동작한다"는 의미이며, "실무 규정을 올바르게 해석한다"는 뜻이 아니다.** |

### ② 완화 조치를 5.1절에 명시

> **규정 문서 작성 시 지킬 것**
>
> 1. **실제 도메인(금융보험·다산콜센터·쇼핑·질병관리본부, [9번 항목](#9-데모-도메인을-실제-데이터-4종금융보험다산콜센터쇼핑질병관리본부으로-한정) 참조)의 QA 라벨에 나타난 카테고리·의도 체계와 표현을 따른다.** 조 번호 체계, 문장 형식, 용어를 실측 데이터의 카테고리·의도 명칭에 맞춘다
> 2. **규정 작성자와 게이트 구현자를 분리한다.** 규정을 쓴 사람이 요건 스키마를 짜면 해석 오류가 드러나지 않는다
> 3. **골든셋의 F-2 케이스는 규정 작성자가 아닌 사람이 만든다.** 같은 사람이 만들면 규정의 빈틈이 그대로 통과된다
> 4. 위 세 가지를 지켰다는 사실을 문서에 적는다

3인 팀에서 1~3번은 실행 가능하다. 규정 작성·스키마 구현·골든셋 라벨링을 세 사람에게 하나씩 배분하면 된다.

### ③ 11절 리스크 표에 행 추가

| 리스크 | 영향 | 대응 |
|---|---|---|
| **F-2 규정을 팀이 작성** | 판정 정확도 100%가 자기충족적 수치가 됨 | 실제 약관 체계에 맞춰 작성. **규정 작성자 / 스키마 구현자 / 골든셋 작성자를 분리.** 5.5절에 한계 명시 |

### ④ 10.8절 문장 보강

기존 문장 뒤에 한 줄 추가.

> F-2의 판정 정확도 100%도 "팀이 만든 규정 안에서의 100%"임을 명시했다. 절대 규칙이라는 표현이 검증 범위까지 절대적이라는 뜻은 아니다.

---

# 4. C-5 배정을 정규식/NER로 분리한다 *(선택)*

## 4.1 문제

7.2절 부하 완화 조치에서 C-5를 인프라 담당에게 배정하며 근거를 다음과 같이 들었다.

> "전사 직후 처리 지점이라 STT 파이프라인 담당이 자연스럽다. **정규식 중심이라 AI 전문성 의존도가 낮다**"

앞 문장은 타당하다. 뒤 문장은 절반만 맞다.

| 패턴 | 방식 | 성격 |
|---|---|---|
| P1 주민등록번호 | 정규식 | 인프라 담당 가능 |
| P2 카드번호 | 정규식 | 가능 |
| P3 계좌번호 | 정규식 | 가능 |
| P4 휴대전화번호 | 정규식 | 가능 |
| P5 인증번호 (문맥 조건) | 정규식 + 문맥 | 경계 |
| **P6 인명** | **NER** | **HF 모델 로드·튜닝 — AI 작업** |
| **P7 상세주소** | **NER** | **동일** |

추가로 5주차 **마스킹 재현율 곡선**은 오류 주입 스크립트·평가 하네스와 맞물린 작업이다.

## 4.2 반영안

| 범위 | 담당 |
|---|---|
| P1~P5 (정규화 + 정규식) + 파이프라인 배선 | 인프라 담당 |
| **P6~P7 (NER 모델)** | **AI 담당** |
| 5주차 마스킹 재현율 곡선 | AI 담당 (오류 주입 스크립트와 함께) |

부하 완화 효과는 대부분 유지된다. 정규식 5종과 파이프라인 배선이 작업량의 다수를 차지하기 때문이다.

**또는** P6~P7을 **"여유 시"로 내리는 방법도 있다.** 이 경우 C-5 절대 규칙의 범위를 **P1~P5로 한정**하고 그 사실을 6.1절과 5.5절에 반영한다. 3인 팀에서는 이쪽이 더 현실적일 수 있다.

## 4.3 수정 위치

| 위치 | 수정 |
|---|---|
| 7.2절 완화 조치 표 | C-5 배정 행을 P1~P5 / P6~P7로 분리 |
| 6.1절 C-5 행 | P6~P7을 여유 시로 내릴 경우 **"P1~P5 패턴 마스킹 누락 0건"**으로 범위 조정 |

---

# 5. 아키텍처 "변경 없음" 문구 정정 *(선택)*

## 5.1 문제

3절 서두에 **"rev.3에서 변경 없음"**이라고 적혀 있으나, 실제 다이어그램에는 다음이 추가되었다.

- `C-5 마스킹 모듈` 노드
- `F-2 게이트 모듈` 노드
- MySQL 하위 테이블 5종 (`call` / `transcript` / `recommendation` / `closure` / `eval_result`)

이 추가는 **새 도구·새 저장소를 도입한 것이 아니라 기존 구성요소 내부를 명시한 것**이므로 "스택 불변" 원칙에는 어긋나지 않는다. 오히려 문서로서는 rev.3보다 정확하다.

다만 **"변경 없음"이라는 문구 자체가 사실과 다르다.** 검토자가 대조하면 바로 드러난다.

## 5.2 반영안

> **구성요소 추가 없음.** 새 DB·검색엔진·프로토콜·목록 밖 도구를 도입하지 않았다.
> 단, rev.3 다이어그램에서 생략되어 있던 **C-5 마스킹 모듈, F-2 게이트 모듈, MySQL 테이블 구성**을 명시적으로 표기했다. 이는 기존 FastAPI 코어와 MySQL 내부를 세분화한 것이며 신규 인프라가 아니다.

---

# 6. 개별 사건 자료의 추적성 *(선택)*

## 6.1 상황

rev.3에 있던 F 모듈 배경 보도 링크가 rev.4에서 삭제되었다. 외부 배포 안전성 관점에서는 이 편이 깔끔하며, 부록 A-4 원칙에도 부합한다.

다만 **"이 아이디어가 어디서 나왔는가"를 팀이 되짚을 근거가 사라진다.** 몇 주 뒤 F 모듈의 문제의식을 설명해야 할 때 출처를 다시 찾아야 한다.

## 6.2 반영안

기획서 본문에는 두지 않되, **팀 위키·비공개 노션·저장소 내 `docs/internal/` 등 별도 공간**에 다음 형태로 남긴다.

```
F 모듈 문제의식의 출처 (내부 전용 — 발표·제출본 반영 금지)
  - 링크
  - 참고한 것: 종결 사유의 근거가 검증되지 않는 구조
  - 참고하지 않은 것: 개별 사건의 시점·지역·경위·당사자
  - 취급: 진행 중 사안이며 유가족이 존재한다. 발표에서 언급하지 않는다
```

---

# 9. 데모 도메인을 실제 데이터 4종(금융보험·다산콜센터·쇼핑·질병관리본부)으로 한정

## 9.1 문제

rev.4는 F 모듈 도메인을 "코어와 같은 통신사 콜센터 업무"로 통일했다([1번 항목](#1번-상세---f-모듈-도메인-통일)). 이 결정 자체는 유지하지만, 전제가 하나 빠져 있었다 — **통신 도메인 데이터를 실제로 확보한 적이 없다.** 지식베이스(`knowledge-base/`)의 "한별텔레콤" 약관·매뉴얼·내부규정은 전부 팀이 처음부터 지어낸 것이고, 대조할 실제 데이터가 없었다.

반면 실제로 신청·다운로드한 AI Hub 「민원(콜센터) 질의-응답」데이터셋(`data/raw/aihub-minwon-qa/`)은 통신이 아니라 **금융보험 · 다산콜센터 · 쇼핑(K쇼핑) · 질병관리본부** 4개 도메인으로만 구성돼 있다. 각 레코드는 화자·발화문·고객의도·상담사의도·개체명·**지식베이스 참조 필드**까지 포함한 실측 라벨이다.

## 9.2 반영안

**핵심 시나리오를 이 4개 도메인으로 한정하고, 전부 지원한다** (1개로 좁히지 않는다 — 확보한 데이터를 버리지 않기 위함). 상세 근거·되돌리는 법은 `_project/decisions/004-데모-도메인-4종-확정.md` 참고.

| 기존 (한별텔레콤 단일) | 변경 후 |
|---|---|
| 지식베이스 1벌(요금제약관·응대매뉴얼·내부처리규정) | 도메인별 지식베이스 4벌(`knowledge-base/{finance,dasan,shopping,health}/`) |
| F-2 처리 유형 = 해지·명의변경·보상 (통신 특화) | 도메인별로 "종결형 처리"가 있는 곳(금융보험·쇼핑)에만 F-2 적용. 다산콜센터·질병관리본부는 안내형 업무라 F-2 대상에서 제외 |
| 골든셋 10개, 통신 시나리오 | **재작성 필요 — 후속 티켓.** 4개 도메인 비율로 재구성 |
| DB 스키마의 `subscriber`/`plan` 등 통신 특화 테이블 | **미반영 — 후속 엔지니어링 티켓.** 이번 반영 범위에서 제외 |

## 9.3 수정 위치

| 위치 | 수정 |
|---|---|
| 1절(F 모듈 도메인 통일) | "통신사 콜센터"라는 표현은 예시 중 하나로 남기되, 코어 도메인이 4종임을 명시 |
| 3.3절②(규정 작성 시 지킬 것) | "실제 통신사 약관" → "실제 도메인별 QA 라벨의 카테고리·의도 체계" |
| `jekyll/docs/01,02,04,05,06,07,08,09,12,13,14,15,16` | 각 절 예시·데이터 계획·평가 설계를 4개 도메인 기준으로 갱신 |
| `knowledge-base/` | 도메인별 서브폴더로 재구성 |

---

# 10. 팀을 3인 → 4인으로 개편

## 10.1 문제

rev.4는 "3인 실명 버전"으로 채택됐고([`_project/decisions/001`](https://github.com/solidbob02/call.solidbob.cloud)),
[7.2절 부하 경고](/docs/07/)는 **류준이 백엔드·AI를 혼자 맡는 구조적 과부하**를 지적하며
C-5·CI 운영을 정성윤에게 이관하는 완화 조치를 뒀다. 완화 조치일 뿐 근본 해결은 아니었다.

## 10.2 반영안

사용자 통보에 따라 팀을 다음과 같이 재편한다. 상세 근거·되돌리는 법:
`_project/decisions/005-팀-개편-4인-체제.md`.

| | 이전 (3인) | 이후 (4인) |
|---|---|---|
| AWS·인프라 | 정성윤 | 정성윤 (변경 없음) |
| 백엔드·AI | 류준 (단독) | 류준 + 장민석 (공동) |
| 앱·프론트엔드 | 장민석 | 조서희 (웹 프론트엔드 전담. **앱(Flutter) 개발은 중단**) |

**부하 경고의 성격이 바뀐다.** 7.2절이 지적한 근본 문제(백엔드·AI 단독 담당)가 이번
개편으로 해소된다. 기존 완화 조치(C-5·CI→정성윤)는 유지하되, "과부하 회피"가 아니라
"인프라 인접 업무의 정상 분업"으로 재서술한다.

## 10.3 수정 위치

| 위치 | 수정 |
|---|---|
| 7.1절 담당 표 | 백엔드·AI에 장민석 추가, 앱·프론트엔드를 조서희로 교체 |
| 7.2절 부하 경고 | 문제 해소 사실 반영, 기존 완화 조치의 성격 재서술 |
| `CLAUDE.md`, `.claude/rules/rfp-harness.md`, `jekyll/kanban.markdown` | 팀 구성 문구 갱신 |
| 커밋 브랜치 컨벤션 | `flutter` 브랜치 사용 중단 명시 (삭제는 하지 않음) |

---

# 7. 반영 체크리스트

## 반영 권장 (1~3번)

- [ ] 4.1절 트리거 허용 창 조정 (0~800ms → 0~1,500ms 또는 침묵 임계값 역산)
- [ ] 6.1절 트리거 지표 동기화 + **지연시간 분포(p50/p95) 기록** 행 추가
- [ ] 1주차 태스크에 **침묵 임계값 결정** 포함
- [ ] 3.1절에 **생성 단계 폴백 모드** 표 추가
- [ ] 11절 GPU 미확보 리스크 대응에 폴백 전환 명시
- [ ] 5.6절 V2 행 영향 범위 보강
- [ ] 5.5절 한계 표에 **F-2 판정 정확도의 순환 검증** 행 추가
- [ ] 5.1절에 **규정 작성 / 스키마 구현 / 골든셋 라벨링 담당 분리** 원칙 추가
- [ ] 11절에 **F-2 규정 자체 작성** 리스크 행 추가
- [ ] 10.8절에 F-2 100%의 범위 한정 문장 추가
- [x] **데모 도메인을 실제 데이터 4종(금융보험·다산콜센터·쇼핑·질병관리본부)으로 한정** — [9번 항목](#9-데모-도메인을-실제-데이터-4종금융보험다산콜센터쇼핑질병관리본부으로-한정), `_project/decisions/004` (2026-08-26 반영)
- [x] **팀을 3인 → 4인으로 개편** (플러터 중단, 장민석→백엔드·AI, 조서희 신규 합류) — [10번 항목](#10-팀을-3인--4인으로-개편), `_project/decisions/005` (2026-08-26 반영)

## 선택 (4~6번)

- [ ] 7.2절 C-5 배정을 P1~P5 / P6~P7로 분리 (또는 P6~P7을 여유 시로 하향)
- [ ] 3절 "rev.3에서 변경 없음" → "구성요소 추가 없음, 내부 모듈 명시" 로 문구 정정
- [ ] 개별 사건 자료를 팀 내부 공간에 별도 보관

## 반영 후 재확인

- [ ] 6.1절 지표 중 **정답 정의를 코드로 표현할 수 없는 항목이 남아 있는지** 재점검 (6.2절 원칙 5)
- [ ] 5.5절 한계 표에 **누락된 기능이 더 없는지** 재점검 — F-2가 빠져 있었던 것과 같은 유형의 누락

---

# 8. 반영하지 않아도 되는 것

교차검증 과정에서 나왔으나, 현재 rev.4가 이미 더 나은 방식으로 처리한 항목들이다. **되돌리지 말 것.**

| 항목 | rev.4의 처리 | 판정 |
|---|---|---|
| 생성 LLM을 3.1 표에 별도 행으로 추가 | HuggingFace Transformers의 세 번째 용도로 명시 | **rev.4가 낫다** — 스택 불변 원칙에 더 엄격 |
| C-5 정규화 순서 (한글 수사 우선) | 연속 숫자열 판정을 앞에, 한글 수사를 보조로 | **rev.4가 낫다** — Google STT 한국어의 실제 실패 모드에 부합 |
| 과잉 마스킹률 ≤ 0.05 목표 | "누락 0건 > 과잉 억제" 우선순위 고정, 과잉률은 참고 기록 | **rev.4가 낫다** — 동시 최적화 불가능한 두 목표를 정리 |
| 4.2절 실험을 "합성 오류 기반"으로 서술 | "구간별 비교를 위해 오류율을 통제한 설계"로 서술 | **rev.4가 낫다** — 방어가 아니라 설계 의도로 프레이밍 |
| G-2를 7주차로 당기기 | 8주차 "여유 시"로 유지 | **rev.4가 낫다** — 3인 팀 부하 고려 |
| F-4를 채택으로 승격 | "여유 시"로 유지 | **rev.4가 낫다** |
| 트리거 허용 창 하한 −300ms | 하한 0ms | **rev.4가 낫다** — `is_final` 기준으로 음수 구간은 정의가 애매 |
