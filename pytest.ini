[pytest]
# services/core 등 각 서비스 tests/ 아래에서 실행한다 (testing.md 참고).
# 실제 DB·ES·외부 STT/LLM이 필요한 테스트는 @pytest.mark.integration으로 분리하고
# 기본 실행에서는 뺀다.
markers =
    integration: 실제 인프라(MySQL·Elasticsearch·외부 API)가 필요한 테스트
addopts = -m "not integration" -q
