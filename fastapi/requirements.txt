# CallGuard FastAPI 코어 — Python 의존성 (모델 다운로드/실험용 포함).
# 2026-08-26 저장소 루트에서 fastapi/ 로 이동 ([Task 1] 스캐폴딩).
#
# Python 3.13 (pyenv). 설치 (저장소 루트에서 — .venv 는 루트에 둔다, scripts/check_requirements_updates.py 가 그 경로를 본다):
#   python3.13 -m venv .venv && source .venv/bin/activate
#   pip install -r fastapi/requirements.txt
#
# 모델 받기:
#   python scripts/download_models.py
#
# 버전은 매주 월요일 09:00에 로컬 launchd(com.callguard.requirements-check)가 PyPI
# 최신 버전을 확인해 이 파일과 .venv를 자동 갱신한다 (scripts/check_requirements_updates.py).
# 결과는 logs/requirements_check.log 참고. 수동 실행: .venv/bin/python scripts/check_requirements_updates.py

torch==2.13.0
transformers==5.15.1
huggingface_hub==1.28.0
sentencepiece==0.2.2
accelerate==1.14.0
pytest==9.1.1
google-cloud-speech==2.40.0

# --- FastAPI 코어 런타임 + 구조 하네스 ---
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.4
import-linter==2.13
httpx==0.28.1
