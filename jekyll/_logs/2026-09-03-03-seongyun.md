---
date: 2026-09-03
author: "정성윤"
person: seongyun
seq: 3
---

- **`main` 을 `PM` 에 동기화했다** — 13커밋(프론트 PR #47·#48) fast-forward. `PM` 에만 있던
  커밋이 없어 병합 커밋 없이 들어왔다. 작업 중이던 변경은 스태시로 보존했고 **`STATE.md` 한
  파일만 충돌**했다. **줄 주인 기준으로 갈랐다** — 인프라 줄은 내 변경(base 대비 upstream 이
  그대로였다), `apps/` 줄은 조서희 변경(내 쪽이 base 그대로였다). `open-items` 는 자동 병합됐다.
- **⚠ `www` 공개 랜딩이 6일째 낡아 있었다 — 오늘 제일 큰 발견이다.**
  라이브 번들을 받아 문자열을 대조했더니 `확보한 데이터 네 곳만 다룬다.`·`한별금융 이용약관
  제2조 1항` 이 들어 있었다 — **`9265e79`(08-27) 빌드**다. `9eef20a`(08-31 다산 단일 랜딩)가
  반영되지 않았다. **즉 `decisions/201` 로 폐기한 금융·4개 도메인 서사가 공개 주소에 그대로
  떠 있었다.**
- **원인은 Git 연동이 붙은 적이 없다는 것이다.** 근거 셋 — ① GitHub Deployments 최근 15건이
  **전부 `github-pages`**, Vercel 이 만든 배포 기록 **0건** ② 저장소에 `.vercel/`·`vercel.json`
  **없음**, 별도 랜딩 저장소도 없음 ③ `_logs/2026-08-28-05-seohee.md` 가 *"코드가 아니라
  **배포 사실을 공유받아** 반영했다"* 고 적고 있다 — 조서희 본인도 배포자가 아니었다.
  **08-28 에 누군가 로컬에서 CLI 로 한 번 올린 것이 전부였다.**
- **수동 배포는 멈춰도 아무 일이 일어나지 않는다.** 이게 6일이 간 이유다. `pages.yml` 은
  안 돌면 바로 드러나는데(문서가 안 바뀐다) 랜딩은 아무도 안 봤다. **자동으로 바꾼다.**
- **저장소 소유권이 `solidbob02` → `SeongYuna` 로 넘어왔다**(사용자가 GitHub 에서 수행).
  이전 후 전수 점검 — 룰셋 `main` ✅(필수검사 `jekyll`·`ai`·`server`, PR 필수, 삭제·force push
  금지) · Pages ✅(`cname=docs.solidbob.cloud`, HTTPS 강제, `build=workflow`) ·
  `docs.solidbob.cloud` HTTP 200 ✅ · 협업자 4명 write 전부 유지 ✅.
  **아무도 접근을 잃지 않았고 CI·배포도 끊기지 않았다.**
- **이걸로 막혀 있던 게 하나 풀렸다.** 전에는 정성윤이 `push` 만 있고 `admin` 이 없어서
  Vercel GitHub App 을 **`solidbob02` 개인 계정에 설치할 방법이 없었다**(개인 계정 App 설치는
  계정 주인만 가능). 우회로 둘 다 admin 을 요구했다 — 포크는 저장소가 갈리고, Actions 배포는
  `VERCEL_TOKEN` 을 Secrets 에 넣어야 한다. **이제 직접 한다.**
- **`_project/decisions/106` 을 남겼다** — 소유권 이전 + 배포 계정 단일화. 6단계 절차와
  되돌리는 법을 적었다. **다운타임을 마지막에 몰아넣는 순서**로 짰다(새 프로젝트를 먼저
  띄워 `*.vercel.app` 에서 확인 → 그 다음 도메인 해제). ⚠ Vercel 이 TXT 검증 챌린지를 주면
  해제 없이 붙을 수 있어 **4단계를 먼저 시도**하게 적었다.
- **낡은 서술을 정리했다** — `CLAUDE.md`(룰셋 권한이 `solidbob02` 몫이라던 줄) ·
  `STATE.md`(「협업자, admin 아님」) · `README.md`(사이트 주소가 아직 `github.io` 프로젝트
  페이지였다 → `docs.solidbob.cloud`) · `jekyll/index.markdown` 표지 · 문서 링크 14개 파일.
  `git remote` 도 `SeongYuna/…` 로 바꿨다(그동안 리다이렉트로 동작 중이었다).
- **⚠ 남의 백로그 티켓 9건은 일부러 안 고쳤다.** `github.com/solidbob02/…` 링크가 남아 있지만
  **리다이렉트로 살아 있고**, `§4` 가 "남의 티켓 파일은 건드리지 않는다"고 정한 이유가 병합
  충돌이다. 이득보다 비용이 크다. `open-items:134` 의 1건도 남겼다 — **해결된 이슈의 원인
  기록**(옛 주소가 어떻게 깨졌는지)이라 절대 원칙 8 대상이다.
- **`_project/dns-solidbob-cloud.md` 를 실측으로 갱신했다.** 세 군데가 낡아 있었다 —
  ① 목표 상태 표가 `call CNAME solidbob02.github.io` 였다(**`102` 안인데 `104` 가 철회했다** —
  지킬은 `docs`, `call` 은 프론트다) ② 「apex TXT 토큰 한 글자 빠짐(19자)」이 **지금은 20자로
  맞다**(누가 언제 고쳤는지 기록이 없다 — 당시 기록 절은 그대로 남겼다) ③ `api`·`call` 현재
  상태가 없었다. **`call` 은 여전히 NXDOMAIN 이다.**
- **로컬로 프론트 두 화면을 띄워 확인했다** — `apps/dashboard` :5173 · `apps/platform` :3000.
  `apps/platform` 은 `node_modules` 가 없어 `npm ci` 했다(이번 머지가 Tailwind v4 를 새로 넣었다).
  로컬 랜딩 `<title>` 은 `CallGuard — 다산콜센터 상담사 어시스트`, 라이브는 아직
  `CallGuard — 실시간 상담원 어시스트` 다 — 차이가 그대로 보인다.
- **검증**: `apps/platform` 프로덕션 빌드 통과(`tsc --noEmit && vite build`, 35모듈,
  158KB/gzip 51KB). `dist/` 는 `.gitignore:63` 이 막고 있어 저장소는 깨끗하다.
  ⚠ **Vercel 배포 자체는 하지 않았다** — 계정 이전이 먼저다.
- **남은 것**: [w3-vercel-account-migration](/backlog/w3-vercel-account-migration/) —
  **조서희가 `solidbob.cloud`·`www` 를 놓아줘야 진행된다**(한 도메인은 한 Vercel 계정에만 붙는다).
  놓아준 뒤 절차는 `decisions/106`. 조서희를 새 Vercel 팀에 초대하는 것도 잊지 않는다 —
  안 하면 `frontend` Preview 를 못 보게 돼 **있던 것을 뺏는 셈**이 된다.
- **[미결](/open-items/)에 3건 올렸다** — ① `docs` CNAME 을 `seongyuna.github.io` 로
  (지금 동작하지만 옛 계정이 사라지면 죽는다) ② **연동 후 실제로 배포가 도는 것을 보기 전까지
  「됐다」가 아니다** ③ `api.solidbob.cloud` TLS 미발급 — Railway 를 고칠지 `server` 로 일원화할지.
