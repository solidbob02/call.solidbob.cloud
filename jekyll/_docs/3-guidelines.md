---
layout: page
title: 3. 주요 개발 수행 지침
nav_order: 3
updated: 2026-08-25
owner: lead
status: agreed
---

## 1) 일반 사항

### 인터페이스 계약 우선

1주차에 모듈 간 메시지 스키마 **3종(전사 · 카드 · 종결)** 을 확정한다. **이것이 병렬 작업의 전제 조건이다.**
계약 문서가 `agreed` 상태가 된 뒤의 변경은 결정 기록을 남긴다. → [인터페이스 계약]({{ '/docs/interface-contract/' | relative_url }})

### 평가 하네스를 먼저 깔고 손을 뗀다

하네스 **설계**는 백엔드·AI 담당이 1주차에 끝내고, **운영·유지는 인프라 담당이 이관받는다.** 이후 모듈 개발은 각자 진행한다.

- 각자 모듈을 고칠 때마다 숫자가 자동으로 나온다
- "제 방식이 더 나은 것 같은데요" 같은 논쟁이 측정으로 대체된다
- 대신 짜주지 않고 리뷰로 올린다

### 검증 담당을 분리한다 (보완지시서 3)

F-2의 판정 정확도 100%는 **팀이 만든 규정을, 팀이 만든 게이트가, 팀이 만든 골든셋으로 검증**하면 자기충족적 수치가 된다. 3인이므로 세 역할을 한 사람씩 나눠 맡을 수 있다.

| 역할 | 원칙 |
|---|---|
| 규정 문서 작성 | 실제 통신사 약관의 조 번호 체계·문장 형식·용어를 따른다 |
| 요건 스키마 구현 | **규정을 쓴 사람이 짜지 않는다.** 같은 사람이면 해석 오류가 드러나지 않는다 |
| 골든셋 F-2 케이스 작성 | **규정 작성자가 아닌 사람이 만든다.** 같은 사람이면 규정의 빈틈이 그대로 통과된다 |

세 가지를 지켰다는 사실을 문서에 적는다.

### 기록이 곧 산출물

본 저장소는 제품 코드 저장소가 아니라 **기록 저장소**다. 규칙은 `CLAUDE.md`가 정본이다.

| | 무엇 | 시간 축 | 수정 |
|---|---|---|---|
| [개발 로그]({{ '/log/' | relative_url }}) | 그날 무엇을 왜 했는가 | 과거·쌓기만 함 | 안 함 |
| 문서 (본 제안서 각 절) | 지금 무엇이 맞는가 | 현재 | 계속 덮어씀 |
| [진행 상황]({{ '/progress/' | relative_url }}) | 어디까지 왔고 숫자는 얼마인가 | 현재 | 계속 갱신 |
| [미결 항목]({{ '/open-items/' | relative_url }}) | 아직 정하지 못한 것 | 현재 | 해소 시 닫음 |

## 2) 개발 표준 및 산출물

### 담당별 산출물

| 담당 | 범위 | 산출물 |
|---|---|---|
| **AWS·인프라** | WebSocket 게이트웨이, STT 연동, 화자 분리, **C-5 P1~P5**, MySQL 스키마, Docker·배포, 레이턴시 측정, **CI 운영** | 전사 스트림 API + 배포 환경 |
| **백엔드·AI** | ES 인덱스·하이브리드 검색·리랭킹, 트리거 판정, 생성, 컴플라이언스 분류기, **C-5 P6~P7(NER)**, F-2 게이트, 하네스 설계 | 검색·추천 API + 성능 리포트 |
| **앱·프론트엔드** | React 대시보드(자막·카드·경고·종결 모달), WebSocket 클라이언트, 데모 시나리오, 결과 시각화 | 동작하는 화면 + 그래프 |
| 공동 | 골든셋 작성·라벨링, 발표 자료 | 골든셋 150건 |

담당자 이름과 부하 완화 조치는 [4. 개발 일정 및 추진 체계]({{ '/docs/4-schedule/#2-조직-구성-및-역할-분담' | relative_url }}).

### 저장소 구조

```
CLAUDE.md            작업 규칙 (세션 시작 시 필독)
docs/                기술 문서 — 구현자용. 사이트에 게시되지 않는다
  architecture.md    아키텍처 + 데이터 모델 + ERD 재생성 절차
  erd/               schema.mmd(정본) · schema.png(산출물)
_project/            비공개 — 기획서 원본·보완지시서, STATE.md, 결정 기록, 템플릿
jekyll/              지킬 사이트 루트 — 지킬 명령은 전부 이 안에서 실행
  _posts/            개발 로그 — 과거, 수정하지 않음
  _docs/             제안서 각 절 + 세부 문서 — 현재 사실, 계속 덮어씀
  _data/             milestones.yml 진행률 · metrics.yml 수치 · open_items.yml 미결 항목
```

저장소 루트에는 지킬 사이트와 함께 앞으로 추가될 애플리케이션 코드가 나란히 놓인다.
브랜치는 역할별로 나눈다 — `PM` / `frontend` / `backend` / `flutter`.

### 커밋 규칙

```
log(w3): 스트리밍 STT 파이프라인 연결
docs(search): 인터페이스 계약 v2 확정
data(metrics): 오류율 10% 구간 Recall@5 실측 반영
```

타입: `log` | `docs` | `data` | `code` | `rule` | `chore`

### 결정 기록

되돌리기 어려운 선택(지식베이스 도메인, 트리거 전략, 청킹 방식, 임베딩 모델, 기획서와 다르게 간 지점)은 `_project/decisions/`에 **맥락 / 선택지 / 결정 / 근거 / 되돌리는 법** 형식으로 남긴다.

## 3) 품질 관리 및 테스트

### 채점 원칙 (변경 금지)

1. **LLM을 채점자로 쓰지 않는다.** 답을 만든 모델이 자기 답을 심판하면 순환이 된다.
2. **여러 번 실행한 값 중 최저치를 기준선으로 고정한다.** 생성 모델은 같은 입력에도 답이 달라진다.
3. **기준선 미달은 CI 실패.** 목적은 개선이 아니라 회귀 방지다.
4. **C-5·F-2의 절대 규칙은 1건이라도 뚫리면 실패다.** 평균값으로 평가하지 않는다.
5. **모든 지표는 정답 정의가 코드로 표현 가능해야 한다.** 표현할 수 없으면 지표가 아니다 — 트리거 허용 창을 정의한 이유가 이것이다.

여기에 이 저장소의 규칙 하나를 더한다.

6. **측정하지 않은 수치를 기록하지 않는다.** 미측정은 `null`로 두고 화면에 "미측정"으로 표시한다.

### 목표 기준선

| 영역 | 지표 | 기준선 |
|---|---|---|
| 검색 | Recall@5 | ≥ {{ site.data.metrics.targets.retrieval.recall_at_5_clean }} (오류 0%) / ≥ {{ site.data.metrics.targets.retrieval.recall_at_5_err10 }} (오류 10%) |
| 검색 | MRR | ≥ {{ site.data.metrics.targets.retrieval.mrr }} |
| 트리거 | 적절 발동률 (종료 후 0~{{ site.data.metrics.targets.trigger.window_ms_max }}ms) | ≥ {{ site.data.metrics.targets.trigger.fire_precision }} |
| 트리거 | 조기 / 지연 발동률 | 각각 별도 집계 |
| 트리거 | 발동 지연 분포 (p50/p95) | 기록 |
| 생성 | 환각 수치 발생 | 150문항 중 {{ site.data.metrics.targets.generation.hallucinated_numbers_max }}건 이하 |
| 생성 | 출처 표시율 | 100% |
| 컴플라이언스 | 재현율 | ≥ {{ site.data.metrics.targets.compliance.recall }} |
| 컴플라이언스 | 정밀도 | ≥ {{ site.data.metrics.targets.compliance.precision }} |
| **C-5** | **P1~P7 패턴 마스킹 누락** | **{{ site.data.metrics.targets.masking.missed_count }}건 — 절대 규칙** |
| C-5 | 과잉 마스킹률 | 참고 기록 (목표 아님) |
| C-5 | 오류율별 마스킹 재현율 | 곡선 기록 |
| 성능 | 내부 처리 p95 | ≤ {{ site.data.metrics.targets.latency.internal_p95_ms }}ms |
| 성능 | E2E 체감 지연 | 측정·기록 (목표 미설정) |
| 성능 | 통화당 토큰 비용 | 측정·기록 |
| **F-2** | 종결 요건 판정 정확도 | **100% — 절대 규칙** *(조건부)* |
| F-2 | 근거 규정 검색 정확도 | ≥ {{ site.data.metrics.targets.f2.evidence_retrieval }} *(조건부)* |
| F-1 / G-1 | 탐지 성능 | **측정 불가 — 아래 참조** |

컴플라이언스와 마스킹에서 재현율을 우선한 것은 의도적이다. **누락(FN)이 오탐(FP)보다 위험**하다는 도메인 비대칭을 지표 우선순위로 명시했다.

### 측정할 수 없는 것

검증할 수 없는 것을 검증한 것처럼 제시하지 않는다. 상세는 [평가 설계]({{ '/docs/evaluation/#4-측정할-수-없는-것' | relative_url }}).

### 수치 기록 형식

모든 성능 수치는 `jekyll/_data/metrics.yml` 한 곳에서 온다. 값 하나에 다음 네 가지가 반드시 붙는다.

```yaml
value: 0.66
measured_at: 2026-09-20      # 언제
commit: a1b2c3d              # 어느 코드로
command: "python eval/run.py --error-rate 0.10"   # 어떻게 재현하나
n: 150                       # 표본 수
```

넷 중 하나라도 못 채우면 그 숫자는 아직 기록할 준비가 되지 않은 것이다.
