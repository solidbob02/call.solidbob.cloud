---
title: "V3·V4 실측 — STT 숫자 출력 형태 / 부분 결과 지연"
assignee: "류준"
role: "ai"
status: "done"
sprint: 1
priority: 4
date: 2026-08-25
---

- V3: 한국어 긴 숫자열 출력 형태 → C-5 정규화 단계 설계의 전제
- V4: 스트리밍 부분 결과 지연 → E2E 지표 기준값

전제(결제 계정·키·비용 방어선)는 [별도 티켓](/backlog/w1-stt-billing-quota/)에서 처리했다.

## 실측 결과 (2026-08-25, 류준)

AI Hub 실제 통화 음성으로 측정했다. 재현 스크립트: `scripts/test_stt_v3.py`, `scripts/test_stt_v4_streaming.py`.

**V3 — 한국어 숫자 출력 형태**

오디오 3건 확인 결과 **케이스마다 다르다.** 완전 정규화 / 부분 정규화 / 오인식이 혼재한다.
특히 **자릿수 낭독형(인증코드류)은 저품질 통화 음성에서 오인식 위험이 크다.**

→ C-5 정규화 파이프라인은 "①연속 숫자열 판정 → ②한글 수사 변환" 한 가지 순서만 가정해서는 안 된다.
   세 패턴을 모두 통과시키는 설계가 필요하다.

**V4 — 스트리밍 부분 결과 지연**

20.58초 실통화 음성을 실시간 페이싱으로 전송해 측정했다.

| 구간 | 값 |
|---|---|
| 첫 interim 결과 도착 | **962ms** |
| 최종 결과 (발화 종료 기준) | **+346ms** |

→ E2E 체감 지연의 기준값이다. 내부 처리 예산(p95 ≤ 1,000ms)과 별개로 측정·기록한다.

## 결과 기록 위치

`jekyll/docs/05-데이터확보계획.markdown` 의 5.6절 V1~V4 표(V1·V2 가 이미 이 형식으로 기록돼 있다),
`jekyll/open-items.markdown` 체크박스, `jekyll/progress.markdown` 당일 항목.
